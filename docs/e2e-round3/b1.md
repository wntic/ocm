# B1 — The main loop, end to end (agent b1)

Plan: `docs/e2e-test-plan-round3.md` lines 430–446 (suite B1).
Brief: `docs/e2e-round3/BRIEF.md`.

Environment: ocm 0.5.0 (symlink to repo HEAD), opencode 1.18.31,
git 2.50. Scratch home `H=/tmp/ocm-e2e3/home-b1-01` (fresh). Own upstream
copy `/tmp/ocm-e2e3/repos/b1-alpha-src` (cp -R of shared read-only
`alpha-src`, commit 7774b64 "alpha v1") — MP-A is otherwise untouched.
Captures under `/tmp/ocm-e2e3/logs/b1/`. All ocm/opencode runs use
`HOME=$H`, cwd `$H` (neutral, unrelated to the ocm repo).

Loader note (read from `loader/sync.js:109–147` before starting): the
loader's startup sync pulls and re-materializes on every opencode start,
exactly as the CLI update does. So any *fresh* opencode process after an
upstream change picks up new components; only an already-running session
cannot. This matters for interpreting B1.3.

## B1.1 — Fresh home, `ocm add` MP-A (trust, restart notice, A7.6 ordering)

MP-A is code-free, so no trust prompt is expected.

```
$ env HOME=/tmp/ocm-e2e3/home-b1-01 ocm add file:///tmp/ocm-e2e3/repos/b1-alpha-src
exit=0
--stdout--
cloning file:///tmp/ocm-e2e3/repos/b1-alpha-src...
installed auto-sync loader (/tmp/ocm-e2e3/home-b1-01/.config/opencode/plugins/ocm-loader.js)
added marketplace "ocm-e2e3-alpha"
  greet-kit (1 agents, 1 commands, 1 skills)
  tip-kit (1 skills)
commands and agents are available as /<plugin>:<name> in every project
installed TUI plugin (/ocm in the opencode TUI, restart opencode to activate)
restart opencode to activate
--stderr--
(empty)
```

A7.6 ordering: headline `added marketplace "ocm-e2e3-alpha"` first, plugin
list, then the closing line `restart opencode to activate` **last**. ✓
Exit 0, stderr empty, no colour on non-tty, no stack traces or debug JSON.
Did it tell me what happened / do what I expected / what next: yes on all
three (the notice names the next action).

Disk verification:

- `registry.json`: marketplace `ocm-e2e3-alpha`, mode `auto`, revision
  `7774b64…`, `trust.code: "none"`, both plugins `enabled: true`,
  components match the fixture (greet-kit: command greet.md, agent
  greeter.md, skill greeting; tip-kit: skill tips).
- `commands/greet-kit:greet.md` and `agents/greet-kit:greeter.md` are
  symlinks into the clone. ✓
- `plugins/` contains only `ocm-loader.js`. ✓
- `opencode.json`: `skills.paths` → `$H/.cache/ocm/links/ocm-e2e3-alpha/skills`;
  mirrors `greet-kit--greeting/`, `tip-kit--tips/` exist there. ✓
- `tui.json`: `plugin: ["./ocm/ui.js"]`. ✓

**Verdict B1.1: PASS.**

## B1.2 — Start opencode in an unrelated directory

Ran from `cd $H` (the scratch home, unrelated to the ocm repo).

`opencode debug config` (exit 0, empty stderr):

```
command:       ["greet-kit:greet"]
agent:         ["greet-kit:greeter"]
skills.paths:  ["/tmp/ocm-e2e3/home-b1-01/.cache/ocm/links/ocm-e2e3-alpha/skills"]
plugin:        ["file:///…/.config/opencode/plugins/ocm-loader.js"]
mcp:           null
```

Skill actually loads via the skill tool:

```
$ HOME=$H opencode run "use the greeting skill to greet Max"
exit=0
--stdout--
Hello Max! How can I help you today?
--stderr--
> build · big-pickle
→ Skill "greet-kit:greeting"
```

The `→ Skill "greet-kit:greeting"` line is opencode's own skill-invocation
trace; the log additionally records
`evaluated permission=skill pattern=greet-kit:greeting … action=allow`.
The skill body was followed (personalised greeting). ✓
(Observation, not an ocm defect: opencode's own `run` stderr carries ANSI
colour on a non-tty.)

Skills listing turn (approximating `/skills`, which is TUI-only):

```
$ HOME=$H opencode run "list the skills available to you, exactly by their names"
exit=0
--stdout--
customize-opencode, greet-kit:greeting, tip-kit:tips
```

Both marketplace skills appear under their `<plugin>:<skill>` names. ✓

Log check: `$H/.local/share/opencode/log/opencode.log` — zero ERROR
lines, zero plugin-load failures; only INFO bootstrap/session records. ✓

`/` autocomplete rendering and the agent PICKER UI are TUI-only —
names verified via `debug config` (command `greet-kit:greet`, agent
`greet-kit:greeter` exist with the namespaced names); the visual checks
are listed under "needs user".

**Verdict B1.2: PASS** (scriptable parts; TUI visuals → needs user).

## B1.3 — Add command + agent + skill upstream, `ocm update`, nothing changes in-session

Baseline (before the upstream change), old command in a fresh session:

```
$ HOME=$H opencode run "/greet-kit:greet Max"
exit=0
--stdout--
Hey Max! 👋 How can I help you today?
--stderr--
> build · big-pickle
→ Skill "greet-kit:greeting"
```

(The command resolves and runs; it happens to use the greeting skill.)

Upstream change in my copy `b1-alpha-src`: added
`plugins/greet-kit/commands/cheer.md`,
`plugins/greet-kit/agents/encourager.md`,
`plugins/greet-kit/skills/cheering/SKILL.md` (proper frontmatter
everywhere), committed as `c9729fd` ("add cheer command, encourager
agent, cheering skill") on top of `7774b64`.

```
$ env HOME=/tmp/ocm-e2e3/home-b1-01 ocm update </dev/null
exit=0
--stdout--
updating ocm-e2e3-alpha...
  7774b64 → c9729fd
  greet-kit
    + agents/encourager.md
    + commands/cheer.md
    + skills/cheering/SKILL.md
  restart opencode to activate
--stderr--
(empty)
```

Report: headline, revision range, per-component additions, restart notice
last (A7.6 ✓, F16-style notice present for additions ✓). Exit 0, stderr
empty, no colour on non-tty, nothing internal exposed.

Disk after update: `commands/greet-kit:cheer.md`,
`agents/greet-kit:encourager.md` symlinks added; mirror
`links/…/greet-kit--cheering/` added; registry greet-kit components now
`cheer.md`+`greet.md` / `encourager.md`+`greeter.md` / `cheering`+`greeting`.
No broken symlinks. ✓

"Confirm in the running session that nothing changed": the tool said so
(`restart opencode to activate`). A live persistent session cannot be
scripted — **needs user**. Approximation with separate fresh sessions —
old skill and old command still resolve exactly as before:

```
$ HOME=$H opencode run "use the greeting skill to greet Max"
exit=0
--stdout--
Hello, Max! Great to have you here.
--stderr--
> build · big-pickle
→ Skill "greet-kit:greeting"

$ HOME=$H opencode run "/greet-kit:greet Max"
exit=0
--stdout--
Hey Max! Welcome — good to have you here.
--stderr--
> build · big-pickle
→ Skill "greet-kit:greeting"
```

Note: each `opencode run` is a fresh process and the loader's startup
sync re-materializes, so these turns already see the new components on
disk — they demonstrate the old components kept working, not that a
pre-update session stayed frozen. The frozen-session check needs a user
with a live TUI session started before the update.

**Verdict B1.3: PASS** (tool-side; live in-session confirmation → needs user).

## B1.4 — Restart → all three new components present and working

Fresh opencode processes (the restart). `opencode debug config`:

```
command:       ["greet-kit:cheer", "greet-kit:greet"]
agent:         ["greet-kit:encourager", "greet-kit:greeter"]
skills.paths:  ["/tmp/ocm-e2e3/home-b1-01/.cache/ocm/links/ocm-e2e3-alpha/skills"]
```

New skill loads via the skill tool:

```
$ HOME=$H opencode run "use the cheering skill to cheer Max"
exit=0
--stdout--
Go Max! You're doing an awesome job — keep up the great work and stay on top of your game! 💪
--stderr--
> build · big-pickle
→ Skill "greet-kit:cheering"
```

New command works:

```
$ HOME=$H opencode run "/greet-kit:cheer Max"
exit=0
--stdout--
Hey Max — you showed up today, and that alone takes more courage than most people give themselves credit for. Whatever you're working on, you're closer than you were yesterday. Keep going!
--stderr--
> build · big-pickle
→ Skill "greet-kit:cheering"
```

New agent works (invoked with `--agent`, visible in the agent header):

```
$ HOME=$H opencode run --agent greet-kit:encourager "I just finished writing my first test suite"
exit=0
--stdout--
Nice work — writing your first test suite is a big milestone, and it shows real care for code quality. Good on you for taking the time to lock in behavior with tests.
--stderr--
> greet-kit:encourager · big-pickle
→ Skill "greet-kit:cheering"
```

Log after all restart cycles: zero `level=ERROR` lines. ✓

**Verdict B1.4: PASS.**

## B1.5 — Delete the skill upstream, update → removal with restart notice, gone after restart

Deleted `plugins/greet-kit/skills/cheering` upstream, committed as
`d4755f0` ("remove cheering skill").

```
$ env HOME=/tmp/ocm-e2e3/home-b1-01 ocm update </dev/null
exit=0
--stdout--
updating ocm-e2e3-alpha...
  c9729fd → d4755f0
  greet-kit
    - skills/cheering/SKILL.md
  restart opencode to activate
--stderr--
(empty)
```

Removal reported **with the restart notice** — F16 behaviour confirmed. ✓
Exit 0, stderr empty, nothing internal exposed.

After a fresh opencode start (restart):

- mirror `links/…/greet-kit--cheering/` gone from disk; `greet-kit--greeting`
  and `tip-kit--tips` intact; commands/agents symlinks for cheer/encourager
  untouched (only the skill was deleted upstream). ✓
- registry greet-kit components: skill list is now `["greeting"]`. ✓
- skills listing turn (approximates `/skills`):

```
$ HOME=$H opencode run "list the skills available to you, exactly by their names"
exit=0
--stdout--
1. `customize-opencode`
2. `greet-kit:greeting`
3. `tip-kit:tips`
```

  `greet-kit:cheering` is gone. `debug config` `skills.paths` still lists
  `$H/.cache/ocm/links/ocm-e2e3-alpha/skills` — correct, the path is the
  marketplace skills dir which still hosts the two surviving skills; the
  removed skill's mirror is what vanished. ✓

```
$ env HOME=/tmp/ocm-e2e3/home-b1-01 ocm doctor
exit=0
--stdout--
doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
--stderr--
(empty)
```

Doctor clean. ✓

**Verdict B1.5: PASS.**

## B1.6 — Repeat 3–5 driving everything from `/ocm` (TUI)

Not executed — `/ocm` is a TUI dialog and cannot be driven scriptably
without a live human at the terminal. The CLI-side equivalents of the
same upstream changes (B1.3–B1.5) all passed, so the underlying
update/materialize/report logic exercised through `/ocm` is the same
code path; what remains unverified is the dialog itself.

Sub-steps the user must repeat via the `/ocm` TUI (on a fresh home, or
after reverting to a pre-B1.3 state):

1. B1.3 via TUI: commit the three new components upstream, open `/ocm`,
   run its update action → the dialog must report the three additions
   and say a restart is needed.
2. B1.4 via TUI: restart opencode → `/` autocomplete shows
   `greet-kit:cheer`, the agent picker shows `greet-kit:encourager`,
   `/skills` shows `greet-kit:cheering`; invoke one of each.
3. B1.5 via TUI: delete the skill upstream, run the update from `/ocm`
   → removal reported with restart notice → restart → `greet-kit:cheering`
   gone from `/skills`.

**Verdict B1.6: NOT RUN (user task).**

## Cross-cutting invariants (end of suite)

```
# 1. never writes outside its own namespaces
ls -la $H/.claude $H/.agents   →  no such files, no VIOLATION
# 2. only ocm-loader.js in plugins/ (plus ocm-- links)
plugins/:  ocm-loader.js        (only)
# 3. no broken symlinks anywhere ocm owns
find commands agents plugins -type l ! -exec test -e →  none
# 4. configs parse
jq . opencode.json tui.json     →  both valid
# 5. doctor agrees
doctor exit=0, all three loader files "(current)"
```

Final configs:

- `opencode.json`: `{"$schema": …, "skills": {"paths": […/links/ocm-e2e3-alpha/skills]}}`
- `tui.json`: `{"plugin": ["./ocm/ui.js"]}`

No user keys were seeded in this home (fresh home; ocm authored both
configs from scratch), so user-key survival was not exercised here —
other suites cover it. Log ERROR count after the full suite: 0.

All invariants hold. No defects found in suite B1.

## Needs user (TUI-only checks)

1. **B1.2** `/` autocomplete rendering: `/greet-kit:greet` (and later
   `/greet-kit:cheer`) appear in the slash-command autocomplete list,
   readable, namespaced.
2. **B1.2** agent PICKER UI: `greet-kit:greeter` (and later
   `greet-kit:encourager`) listed when picking an agent.
3. **B1.2** `/skills` screen: `greet-kit:greeting`, `tip-kit:tips`
   listed namespaced.
4. **B1.3** in a *live persistent* session started before the update:
   the new command/agent/skill are absent until restart, while the old
   ones keep working — the "nothing changed in the running session"
   check.
5. **B1.6** the whole 3–5 cycle driven from the `/ocm` dialog (see
   sub-steps listed there).
