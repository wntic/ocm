# A8 transcript — test agent "a8" (suites A8 + A9, round 3)

Binary: /opt/homebrew/bin/ocm (v0.5.0, symlink to repo HEAD).
Scratch home: `/tmp/ocm-e2e3/home-a8-01` unless a step says otherwise.
Capture discipline: every exit-code/stream assertion captured via run() wrapper
(stdout/stderr separated, exit code echoed). TTY behaviour via `expect`.

## Suite A8 — Validate hardening (spec 24) · F24, F49, F50, F51

### A8.1 — `ocm validate` outside any marketplace (F24)

H=/tmp/ocm-e2e3/home-a8-01
```
$ cd /tmp && env HOME=$H ocm validate
exit=1
--stdout--
--stderr--
error: not an ocm marketplace directory (no plugins/ and no manifest found)
  run ocm validate at the marketplace repository root
```
Exit 1, stderr carries a single actionable error naming the next action, stdout empty. Judged: did it tell me what happened (yes), do the expected thing (yes), know what to do next (yes — "run ocm validate at the marketplace repository root"). **A8.1 PASS** (F24: message present, exit 1, nothing else).

### A8.2 — `ocm validate` from a plugin dir inside a marketplace

```
$ cd /tmp/ocm-e2e3/fixtures/local-mp/plugins/demo-kit && env HOME=$H ocm validate
exit=0
--stdout--
validating /private/tmp/ocm-e2e3/fixtures/local-mp
validate /private/tmp/ocm-e2e3/fixtures/local-mp
--stderr--
```
Pointer line `validating <root>` present, normal (empty) findings follow, exit 0. Also with real findings, from a plugin dir of a broken fixture:

```
$ cd /tmp/ocm-e2e3/fixtures/broken/empty-desc/plugins/* && env HOME=$H ocm validate
exit=1
--stdout--
validating /private/tmp/ocm-e2e3/fixtures/broken/empty-desc
validate /private/tmp/ocm-e2e3/fixtures/broken/empty-desc
  error   plugins/p-one/plugin.json: "description" is required and must be a non-empty string
  warning plugins/p-one/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
1 error, 1 warning
--stderr--
```
**A8.2 PASS** — pointer line + normal findings, correct exits.

### A8.3 — marketplace with `plugins/` but zero plugins

Built `/tmp/ocm-e2e3/a8-fixtures/empty-mp` (marketplace.json `{ "name": "empty-mp" }` + empty `plugins/` dir).

```
$ cd /tmp/ocm-e2e3/a8-fixtures/empty-mp && env HOME=$H ocm validate
exit=0
--stdout--
validate /private/tmp/ocm-e2e3/a8-fixtures/empty-mp
--stderr--
```
Zero findings, exit 0, no noise about the empty dir. **A8.3 PASS**.

### A8.4 — containment: `mcpServers: "../other/mcp.json"`

```
$ cd /tmp/ocm-e2e3/fixtures/broken/containment && env HOME=$H ocm validate
exit=1
--stdout--
validate /private/tmp/ocm-e2e3/fixtures/broken/containment
  error   marketplace.json: "mcpServers" must be a ./-relative path
1 error, 0 warnings
--stderr--
"is not a JSON object" not present in either stream: confirmed
```
**A8.4 PASS** — containment error names the field and the rule; forbidden internal string absent; exit 1.

### A8.5 — malformed plugin.json / malformed marketplace.json

```
$ cd /tmp/ocm-e2e3/fixtures/broken/bad-plugin-json && env HOME=$H ocm validate
exit=1
--stdout--
validate /private/tmp/ocm-e2e3/fixtures/broken/bad-plugin-json
  error   plugins/p-one/plugin.json: invalid JSON at position 34:19 — fix it or remove it; ocm requires this file to be readable
1 error, 0 warnings
--stderr--
```

```
$ cd /tmp/ocm-e2e3/fixtures/broken/bad-marketplace-json && env HOME=$H ocm validate
exit=1
--stdout--
validate /private/tmp/ocm-e2e3/fixtures/broken/bad-marketplace-json
  error   marketplace.json: invalid JSON at position 34:19
  warning plugins/p-one/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
1 error, 1 warning
--stderr--
```
**A8.5 partial** — plugin.json message matches spec §2 ("not valid JSON — fix it or remove it; ocm requires this file to be readable", prefixed with `invalid JSON at position 34:19`); malformed marketplace.json in `validate` gives an error + the $schema warning but NOT the loader's `entries below were not applied` warning. That warning lives in loader/manifest.js:69 (the add/sync path). Checking the add path:

#### A8.5b — malformed marketplace.json through `ocm add` (loader path)

```
$ env HOME=$H ocm add /tmp/ocm-e2e3/fixtures/broken/bad-marketplace-json
exit=0
--stdout--
installed auto-sync loader (/tmp/ocm-e2e3/home-a8-02/.config/opencode/plugins/ocm-loader.js)
added marketplace "bad-marketplace-json"
  p-one (1 commands)
commands and agents are available as /<plugin>:<name> in every project
installed TUI plugin (/ocm in the opencode TUI, restart opencode to activate)
restart opencode to activate
--stderr--
  warning: /private/tmp/ocm-e2e3/fixtures/broken/bad-marketplace-json/marketplace.json: not a valid JSON object — the manifest is ignored; the entries below were not applied
```
The `entries below were not applied` warning is the loader/add surface (stderr, exit 0, plugins still install via directory scan). `validate` reports malformed marketplace.json as an error `invalid JSON at position …` exit 1 — matching the phase-24 test (line 229–233) and the spec edge table. **A8.5 PASS** across both surfaces.

#### A8.5c — add on bad-plugin-json (loader warning) and containment (loader containment message)

```
$ env HOME=$H ocm add /tmp/ocm-e2e3/fixtures/broken/bad-plugin-json
exit=0
--stdout--
installed auto-sync loader (/tmp/ocm-e2e3/home-a8-03/.config/opencode/plugins/ocm-loader.js)
added marketplace "bad-plugin-json"
  p-one (1 commands)
commands and agents are available as /<plugin>:<name> in every project
installed TUI plugin (/ocm in the opencode TUI, restart opencode to activate)
restart opencode to activate
--stderr--
  warning: /private/tmp/ocm-e2e3/fixtures/broken/bad-plugin-json/plugins/p-one/plugin.json: not valid JSON — fix it or remove it; ocm requires this file to be readable
```

```
$ env HOME=$H ocm add /tmp/ocm-e2e3/fixtures/broken/containment
exit=1
--stdout--
--stderr--
no plugins found in /private/tmp/ocm-e2e3/fixtures/broken/containment
  expected plugins/<name>/{commands,agents,skills}/ at the repository root
  run `ocm scan /private/tmp/ocm-e2e3/fixtures/broken/containment` to see what was found
```
The shared `containment` fixture's p-one has no components, so `add` refuses earlier (`no plugins found`). Built `a8-fixtures/containment-full` (p-one with a command, escape target `other/mcp.json` exists) to test the spec §2 scenario on both surfaces:

```
$ cd /tmp/ocm-e2e3/a8-fixtures/containment-full && env HOME=$H ocm validate
exit=1
--stdout--
validate /private/tmp/ocm-e2e3/a8-fixtures/containment-full
  error   marketplace.json: "mcpServers" must be a ./-relative path
  warning plugins/p-one/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
1 error, 1 warning
--stderr--
```

```
$ env HOME=$H ocm add /tmp/ocm-e2e3/a8-fixtures/containment-full
exit=0
--stdout--
installed auto-sync loader (/tmp/ocm-e2e3/home-a8-04/.config/opencode/plugins/ocm-loader.js)
added marketplace "containment-full"
  p-one (1 commands)
commands and agents are available as /<plugin>:<name> in every project
installed TUI plugin (/ocm in the opencode TUI, restart opencode to activate)
restart opencode to activate
--stderr--
  warning: plugin "p-one": mcpServers "../other/mcp.json" must resolve inside the plugin directory
```
**A8.4 PASS** — on the add surface the spec §2 containment message appears verbatim (`warning: plugin "p-one": mcpServers "../other/mcp.json" must resolve inside the plugin directory`, stderr, exit 0, plugin installs without MCP servers); on the validate surface the field is refused with the documented `./`-relative syntax rule, exit 1. The forbidden string "is not a JSON object" appears nowhere on either surface. F50 (wrong reason named) closed.

### A8.6 — documented-contract check (F49): five random validate error strings vs README

Collecting the full defect-matrix output first (remaining fixtures, same $H):

```
$ cd /tmp/ocm-e2e3/fixtures/broken/dup-plugins && env HOME=/tmp/ocm-e2e3/home-a8-01 ocm validate
exit=1
--stdout--
validate /private/tmp/ocm-e2e3/fixtures/broken/dup-plugins
  error   marketplace.json: plugin "p-one" listed twice — the second entry is ignored today
  warning plugins/p-one/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
  warning plugins/p-two/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
1 error, 2 warnings
--stderr--
```
```
$ cd /tmp/ocm-e2e3/fixtures/broken/basename-clash && env HOME=/tmp/ocm-e2e3/home-a8-01 ocm validate
exit=1
--stdout--
validate /private/tmp/ocm-e2e3/fixtures/broken/basename-clash
  warning plugins/p-one/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
  warning plugins/p-two/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
  error   plugins/p-two: command "shared.md" is also shipped by plugin "p-one" — rename one
1 error, 2 warnings
--stderr--
```
```
$ cd /tmp/ocm-e2e3/fixtures/broken/name-mismatch && env HOME=/tmp/ocm-e2e3/home-a8-01 ocm validate
exit=1
--stdout--
validate /private/tmp/ocm-e2e3/fixtures/broken/name-mismatch
  warning plugins/p-one/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
  error   plugins/p-one/plugin.json: name "wrong-name" disagrees with the directory name "p-one"
1 error, 1 warning
--stderr--
```
Random pick (via `$RANDOM` over the pool of error strings seen in A8.1–A8.5 + the matrix runs above): (1) description required, (2) listed twice, (3) mcpServers ./-relative, (4) cross-plugin command collision, (5) name disagrees with directory.

| # | validate error (verbatim) | README states the rule? |
|---|---|---|
| 1 | `plugins/p-one/plugin.json: "description" is required and must be a non-empty string` | yes — README:206 "`description` is required — a non-empty string of at most 200 characters" (command-side: README:143) |
| 2 | `marketplace.json: plugin "p-one" listed twice — the second entry is ignored today` | yes — README:195 "so is a plugin listed twice in `plugins[]` (the first entry silently wins otherwise)" |
| 3 | `marketplace.json: "mcpServers" must be a ./-relative path` | yes — README:196-198 "`mcpServers` is ./-relative, resolving inside the plugin directory … `../` and absolute paths are rejected" |
| 4 | `plugins/p-two: command "shared.md" is also shipped by plugin "p-one" — rename one` | yes — README:66 "shipping the same command or agent name from two plugins is a validate error" (rule stated, string not quoted — the phase-24 invariant is rule-stated, never string-quoted) |
| 5 | `plugins/p-one/plugin.json: name "wrong-name" disagrees with the directory name "p-one"` | yes — README:208 "`name` is optional and, when present, must equal the directory name" |

All five rules stated. The rule-of-thumb sentence exists (README:267 "…enforces the documented contract, nothing else"). **A8.6 PASS** — F49 closed.

### A8.7 — README authoring subsections (F51)

- `### Command and agent frontmatter` (README:139): documents required field (`description`), recognized fields (`agent`, `mode`, `tools`, `model`, `extension`, `allowed-tools`, `$schema`), strict-YAML `": "` rule, empty-body error, `!` warning — with a complete worked command example (README:152-164).
- `### Category and tags` (README:224): documents `extensions["dev.wntic.ocm"]` `category`/`tags`, worked JSON example, what they surface in (`ocm search`, `ocm info`, TUI grouping), and the validate constraint (free-form in the `extensions` form; top-level `category`/`tags` is legacy — validate warns, extensions wins).

**A8.7 PASS** — F51 closed.

### A8 suite verdicts

- A8.1 PASS — F24 closed (error + next action, exit 1, nothing else)
- A8.2 PASS — pointer line `validating <root>` + normal findings, exits 0/1 correct
- A8.3 PASS — empty `plugins/` → zero findings, exit 0
- A8.4 PASS — F50 closed (containment message on the add surface; validate refuses with a true path rule; "is not a JSON object" absent everywhere)
- A8.5 PASS — plugin.json fix-or-remove error (validate) + warning (add); marketplace.json invalid-JSON error (validate, exit 1) + `the entries below were not applied` warning (add)
- A8.6 PASS — F49 closed (5/5 random rules documented)
- A8.7 PASS — F51 closed (both subsections with worked examples)

Formatting lens: clean throughout — errors/warnings on correct streams, no stack traces, no colour on non-tty, actionable next actions on every refusal. One observation (not a defect): `ocm validate` on an escaping `mcpServers` reports the `./`-relative syntax rule rather than the spec §2 containment wording; the containment wording appears on the add/sync surface. Both are true statements about the same input; the phase-24 test exercises the add surface for the containment string.

---

## Suite A9 — Display (spec 25) · F15, F17, F20, F43, F44, F44b, F47, F60

### A9.1 — F15: denied trust marks executable lines in `list`; `--json` unchanged

Home `home-a8-05`; MP-B = `file:///tmp/ocm-e2e3/repos/exec-src` (plugin `notify` + mcp server `everything`). Non-interactive add (prompt defaults to N = deny):

```
$ env HOME=$H ocm add file:///tmp/ocm-e2e3/repos/exec-src </dev/null
exit=0
--stdout--
cloning file:///tmp/ocm-e2e3/repos/exec-src...
installed auto-sync loader (/tmp/ocm-e2e3/home-a8-05/.config/opencode/plugins/ocm-loader.js)
marketplace "ocm-e2e3-exec" ships code that opencode will execute:
  mcp     notify/everything (local server: )
  plugin  notify/notify (plugins/notify/plugin/notify.js)
this code runs with your shell's permissions on every opencode start.
review it at /tmp/ocm-e2e3/home-a8-05/.cache/ocm/marketplaces/ocm-e2e3-exec
added marketplace "ocm-e2e3-exec"
  notify (1 plugins, 1 mcp servers)
commands and agents are available as /<plugin>:<name> in every project
installed TUI plugin (/ocm in the opencode TUI, restart opencode to activate)
--stderr--
trust this marketplace to run code? [y/N/skip]
  warning: blocked (untrusted): notify:notify.js not linked — run `ocm trust ocm-e2e3-exec` to approve
  warning: blocked (untrusted): notify:mcp/everything not installed — run `ocm trust ocm-e2e3-exec` to approve
```

```
$ env HOME=$H ocm list
exit=0
--stdout--
ocm-e2e3-exec
  source: file:///tmp/ocm-e2e3/repos/exec-src
  revision: 3b80558
  notify (0.3.0)
    plugins: notify.js
    mcp: everything
--stderr--
```

```
$ env HOME=$H ocm list --json
exit=0
--stdout--
{
  "version": 2,
  "marketplaces": {
    "ocm-e2e3-exec": {
      "url": "file:///tmp/ocm-e2e3/repos/exec-src",
      "dir": "/tmp/ocm-e2e3/home-a8-05/.cache/ocm/marketplaces/ocm-e2e3-exec",
      "local": false,
      "addedAt": "2026-09-15T16:48:25.277Z",
      "mode": "auto",
      "ref": null,
      "subdir": null,
      "revision": "3b805580ade679c362f69c2886ecbeb9965ef8c1",
      "syncIntervalMs": null,
      "trust": {
        "code": "none",
        "components": {
          "plugins/notify/mcp.json:everything": "c9f0c5f218a8b5cd56ed927b291f3deec6cc0a2bece2b54dbae65f3b38e3cea1",
          "plugins/notify/plugin/notify.js": "52119ad6cbf614a9345f9bfbbd3fac281b25c6b55dd7917074eee02a7f9134f9"
        }
      },
      "lastSync": {
        "at": "2026-09-15T16:48:25.277Z",
        "ok": true,
        "error": null
      },
      "plugins": {
        "notify": {
          "source": "plugins/notify",
          "components": {
            "plugin": [
              "notify.js"
            ],
            "mcp": [
              "everything"
            ]
          },
          "enabled": true,
          "installedAt": "2026-09-15T16:48:25.277Z",
          "version": "0.3.0",
          "manifest": {
            "description": "Executable notifier — plugin JS and an MCP server",
            "version": "0.3.0",
            "category": "tooling",
            "tags": [
              "notify",
              "exec"
            ]
          }
        }
      }
    }
  }
}
--stderr--
```
`trust.code` recorded as `"none"` (non-interactive default), and `list` shows bare `plugins: notify.js` / `mcp: everything` — no blocked marker. Retrying with an explicit interactive deny (expect, answer `n`):

```
$ expect: spawn env HOME=$H ocm add file:///tmp/ocm-e2e3/repos/exec-src ; answer n
spawn env HOME=/tmp/ocm-e2e3/home-a8-06 ocm add file:///tmp/ocm-e2e3/repos/exec-src
cloning file:///tmp/ocm-e2e3/repos/exec-src...
installed auto-sync loader (/tmp/ocm-e2e3/home-a8-06/.config/opencode/plugins/ocm-loader.js)
marketplace "ocm-e2e3-exec" ships code that opencode will execute:
  mcp     notify/everything (local server: )
  plugin  notify/notify (plugins/notify/plugin/notify.js)
this code runs with your shell's permissions on every opencode start.
review it at /tmp/ocm-e2e3/home-a8-06/.cache/ocm/marketplaces/ocm-e2e3-exec
[0m[31mtrust this marketplace to run code? [y/N/skip][0m
n
[1G[0J [1Gn
[0m[31m  warning: blocked (untrusted): notify:notify.js not linked — run `ocm trust ocm-e2e3-exec` to approve[0m
[0m[31m  warning: blocked (untrusted): notify:mcp/everything not installed — run `ocm trust ocm-e2e3-exec` to approve[0m
added marketplace "ocm-e2e3-exec"
  notify (1 plugins, 1 mcp servers)
commands and agents are available as /<plugin>:<name> in every project
installed TUI plugin (/ocm in the opencode TUI, restart opencode to activate)
expect-exit=0
```
```

```
$ env HOME=$H ocm list        # after explicit deny
exit=0
--stdout--
ocm-e2e3-exec
  source: file:///tmp/ocm-e2e3/repos/exec-src
  revision: 3b80558
  notify (0.3.0)
    plugins: notify.js (blocked — ocm trust ocm-e2e3-exec)
    mcp: everything (blocked — ocm trust ocm-e2e3-exec)
--stderr--
trust.code in registry:
```
trust.code (explicit deny): denied
trust.code (non-interactive default, home-a8-05): none
```
Registry path note: BRIEF says `$H/.config/ocm/registry.json`; actual is `$H/.config/opencode/ocm/registry.json`. trust.code: explicit `n` → `denied`; non-interactive default → `none`; explicit `y` → `granted`. Only `denied` carries the blocked markers.

Now the `--json` comparison — trusted home (home-a8-07) vs denied home (home-a8-06), structural diff:

```
$ env HOME=…-06 ocm list --json   (denied)  vs  env HOME=…-07 ocm list --json  (trusted)
4c4
<       "addedAt": "2026-09-15T16:48:34.646Z",
---
>       "addedAt": "2026-09-15T16:48:50.440Z",
7c7
<         "at": "2026-09-15T16:48:34.646Z",
---
>         "at": "2026-09-15T16:48:50.440Z",
24c24
<           "installedAt": "2026-09-15T16:48:34.646Z",
---
>           "installedAt": "2026-09-15T16:48:50.440Z",
43c43,49
<         "code": "denied"
---
>         "code": "granted",
>         "components": {
>           "plugins/notify/mcp.json:everything": "c9f0c5f218a8b5cd56ed927b291f3deec6cc0a2bece2b54dbae65f3b38e3cea1",
>           "plugins/notify/plugin/notify.js": "52119ad6cbf614a9345f9bfbbd3fac281b25c6b55dd7917074eee02a7f9134f9"
>         },
>         "fingerprint": "df688aa7b262a54cb4e9c69424d80af9e1fe815ecfef4feae353849e562391e3",
>         "grantedAt": "2026-09-15T16:48:50.465Z"
diff-exit=0 (differences above are the truthful trust-state fields only)
```
```

**A9.1 PASS (with one defect and one gap noted).**
- After an explicit interactive deny (`n`), `ocm list` marks both executable lines: `plugins: notify.js (blocked — ocm trust ocm-e2e3-exec)` / `mcp: everything (blocked — ocm trust ocm-e2e3-exec)` — spec 25 §1 exact. F15 closed for the denied case.
- `--json` is the registry verbatim; the only diffs vs a trusted home are timestamps and the truthful trust-state fields (denied/granted, components, fingerprint, grantedAt). Unchanged, as spec requires.
- **Defect D1 (new, med, CLI):** non-interactive add (`</dev/null`) records `trust.code: "none"`, and `ocm list` then renders `plugins: notify.js` / `mcp: everything` bare — indistinguishable from a trusted marketplace, though nothing is linked. The blocked marker keys on `"denied"` only (src/commands/list.ts:63). A user who scripted `ocm add` sees blocked components listed as if installed.
- **Defect D2 (new, med, CLI+TUI):** the trust listing prints `mcp     notify/everything (local server: )` — empty command. `trust-prompt.ts:16` (and `loader/ui-trust.js:15`) only render `command` when it is an array; the fixture's `"command": "node"` (string form, opencode-valid — it passes through to `opencode.json` as `"command": "node"`) renders as nothing. The security-review surface understates what will run.
- Colour note (formatting lens): on a tty the prompt and warnings are red (expect capture shows `[31m` codes); non-tty runs are colour-free. Warnings on stderr, data on stdout — correct.

### A9.2 — F20: trust-pending seeded via loader sync

Own copy `/tmp/ocm-e2e3/repos/a8-exec-src`. Home `home-a8-08`, add with trust granted:

```
$ expect: spawn env HOME=$H ocm add file:///tmp/ocm-e2e3/repos/a8-exec-src ; answer y
expect-exit=0
```
spawn env HOME=/tmp/ocm-e2e3/home-a8-08 ocm add file:///tmp/ocm-e2e3/repos/a8-exec-src
cloning file:///tmp/ocm-e2e3/repos/a8-exec-src...
installed auto-sync loader (/tmp/ocm-e2e3/home-a8-08/.config/opencode/plugins/ocm-loader.js)
marketplace "ocm-e2e3-exec" ships code that opencode will execute:
  mcp     notify/everything (local server: )
  plugin  notify/notify (plugins/notify/plugin/notify.js)
this code runs with your shell's permissions on every opencode start.
review it at /tmp/ocm-e2e3/home-a8-08/.cache/ocm/marketplaces/ocm-e2e3-exec
[0m[31mtrust this marketplace to run code? [y/N/skip][0m
y
[1G[0J [1Gy
added marketplace "ocm-e2e3-exec"
  notify (1 plugins, 1 mcp servers)
commands and agents are available as /<plugin>:<name> in every project
installed TUI plugin (/ocm in the opencode TUI, restart opencode to activate)
restart opencode to activate
```
Upstream change committed in the copy (new mcp server `hook` in `plugins/notify/mcp.json`, commit 32448b3). Forcing the loader's startup sync from a neutral cwd:

```
$ cd $H && OCM_SYNC_INTERVAL_MS=0 HOME=$H opencode debug config > /dev/null
exit=1
[91m[1mError: [0mConfiguration is invalid at /tmp/ocm-e2e3/home-a8-08/.config/opencode/opencode.json
↳ Expected { readonly "type": "local", ... } | { readonly "type": "remote", ... }, got {"command":"node","args":["-e","console.log('everything mcp placeholder')"]} mcp.ocm--notify--everything
↳ Missing key mcp.ocm--notify--everything.enabled
--- mcp keys after sync:
[
  "ocm--notify--everything"
]
--- registry trustPending/lastSync:
{
  "trustPending": null,
  "lastSync": {
    "at": "2026-09-15T16:50:04.285Z",
    "ok": true,
    "error": null
  },
  "trust": "granted"
}
```

**Environment discovery (affects A9.2+):** opencode 1.18.31 rejects old-shape mcp entries. `ocm add` of MP-B (trusted) writes `"ocm--notify--everything": {"command":"node","args":[…]}` (raw pass-through of the fixture's mcp.json), and the next `opencode debug config` exits 1: `Configuration is invalid … Expected { readonly "type": "local", … } | { readonly "type": "remote", … }, got {"command":"node",…} mcp.ocm--notify--everything / Missing key mcp.ocm--notify--everything.enabled`. A bare probe confirms: user-written `{"command":["echo"]}` is rejected the same way; `{"type":"local","command":["echo"],"enabled":true}` passes. ocm's own `template/plugins/demo-kit/mcp.json` uses the new shape — the shared MP-B fixture predates it. Checking whether `ocm validate` flags the old shape:

```
$ cd /tmp/ocm-e2e3/repos/exec-src && env HOME=$H ocm validate
exit=1
--stdout--
validate /private/tmp/ocm-e2e3/repos/exec-src
  error   plugins/notify/mcp.json: entry "everything" is missing "type"
  error   plugins/notify/plugin/notify.js: does not default-export { id, server } or a function
2 errors, 0 warnings
--stderr--
```
```
`ocm validate` flags both (`entry "everything" is missing "type"`; `does not default-export { id, server } or a function`) — MP-B is stale against ocm's own documented contract, so for A9.2+ my copy `a8-exec-src` was fixed to the canonical shapes (commit 40d5d12: default-export plugin, `type`/`enabled` mcp entry). Re-running A9.2 on the fixed copy, home `home-a8-09`:

```
$ expect: spawn env HOME=$H ocm add file:///tmp/ocm-e2e3/repos/a8-exec-src ; answer y
expect-exit=0
spawn env HOME=/tmp/ocm-e2e3/home-a8-09 ocm add file:///tmp/ocm-e2e3/repos/a8-exec-src
cloning file:///tmp/ocm-e2e3/repos/a8-exec-src...
installed auto-sync loader (/tmp/ocm-e2e3/home-a8-09/.config/opencode/plugins/ocm-loader.js)
marketplace "ocm-e2e3-exec" ships code that opencode will execute:
  mcp     notify/everything (local server: node -e console.log('everything mcp placeholder'))
  plugin  notify/notify (plugins/notify/plugin/notify.js)
this code runs with your shell's permissions on every opencode start.
review it at /tmp/ocm-e2e3/home-a8-09/.cache/ocm/marketplaces/ocm-e2e3-exec
[0m[31mtrust this marketplace to run code? [y/N/skip][0m
y
[1G[0J [1Gy
added marketplace "ocm-e2e3-exec"
  notify (1 plugins, 1 mcp servers)
commands and agents are available as /<plugin>:<name> in every project
installed TUI plugin (/ocm in the opencode TUI, restart opencode to activate)
restart opencode to activate
--- opencode debug config (loader runs, config must validate):
exit=0
--- mcp keys:
[
  "ocm--notify--everything"
]
--- plugins dir:
ocm--notify--notify.js
ocm-loader.js
```
Upstream change: `hook` server added (commit 3134d84). Forcing the loader sync:

```
$ cd $H && OCM_SYNC_INTERVAL_MS=0 HOME=$H opencode debug config >/dev/null
```
exit=0
--- registry after sync:
{
  "revision": "40d5d12",
  "trustPending": null,
  "lastSync": {
    "at": "2026-09-15T16:51:45.399Z",
    "ok": true,
    "error": null
  }
}
--- mcp keys after sync:
[
  "ocm--notify--everything"
]
--- opencode log tail:
timestamp=2026-09-15T16:51:45.859Z level=INFO run=f3745b7d message=fromDirectory directory=/private/tmp/ocm-e2e3/home-a8-09
timestamp=2026-09-15T16:51:45.866Z level=INFO run=f3745b7d message=bootstrapping directory=/private/tmp/ocm-e2e3/home-a8-09
timestamp=2026-09-15T16:51:45.869Z level=INFO run=f3745b7d message=loading path=/tmp/ocm-e2e3/home-a8-09/.config/opencode/config.json
timestamp=2026-09-15T16:51:45.869Z level=INFO run=f3745b7d message=loading path=/tmp/ocm-e2e3/home-a8-09/.config/opencode/opencode.json
timestamp=2026-09-15T16:51:45.873Z level=INFO run=f3745b7d message=loading path=/tmp/ocm-e2e3/home-a8-09/.config/opencode/opencode.jsonc
```
Revision did not advance (still 40d5d12) though lastSync.ok=true was recorded — the loader's `syncAll` is fire-and-forget (`void core.syncAll(...).catch()`), suspecting a race with `debug config`'s quick exit. Rerunning, then sleeping to let the sync land:

```
$ cd $H && OCM_SYNC_INTERVAL_MS=0 HOME=$H opencode debug config >/dev/null ; sleep 3
{
  "revision": "40d5d12",
  "trustPending": null,
  "lastSync": {
    "at": "2026-09-15T16:51:45.399Z",
    "ok": true,
    "error": null
  }
}
--- clone HEAD:
40d5d12 fix plugin export and mcp shape for opencode 1.18.31
--- mcp keys:
[
  "ocm--notify--everything"
]
```
`debug config` never loads server plugins (log shows config loading only) — the loader sync does not run under it. Trying the real TUI briefly (expect, 10s, then Ctrl-C):

```
$ expect: spawn env HOME=$H OCM_SYNC_INTERVAL_MS=0 opencode (cwd $H); sleep 10; send \x03
expect-exit=0
{
  "revision": "3134d84",
  "trustPending": true,
  "lastSync": {
    "at": "2026-09-15T16:53:16.008Z",
    "ok": true,
    "error": null
  }
}
--- clone HEAD:
3134d84 add hook mcp server
--- mcp keys:
ocm--notify--everything
```
The real TUI run DID sync: revision → 3134d84, `trustPending: true`, and `hook` correctly NOT installed (only `ocm--notify--everything` in mcp keys). Now the four F20 surfaces:

```
$ env HOME=$H ocm list
exit=0
ocm-e2e3-exec (1 component awaiting trust)
  source: file:///tmp/ocm-e2e3/repos/a8-exec-src
  revision: 3134d84
  plugins/notify/mcp.json:hook — awaiting trust (ocm trust ocm-e2e3-exec)
  notify (0.3.0)
    plugins: notify.js
    mcp: everything, hook
--stderr--
```

```
$ env HOME=$H ocm list --all
exit=0
ocm-e2e3-exec (auto, 1 component awaiting trust)
  source: file:///tmp/ocm-e2e3/repos/a8-exec-src
  revision: 3134d84
  synced just now
  plugins/notify/mcp.json:hook — awaiting trust (ocm trust ocm-e2e3-exec)
  notify (0.3.0)
    plugins: notify.js
    mcp: everything, hook
--stderr--
```

```
$ env HOME=$H ocm info ocm-e2e3-exec
exit=1
--stderr--
plugin "ocm-e2e3-exec" not found in any marketplace (ocm search <query>, or ocm update)
```

```
$ env HOME=$H ocm doctor
exit=1
doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
  error   marketplace "ocm-e2e3-exec": executable components pulled but not trusted — run ocm trust ocm-e2e3-exec
1 error, 0 warnings
--stderr--
```
```

`ocm info` takes a plugin name, not a marketplace name. Retrying:

```
$ env HOME=$H ocm info notify
exit=0
notify @ ocm-e2e3-exec
  description  Executable notifier — plugin JS and an MCP server
  version      0.3.0
  category     tooling
  tags         notify, exec
  enabled      yes
  installed    2026-09-15T16:51:45.399Z
  marketplace  file:///tmp/ocm-e2e3/repos/a8-exec-src
  revision     3134d84  (synced just now)
  trust        code changed since trust — run ocm trust ocm-e2e3-exec
  plugins/notify/mcp.json:hook — awaiting trust (ocm trust ocm-e2e3-exec)
  components
    plugin  ocm--notify--notify.js  → /tmp/ocm-e2e3/home-a8-09/.config/opencode/plugins/ocm--notify--notify.js
    mcp     ocm--notify--everything  → /tmp/ocm-e2e3/home-a8-09/.config/opencode/opencode.json (mcp)
    mcp     ocm--notify--hook  (not linked)
--stderr--
```
```

**A9.2 PASS — F20 closed.** `list` shows `(1 component awaiting trust)`; `--all` and `info notify` carry the per-component line `plugins/notify/mcp.json:hook — awaiting trust (ocm trust ocm-e2e3-exec)`; `doctor` errors (`executable components pulled but not trusted — run ocm trust ocm-e2e3-exec`, exit 1). `hook` was correctly not installed. (Note: `ocm info <marketplace-name>` errors — info wants a plugin name; the message is actionable.) Seeding required the real TUI: `opencode debug config` does not load server plugins, so the loader sync never runs under it (see D3).

### A9.3 — F17: info renders disk, not registry fiction

State so far on home-a8-09: trust granted at 40d5d12, upstream added `hook` → info already shows `trust code changed since trust — run ocm trust ocm-e2e3-exec` and `mcp ocm--notify--hook (not linked)` — the "changed" state and the not-linked form both correct. Now the declined re-trust:

```
$ expect: spawn env HOME=$H ocm trust ocm-e2e3-exec ; answer n
expect-exit=0
spawn env HOME=/tmp/ocm-e2e3/home-a8-09 ocm trust ocm-e2e3-exec
marketplace "ocm-e2e3-exec" ships code that opencode will execute:
  mcp     notify/everything (local server: node -e console.log('everything mcp placeholder'))
  mcp     notify/hook (local server: node -e console.log('hook mcp placeholder'))
  plugin  notify/notify (plugins/notify/plugin/notify.js)
this code runs with your shell's permissions on every opencode start.
review it at /tmp/ocm-e2e3/home-a8-09/.cache/ocm/marketplaces/ocm-e2e3-exec
[0m[31mtrust this marketplace to run code? [y/N/skip][0m
n
[1G[0J [1Gn
[0m[31m  warning: 3 components blocked pending trust — run `ocm trust ocm-e2e3-exec`[0m
```
```

```
$ env HOME=$H ocm info notify        # after declined re-trust
exit=0
notify @ ocm-e2e3-exec
  description  Executable notifier — plugin JS and an MCP server
  version      0.3.0
  category     tooling
  tags         notify, exec
  enabled      yes
  installed    2026-09-15T16:51:45.399Z
  marketplace  file:///tmp/ocm-e2e3/repos/a8-exec-src
  revision     3134d84  (synced just now)
  trust        denied
  components
    plugin  ocm--notify--notify.js  (not linked)
    mcp     ocm--notify--everything  (not linked)
    mcp     ocm--notify--hook  (not linked)
--stderr--
```

```
$ env HOME=$H ocm list
exit=0
ocm-e2e3-exec
  source: file:///tmp/ocm-e2e3/repos/a8-exec-src
  revision: 3134d84
  notify (0.3.0)
    plugins: notify.js (blocked — ocm trust ocm-e2e3-exec)
    mcp: everything, hook (blocked — ocm trust ocm-e2e3-exec)
--stderr--
--- on disk: mcp keys / plugins dir:
ocm-loader.js
--- trust.code:
denied
```
Declined re-trust: `trust denied`, all three components `(not linked)`, no arrows; on disk the mcp object is gone and only `ocm-loader.js` remains in plugins/; `list` marks both executable lines blocked. All correct.

The granted state and the disabled case, on home-a8-07 (trusted, everything linked):

```
$ env HOME=$H ocm info notify        # granted, linked
exit=0
notify @ ocm-e2e3-exec
  description  Executable notifier — plugin JS and an MCP server
  version      0.3.0
  category     tooling
  tags         notify, exec
  enabled      yes
  installed    2026-09-15T16:48:50.440Z
  marketplace  file:///tmp/ocm-e2e3/repos/exec-src
  revision     3b80558  (synced 5m ago)
  trust        granted
  components
    plugin  ocm--notify--notify.js  → /tmp/ocm-e2e3/home-a8-07/.config/opencode/plugins/ocm--notify--notify.js
    mcp     ocm--notify--everything  → /tmp/ocm-e2e3/home-a8-07/.config/opencode/opencode.json (mcp)
--stderr--
```

```
$ env HOME=$H ocm disable notify && env HOME=$H ocm info notify
disable exit=0
uninstalled notify@ocm-e2e3-exec
restart opencode to activate
info exit=0
notify @ ocm-e2e3-exec
  description  Executable notifier — plugin JS and an MCP server
  version      0.3.0
  category     tooling
  tags         notify, exec
  enabled      no
  marketplace  file:///tmp/ocm-e2e3/repos/exec-src
  revision     3b80558  (synced 5m ago)
  trust        granted
  components
    plugin  ocm--notify--notify.js  (not linked)
    mcp     ocm--notify--everything  (not linked)
--stderr--
```
**A9.3 PASS — F17 closed.** All three trust-line states match reality (`granted` with arrows to existing files; `code changed since trust — run ocm trust <mp>` after upstream code change; `denied` immediately after a declined re-trust), absent targets render `(not linked)` with no arrows, on disk matches. (The disabled view omits the `installed` line because uninstall nulls `installedAt` — consistent, not a defect.)

### A9.4 — F43: installedAt of an auto-installed plugin

Own copy `a8-alpha-src` (code-free, plugins greet-kit + tip-kit), home `home-a8-10`:

```
$ env HOME=$H ocm add file:///tmp/ocm-e2e3/repos/a8-alpha-src </dev/null
exit=0
cloning file:///tmp/ocm-e2e3/repos/a8-alpha-src...
installed auto-sync loader (/tmp/ocm-e2e3/home-a8-10/.config/opencode/plugins/ocm-loader.js)
added marketplace "ocm-e2e3-alpha"
  greet-kit (1 agents, 1 commands, 1 skills)
  tip-kit (1 skills)
commands and agents are available as /<plugin>:<name> in every project
installed TUI plugin (/ocm in the opencode TUI, restart opencode to activate)
restart opencode to activate
--stderr--
```
Upstream `new-kit` committed; sleeping 5s so the update time is distinguishable from addedAt.

```
$ env HOME=$H ocm update </dev/null
exit=0
updating ocm-e2e3-alpha...
  7774b64 → 059a2a7
  new-kit   installed (auto)
    + commands/fresh.md
    + plugin.json
  restart opencode to activate
--stderr--
```
`new-kit.installedAt = 16:54:34.927Z` vs marketplace `addedAt = 16:54:25.133Z` — the update's time, not backdated. Now the old-registry case: hand-edit the registry to drop `greet-kit`'s `installedAt` entirely:

```
$ jq 'del(.marketplaces["ocm-e2e3-alpha"].plugins["greet-kit"].installedAt)' registry > tmp && mv
$ env HOME=$H ocm info greet-kit
exit=0
greet-kit @ ocm-e2e3-alpha
  description  Greeting utilities — one command, one agent, one skill
  version      0.1.0
  category     demo
  tags         greet, alpha
  enabled      yes
  marketplace  file:///tmp/ocm-e2e3/repos/a8-alpha-src
  revision     059a2a7  (synced just now)
  trust        none
  components
    command greet-kit:greet  → /tmp/ocm-e2e3/home-a8-10/.config/opencode/commands/greet-kit:greet.md
    agent   greet-kit:greeter  → /tmp/ocm-e2e3/home-a8-10/.config/opencode/agents/greet-kit:greeter.md
    skill   greet-kit:greeting  → /tmp/ocm-e2e3/home-a8-10/.cache/ocm/links/ocm-e2e3-alpha/skills/greet-kit--greeting/SKILL.md
--stderr--
```

And `ocm info new-kit` for completeness:

```
$ env HOME=$H ocm info new-kit
exit=0
new-kit @ ocm-e2e3-alpha
  description  Newly added upstream kit
  version      0.1.0
  enabled      yes
  installed    2026-09-15T16:54:34.927Z
  marketplace  file:///tmp/ocm-e2e3/repos/a8-alpha-src
  revision     059a2a7  (synced just now)
  trust        none
  components
    command new-kit:fresh  → /tmp/ocm-e2e3/home-a8-10/.config/opencode/commands/new-kit:fresh.md
--stderr--
```
```

**A9.4 PASS — F43 closed.** Auto-installed `new-kit` carries the update's time (16:54:34.927Z), not the marketplace's addedAt (16:54:25.133Z); a registry entry without `installedAt` omits the line instead of inventing one. (Observation, low: code-free plugins print `trust none` — a trust line for nothing-to-trust is mild noise.)

### A9.5 — F44: `(pinned @ v1.0.0)` in `list`

```
$ env HOME=$H ocm pin ocm-e2e3-alpha v1.0.0
exit=0
marketplace "ocm-e2e3-alpha" pinned to v1.0.0
--stderr--
```

```
$ env HOME=$H ocm list
exit=0
ocm-e2e3-alpha (pinned @ v1.0.0)
  source: file:///tmp/ocm-e2e3/repos/a8-alpha-src
  revision: 059a2a7
  greet-kit (0.1.0)
    agents: greeter.md
    commands: greet.md
    skills: greeting
  new-kit (0.1.0)
    commands: fresh.md
  tip-kit (0.2.0)
    skills: tips
--stderr--
```

```
$ env HOME=$H ocm list --all
exit=0
ocm-e2e3-alpha (auto, pinned @ v1.0.0)
  source: file:///tmp/ocm-e2e3/repos/a8-alpha-src
  revision: 059a2a7
  synced just now
  greet-kit (0.1.0)
    agents: greeter.md
    commands: greet.md
    skills: greeting
  new-kit (0.1.0)
    commands: fresh.md
  tip-kit (0.2.0)
    skills: tips
--stderr--
```
**A9.5 PASS — F44 closed.** Plain `list` shows `(pinned @ v1.0.0)`; `--all` shows `(auto, pinned @ v1.0.0)`.

### A9.6 — F44b: pin picker

Clearing the pin first, then the interactive picker via expect:

```
$ env HOME=$H ocm pin ocm-e2e3-alpha --clear
exit=0
marketplace "ocm-e2e3-alpha" unpinned (following the default branch)
```

```
$ expect: spawn env HOME=$H ocm pin ocm-e2e3-alpha   (no ref) ; answer v1.0.0
expect-exit=0
spawn env HOME=/tmp/ocm-e2e3/home-a8-10 ocm pin ocm-e2e3-alpha
refs available for "ocm-e2e3-alpha":
  main
  v1.0.0
v1.0.0
[1G[0Jpin to which ref? [19Gv1.0.0
marketplace "ocm-e2e3-alpha" pinned to v1.0.0
--- registry ref after picker:
v1.0.0
```
```

Interactive picker: lists heads and tags (`main`, `v1.0.0`), prompts `pin to which ref?`, records the choice (registry ref = v1.0.0). Now non-interactive with no ref (10s timeout guard):

```
$ env HOME=$H ocm pin ocm-e2e3-alpha </dev/null
exit=127 (124 would be a hang)
--stdout--
--stderr--
zsh:4: command not found: timeout
```
```

(macOS has no `timeout`; using expect with a 10s ceiling instead)

```
$ expect -c "spawn env HOME=$H ocm pin ocm-e2e3-alpha; expect eof"   (stdin not a tty? — stdin is the expect pty, so also run a pure </dev/null variant)
expect-exit=124
spawn env HOME=/tmp/ocm-e2e3/home-a8-10 ocm pin ocm-e2e3-alpha
refs available for "ocm-e2e3-alpha":
  main
  v1.0.0
[1G[0Jpin to which ref? [19GHUNG-TIMEOUT
```

Pure non-tty run with </dev/null and a watchdog:

```
$ env HOME=$H ocm pin ocm-e2e3-alpha </dev/null   (killed by watchdog after 10s if hung)
exit=1 (143 = killed by watchdog = hang)
--stdout--
--stderr--
missing ref for "ocm-e2e3-alpha" — available refs: main, v1.0.0
  ocm pin ocm-e2e3-alpha <ref>
```
```

**A9.6 PASS — F44b closed.** Interactive: refs listed (`main`, `v1.0.0`), `pin to which ref?` prompt, choice recorded in the registry. Non-interactive `</dev/null`: exit 1, stderr `missing ref for "ocm-e2e3-alpha" — available refs: main, v1.0.0` + usage line; no hang (returned immediately).

### A9.7 — F47: search matches plugin JS and MCP server names

Added an mcp server named `notify` to the copy (so the term `notify` hits both the plugin JS `notify.js` and an MCP server within one plugin). Fresh home `home-a8-11`, add `--no-trust`:

```
$ env HOME=$H ocm add file:///tmp/ocm-e2e3/repos/a8-exec-src --no-trust </dev/null
exit=0
cloning file:///tmp/ocm-e2e3/repos/a8-exec-src...
installed auto-sync loader (/tmp/ocm-e2e3/home-a8-11/.config/opencode/plugins/ocm-loader.js)
added marketplace "ocm-e2e3-exec"
  notify (1 plugins, 3 mcp servers)
commands and agents are available as /<plugin>:<name> in every project
installed TUI plugin (/ocm in the opencode TUI, restart opencode to activate)
--stderr--
  warning: blocked (untrusted): notify:notify.js not linked — run `ocm trust ocm-e2e3-exec` to approve
  warning: blocked (untrusted): notify:mcp/everything not installed — run `ocm trust ocm-e2e3-exec` to approve
  warning: blocked (untrusted): notify:mcp/hook not installed — run `ocm trust ocm-e2e3-exec` to approve
  warning: blocked (untrusted): notify:mcp/notify not installed — run `ocm trust ocm-e2e3-exec` to approve
```
```

```
$ env HOME=$H ocm search notify
exit=0
notify@ocm-e2e3-exec  0.3.0  tooling  Executable notifier — plugin JS and an MCP server (blocked)
  plugins: notify.js · mcp: everything, hook, notify
--stderr--
```

```
$ env HOME=$H ocm search everything
exit=0
notify@ocm-e2e3-exec  0.3.0  tooling  Executable notifier — plugin JS and an MCP server (blocked)
  matched: mcp everything
--stderr--
```

```
$ env HOME=$H ocm search hook
exit=0
notify@ocm-e2e3-exec  0.3.0  tooling  Executable notifier — plugin JS and an MCP server (blocked)
  matched: mcp hook
--stderr--
```
add exit=0
The phase-25 test shape (plugin `demo-kit`, js `notify.js`, mcp `notify` — no name collision), added to the same home:

```
$ env HOME=$H ocm search notify
exit=0
notify@ocm-e2e3-exec  0.3.0  tooling  Executable notifier — plugin JS and an MCP server (blocked)
  plugins: notify.js · mcp: everything, hook, notify
demo-kit@search-mp  0.1.0  Search annotation test kit
  matched: plugin notify.js
  matched: mcp notify
--stderr--
```
```

**A9.7 PARTIAL — F47 closed with one defect (D4, low).** `search everything` → `matched: mcp everything`; `search hook` → `matched: mcp hook`; a term hitting both a plugin's JS and its MCP server renders one result with both annotations (`demo-kit@search-mp … matched: plugin notify.js / matched: mcp notify`) — when the term is not the plugin's own name. **D4:** when the query equals the plugin name (MP-B shape: plugin `notify`, js `notify.js`, mcp `notify`), the rank-0 name rule short-circuits and NO `matched:` annotation is printed — the spec 25 §7 example `ocm search notify` → `matched: plugin notify.js` and the edge-table case "term matches a plugin name AND its MCP server → both annotations" do not hold literally; the result row is still found and the component summary lists the executables.

### A9.8 — F60: sync freshness in `list`

Failed sync first: move the source repo away, then `ocm update`:

```
$ mv /tmp/ocm-e2e3/repos/a8-alpha-src /tmp/ocm-e2e3/repos/a8-alpha-src.away
$ env HOME=$H ocm update </dev/null
exit=1
updating ocm-e2e3-alpha...
--stderr--
  failed: cannot access file:///tmp/ocm-e2e3/repos/a8-alpha-src — the repository is private, unreachable, or the URL is wrong
update failed for 1 marketplace(s): ocm-e2e3-alpha
--- registry lastSync:
{
  "at": "2026-09-15T16:56:25.687Z",
  "ok": false,
  "error": "cannot access file:///tmp/ocm-e2e3/repos/a8-alpha-src — the repository is private, unreachable, or the URL is wrong"
}
```
```

```
$ env HOME=$H ocm list --all
exit=0
ocm-e2e3-alpha (auto, ! sync failed just now)
  source: file:///tmp/ocm-e2e3/repos/a8-alpha-src
  revision: 059a2a7
  sync failed just now
  greet-kit (0.1.0)
    agents: greeter.md
    commands: greet.md
    skills: greeting
  new-kit (0.1.0)
    commands: fresh.md
  tip-kit (0.2.0)
    skills: tips
--stderr--
  last sync failed: cannot access file:///tmp/ocm-e2e3/repos/a8-alpha-src — the repository is private, unreachable, or the URL is wrong
```

```
$ env HOME=$H ocm list
exit=0
ocm-e2e3-alpha (! sync failed just now)
  source: file:///tmp/ocm-e2e3/repos/a8-alpha-src
  revision: 059a2a7
  greet-kit (0.1.0)
    agents: greeter.md
    commands: greet.md
    skills: greeting
  new-kit (0.1.0)
    commands: fresh.md
  tip-kit (0.2.0)
    skills: tips
--stderr--
```
```

Failure marker on both surfaces; `--all` adds the stderr `last sync failed:` line with the reason. Source repo restored; hand-setting `lastSync.at` to 2h ago (ok: true):

```
$ env HOME=$H ocm list --all        (then plain ocm list)
exit=0
ocm-e2e3-alpha (auto)
  source: file:///tmp/ocm-e2e3/repos/a8-alpha-src
  revision: 059a2a7
  synced 2h ago
--stderr--
plain exit=0
ocm-e2e3-alpha
  source: file:///tmp/ocm-e2e3/repos/a8-alpha-src
  revision: 059a2a7
  greet-kit (0.1.0)
--stderr--
```

`lastSync` removed entirely (never-synced case):

```
$ env HOME=$H ocm list --all
exit=0
ocm-e2e3-alpha (auto)
  source: file:///tmp/ocm-e2e3/repos/a8-alpha-src
  revision: 059a2a7
  never synced
--stderr--
```
```

**A9.8 PASS — F60 closed.** `--all`: `synced 2h ago` / `never synced` / `sync failed just now` (+ stderr `last sync failed: <reason>`); plain `list`: `(! sync failed just now)` marker, silent about mere age (no `synced` lines at all).

### 80-column readability (formatting lens)

```
$ COLUMNS=80 script -q /dev/null env HOME=$H ocm list --all   ; line-length audit
^Docm-e2e3-exec (auto)
  source: file:///tmp/ocm-e2e3/repos/a8-exec-src
  revision: c741523
  synced just now
  notify (0.3.0)
    plugins: notify.js (blocked — ocm trust ocm-e2e3-exec)
    mcp: everything, hook, notify (blocked — ocm trust ocm-e2e3-exec)
search-mp (auto)
  source: /private/tmp/ocm-e2e3/a8-fixtures/search-mp
  demo-kit (0.1.0)
    commands: work.md
    plugins: notify.js
    mcp: notify
--- longest lines (chars):
72:     mcp: everything, hook, notify (blocked — ocm trust ocm-e2e3-exec)
0 lines over 80 columns
```
Longest line 72 columns, 0 lines over 80 — readable. (`--no-trust` records `denied`, so the blocked markers show for it; D1 is specific to the EOF/non-interactive default recording `none`.)

## Cross-cutting invariants (homes a8-09 and a8-11)

```
### /tmp/ocm-e2e3/home-a8-09
$ ls -la $H/.claude $H/.agents
(empty = pass)
$ ls $H/.config/opencode/plugins
ocm-loader.js
$ find … -type l ! -exec test -e {} \; -print
(empty = pass)
$ jq . opencode.json tui.json
(no BAD = pass)
$ ocm doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
doctor exit=0

### /tmp/ocm-e2e3/home-a8-11
$ ls -la $H/.claude $H/.agents
(empty = pass)
$ ls $H/.config/opencode/plugins
ocm--demo-kit--notify.js
ocm-loader.js
$ find … -type l ! -exec test -e {} \; -print
(empty = pass)
$ jq . opencode.json tui.json
(no BAD = pass)
$ ocm doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
doctor exit=0

```
```

(recapturing doctor exit codes without the tail pipeline masking $?)

```
### /tmp/ocm-e2e3/home-a8-09
doctor exit=0
doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
### /tmp/ocm-e2e3/home-a8-11
doctor exit=0
doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
### /tmp/ocm-e2e3/home-a8-10
doctor exit=0
doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
```
```

### User-key survival (seeded on home-a8-11, then `ocm update`)

```
$ seed model/permission/mcp.user-server ; env HOME=$H ocm update </dev/null
update exit=0
{
  "model": "claude-sonnet-4-6",
  "permission": {
    "edit": "allow"
  },
  "userMcp": {
    "type": "local",
    "command": [
      "echo"
    ],
    "enabled": true
  },
  "ocmKeys": [
    "ocm--demo-kit--notify"
  ]
}
```
```

User keys (model, permission, mcp.user-server) survive `ocm update` byte-identically; ocm-owned keys intact alongside.

### D3 impact verification — full TUI on a trusted old-shape marketplace (home-a8-12)

```
$ expect: spawn env HOME=$H opencode        (after add exec-src + trust y)
Configuration is invalid at /tmp/ocm-e2e3/home-a8-12/.config/opencode/opencode.json
↳ Expected { readonly "type": "local", ... } | { readonly "type": "remote", ... }, got {"command":"node","args":["-e","console.log('everything mcp placeholder')"]} mcp.ocm--notify--everything
↳ Missing key mcp.ocm--notify--everything.enabled
```
The TUI tears down and exits immediately — opencode 1.18.31 refuses to start on the config `ocm add` + trust wrote (raw pass-through of a pre-1.18.31-shaped mcp.json). `ocm validate` flags the marketplace (`entry "everything" is missing "type"`), but nothing in the add/trust path does.

## Suite A9 verdicts

- A9.1 PASS (F15 closed for `denied`; D1: `none` state renders bare; D2: empty command in trust listing)
- A9.2 PASS (F20 closed; methodology note: `opencode debug config` does not load plugins — the sync must be seeded via a real TUI start)
- A9.3 PASS (F17 closed — granted/changed/denied all match reality; `(not linked)` for absent targets; no phantom arrows)
- A9.4 PASS (F43 closed — update-time installedAt; missing installedAt omits the line)
- A9.5 PASS (F44 closed — `(pinned @ v1.0.0)` in plain list; `(auto, pinned @ v1.0.0)` in --all)
- A9.6 PASS (F44b closed — interactive picker lists heads+tags, prompts, records; non-interactive errors listing refs, no hang)
- A9.7 PARTIAL (F47 closed in general; D4: annotations suppressed when query == plugin name)
- A9.8 PASS (F60 closed — synced/never synced/sync failed in --all; `! sync failed` in plain list; plain list silent about age)

Invariants: clean on homes a8-09/10/11 (no writes outside namespaces, only ocm-loader.js + ocm-- links in plugins/, no broken symlinks, configs parse, doctor exits 0, user keys survive).

## "Needs user" (could not test without a live human)

- The TUI `/ocm` dialog halves of F15/F20/F44 (blocked markers, awaiting-trust, pin display inside the TUI) — only the CLI surfaces were testable here; the TUI was started headless via expect solely to trigger the loader sync.
- The pin picker's arrow-key/typing ergonomics beyond a single typed answer.

### D1 impact addendum

`ocm doctor` is also silent on the `none` state (exit 0, no findings on home-a8-05); `ocm info notify` does render disk truth (`trust none`, both components `(not linked)`). So after a non-interactive add, only `info` tells the truth; `list` and `doctor` do not.
