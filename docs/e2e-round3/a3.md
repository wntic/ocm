# Suite A3 — Collisions & scan (spec 18) · agent a3

Plan: `docs/e2e-test-plan-round3.md` lines 174–207 (A3.1–A3.8, findings
F10a/b/c, F56, F32, F14, F38, F39, F25).
Env: ocm 0.5.0 (symlink to repo HEAD), opencode 1.18.31, git 2.50, bun 1.4.2.
Homes: `H1=/tmp/ocm-e2e3/home-a3-01` (main), `H2=/tmp/ocm-e2e3/home-a3-02`
(F56 add refusal), `H3=/tmp/ocm-e2e3/home-a3-03` (F56 cap), `H4=/tmp/ocm-e2e3/home-a3-04`
(incumbent-side update + remove-incumbent edge). All commands run with
`HOME=$H`, neutral cwd. Capture helper per BRIEF (`run`), logs under
`/tmp/ocm-e2e3/logs/a3/`.

**Harness note.** My first captures used the shared `/tmp/ocm-e2e3/logs/out.txt`
and twice picked up *another agent's* output interleaved into the file (e.g. an
`updating ocm-e2e3-alpha...` report from a parallel suite appeared mid-doctor).
That is a fixture collision between test agents, **not** an ocm defect — after
moving to a private log dir every capture was clean and every state claim below
was re-verified on disk (registry JSON, symlinks), not only via CLI output.

## Fixtures I built (copies; shared fixtures untouched)

- `/tmp/ocm-e2e3/repos/a3-collide-src` — copy of MP-F (`collide-src`). The
  collision must arrive *upstream* (add-time refusal would block adding MP-F
  after MP-D), so: commit 1 `d4fff5f` = seed plugin `collide-seed` only;
  commit 2 `821bd96` = adds `big-03` + `big-07` (commands `run-03.md` /
  `run-07.md`, deliberately the *same basenames* MP-D ships, so the refusal's
  "conflicting paths on both sides" is exercisable); commit 3 `f085060` =
  trivial change to force re-registration.
- `/tmp/ocm-e2e3/repos/a3-collide2-src` — same seed/collision history, fresh
  repo, used in H4.
- `/tmp/ocm-e2e3/repos/a3-big11-src` — copy of MP-D (`big-src`) + `big-11`
  (11 plugins total) — the incumbent for the 11-collision cap test (MP-D itself
  ships only 10 names).
- `/tmp/ocm-e2e3/repos/a3-rival11-src` — rival marketplace `a3-rival11`
  shipping the same 11 names (`big-01`…`big-11`), each with valid plugin.json
  (non-empty description) + `commands/run-NN.md`.
- `/tmp/ocm-e2e3/a3-fixtures/both-defects` — local marketplace with duplicate
  `plugins[]` name **and** two plugins shipping the same command basename.
- `/tmp/ocm-e2e3/a3-fixtures/empty-mp` — empty dir (A3.7).

## Setup — H1, collision created upstream (round-2 plan Test 12 route)

```
$ env HOME=$H1 ocm add file:///tmp/ocm-e2e3/repos/big-src
exit=0
--stdout--
cloning file:///tmp/ocm-e2e3/repos/big-src...
installed auto-sync loader (/tmp/ocm-e2e3/home-a3-01/.config/opencode/plugins/ocm-loader.js)
added marketplace "ocm-e2e3-big"
  big-01 (1 commands, 1 skills)
  big-02 (1 agents, 1 commands, 1 skills)
  big-03 (1 commands, 2 skills)
  big-04 (1 commands, 1 skills)
  big-05 (1 commands, 1 skills)
  big-06 (1 commands, 1 skills)
  big-07 (1 commands, 1 skills)
  big-08 (1 commands, 1 skills)
  big-09 (1 commands, 1 skills)
  big-10 (1 commands, 1 skills)
commands and agents are available as /<plugin>:<name> in every project
installed TUI plugin (/ocm in the opencode TUI, restart opencode to activate)
restart opencode to activate
--stderr--
```

```
$ env HOME=$H1 ocm add file:///tmp/ocm-e2e3/repos/a3-collide-src
exit=0
--stdout--
cloning file:///tmp/ocm-e2e3/repos/a3-collide-src...
added marketplace "ocm-e2e3-collide"
  collide-seed (1 commands)
commands and agents are available as /<plugin>:<name> in every project
restart opencode to activate
--stderr--
```

Upstream commit `821bd96` (adds big-03/big-07), then:

```
$ env HOME=$H1 ocm update ocm-e2e3-collide
exit=0
--stdout--
updating ocm-e2e3-collide...
  d4fff5f → 821bd96
  big-03   name owned by marketplace "ocm-e2e3-big" — kept disabled; ocm install big-03@ocm-e2e3-collide --force to take over
    + commands/run-03.md
    + plugin.json
  big-07   name owned by marketplace "ocm-e2e3-big" — kept disabled; ocm install big-07@ocm-e2e3-collide --force to take over
    + commands/run-07.md
    + plugin.json
--stderr--
```

Registry (on disk, `$H1/.config/opencode/ocm/registry.json`): both newcomers
`enabled: false`, `collision: "ocm-e2e3-big"`, `installedAt: null`.
`commands/big-03:run-03.md` and `big-07:run-07.md` still symlink to
`ocm-e2e3-big`'s clone. **Conclusion: collision records created exactly as
spec 18 §1.3 describes — reported, not acted on.**

## A3.1 — F10a: install refusal

```
$ env HOME=$H1 ocm install big-03@ocm-e2e3-collide
exit=1
--stdout--
--stderr--
plugin "big-03" is already provided by marketplace "ocm-e2e3-big"
  big-03:run-03.md from marketplace "ocm-e2e3-big" conflicts with big-03:run-03.md from marketplace "ocm-e2e3-collide"
install with --force to take the name over, or ask the author to rename
```

- exit 1 ✓
- names **both** marketplaces ✓ and the conflicting path **on both sides** ✓
  (`big-03:run-03.md from marketplace "ocm-e2e3-big" conflicts with
  big-03:run-03.md from marketplace "ocm-e2e3-collide"`)
- nothing materialized: `commands/big-03:run-03.md` still → ocm-e2e3-big clone ✓
- registry byte-identical (`cmp` before/after) ✓

Formatting lens: the refusal is on stderr, exit 1, no colour, no stack trace.
The spec 18 §1 example shows an `error:` prefix; the implemented house style
for thrown CLI errors is a bare message (consistent across all verbs) — noted
as observation O1, not counted against the step.

**Verdict: pass.**

## A3.2 — F10b: takeover with --force, then idempotent re-run

```
$ env HOME=$H1 ocm install big-03@ocm-e2e3-collide --force
exit=0
--stdout--
took over "big-03" from marketplace "ocm-e2e3-big"
restart opencode to activate
--stderr--
```

On disk after:
- registry: ocm-e2e3-big's big-03 `enabled: false, installedAt: null`;
  ocm-e2e3-collide's big-03 `enabled: true, installedAt: <ts>, collision: null` ✓
  (incumbent disabled **and** uninstalled; newcomer installed)
- `commands/big-03:run-03.md` → ocm-e2e3-collide's clone ✓ (incumbent unlinked)
- incumbent's big-03 skill mirrors under
  `~/.cache/ocm/links/ocm-e2e3-big/skills/` are gone (teardown) ✓
- report states the takeover with the spec's exact wording ✓

Re-runs:

```
$ env HOME=$H1 ocm install big-03@ocm-e2e3-collide --force
exit=0
--stdout--
already installed big-03@ocm-e2e3-collide
--stderr--
```

```
$ env HOME=$H1 ocm install big-03@ocm-e2e3-collide
exit=0
--stdout--
already installed big-03@ocm-e2e3-collide
--stderr--
```

No second takeover, no warnings, incumbent state untouched. **Verdict: pass.**

## A3.3 — F10c: update must not flip an explicit choice

Newcomer side (H1). With collide's big-03 `enabled: true` (explicitly
installed), first an up-to-date update, then a re-registering update (upstream
commit `f085060`):

```
$ env HOME=$H1 ocm update ocm-e2e3-collide </dev/null
exit=0
--stdout--
updating ocm-e2e3-collide...
  already up to date
--stderr--
```

```
$ env HOME=$H1 ocm update ocm-e2e3-collide </dev/null
exit=0
--stdout--
updating ocm-e2e3-collide...
  821bd96 → f085060
  big-03
    ~ commands/run-03.md
--stderr--
```

Registry before/after both updates: collide's big-03 `enabled: true`,
`collision: null`; link still → collide's clone. **No silent flip.** The
collision-reported-as-a-line behaviour was already evidenced in the setup
update (`name owned by marketplace "ocm-e2e3-big" — kept disabled; ocm install
… --force to take over`).

Incumbent side (H4, own copies `a3-big-src` + `a3-collide2-src`, same
seed→collision→`install --force` flow, then a trivial upstream change in
a3-big-src):

```
$ env HOME=$H4 ocm update ocm-e2e3-big </dev/null
exit=0
--stdout--
updating ocm-e2e3-big...
  834c0dc → 6750fde
  big-09
    ~ plugin.json
  restart opencode to activate
--stderr--
```

Registry: collide's big-03 stays `enabled: true` (link intact) ✓; big's big-03
stays `enabled: false` ✓ — **but** it gained `"collision": "ocm-e2e3-collide"`
during this update with **zero output** about it. Spec 18 §1.3: "A collision
encountered during update is reported, not acted on." The record was acted on
(written) and not reported. → new defect **a3-D1** (low). The newcomer-side
requirement of the plan step itself holds.

**Verdict: pass** (with new low defect a3-D1 on the incumbent side).

## A3.4 — F56: add-time refusal lists all colliding plugins

H2, pristine shared fixtures (MP-D added first, then MP-F):

```
$ env HOME=$H2 ocm add file:///tmp/ocm-e2e3/repos/collide-src </dev/null
exit=1
--stdout--
cloning file:///tmp/ocm-e2e3/repos/collide-src...
--stderr--
plugin "big-03" is already provided by marketplace "ocm-e2e3-big"
plugin "big-07" is already provided by marketplace "ocm-e2e3-big"
not adding "ocm-e2e3-collide"; remove one, or ask its author to rename
```

- **both** colliding plugins named (round-2 F56 named only the first) ✓
- exit 1 ✓; registry byte-identical ✓; clone discarded (cache holds only
  `ocm-e2e3-big`) ✓
- Observation O2: no component-path lines here, because pristine MP-F ships
  `commands/clash.md` while MP-D ships `run-03.md`/`run-07.md` — no shared
  dests exist, so `collisionPathLines` emits nothing. The name conflict is
  real (the `big-03:` namespace) but nothing in the text says the *name* is
  the conflict. Low; the message is still actionable ("ask its author to
  rename").

Cap half (H3, 11 collisions — `a3-big11-src` incumbent, `a3-rival11-src`
rival):

```
$ env HOME=$H3 ocm add file:///tmp/ocm-e2e3/repos/a3-rival11-src </dev/null
exit=1
--stdout--
cloning file:///tmp/ocm-e2e3/repos/a3-rival11-src...
--stderr--
plugin "big-01" is already provided by marketplace "ocm-e2e3-big"
  big-01:run-01.md from marketplace "ocm-e2e3-big" conflicts with big-01:run-01.md from marketplace "a3-rival11"
plugin "big-02" is already provided by marketplace "ocm-e2e3-big"
  big-02:run-02.md from marketplace "ocm-e2e3-big" conflicts with big-02:run-02.md from marketplace "a3-rival11"
plugin "big-03" is already provided by marketplace "ocm-e2e3-big"
  big-03:run-03.md from marketplace "ocm-e2e3-big" conflicts with big-03:run-03.md from marketplace "a3-rival11"
plugin "big-04" is already provided by marketplace "ocm-e2e3-big"
  big-04:run-04.md from marketplace "ocm-e2e3-big" conflicts with big-04:run-04.md from marketplace "a3-rival11"
plugin "big-05" is already provided by marketplace "ocm-e2e3-big"
  big-05:run-05.md from marketplace "ocm-e2e3-big" conflicts with big-05:run-05.md from marketplace "a3-rival11"
plugin "big-06" is already provided by marketplace "ocm-e2e3-big"
  big-06:run-06.md from marketplace "ocm-e2e3-big" conflicts with big-06:run-06.md from marketplace "a3-rival11"
plugin "big-07" is already provided by marketplace "ocm-e2e3-big"
  big-07:run-07.md from marketplace "ocm-e2e3-big" conflicts with big-07:run-07.md from marketplace "a3-rival11"
plugin "big-08" is already provided by marketplace "ocm-e2e3-big"
  big-08:run-08.md from marketplace "ocm-e2e3-big" conflicts with big-08:run-08.md from marketplace "a3-rival11"
plugin "big-09" is already provided by marketplace "ocm-e2e3-big"
  big-09:run-09.md from marketplace "ocm-e2e3-big" conflicts with big-09:run-09.md from marketplace "a3-rival11"
plugin "big-10" is already provided by marketplace "ocm-e2e3-big"
  big-10:run-10.md from marketplace "ocm-e2e3-big" conflicts with big-10:run-10.md from marketplace "a3-rival11"
… and 1 more
not adding "a3-rival11"; remove one, or ask its author to rename
```

10 listed, each with its component path on both sides, then `… and 1 more`
(big-11 capped), closing remedy line. Registry untouched, clone discarded.
**Verdict: pass.**

## A3.5 — F32: doctor sees recorded collisions (H1, big-07 collision record)

```
$ env HOME=$H1 ocm doctor
exit=1
--stdout--
doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
  error   plugin "big-07" from marketplace "ocm-e2e3-collide" is disabled — the name is owned by marketplace "ocm-e2e3-big"
    install with ocm install big-07@ocm-e2e3-collide --force, or remove one marketplace
1 error, 0 warnings
--stderr--
```

- finding prints, names both marketplaces and the takeover command ✓
- exit non-zero ✓

```
$ env HOME=$H1 ocm doctor --fix
exit=1
--stdout--
doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
  error   plugin "big-07" from marketplace "ocm-e2e3-collide" is disabled — the name is owned by marketplace "ocm-e2e3-big"
    install with ocm install big-07@ocm-e2e3-collide --force, or remove one marketplace
1 error, 0 warnings
--stderr--
```

- `--fix` changed nothing: same finding, no `fixed` lines, registry
  byte-identical (`cmp` before/after) ✓

Uninstall of the disabled plugin (a no-op — it was never installed):

```
$ env HOME=$H1 ocm uninstall big-07@ocm-e2e3-collide
exit=0
--stdout--
big-07@ocm-e2e3-collide not installed
--stderr--
```

Doctor after: identical finding, exit 1 (clean re-capture; the record remains
`enabled: false, collision: "ocm-e2e3-big"` in the registry). ✓

**Verdict: pass.**

## A3.6 — F14/F39: scan truthfulness (H1)

Uninstalled plugin (uninstalled first):

```
$ env HOME=$H1 ocm uninstall big-05@ocm-e2e3-big
exit=0
--stdout--
uninstalled big-05@ocm-e2e3-big
restart opencode to activate
--stderr--
```

```
$ env HOME=$H1 ocm scan big-05@ocm-e2e3-big
exit=0
--stdout--
installing big-05@ocm-e2e3-big would materialize:
  /tmp/ocm-e2e3/home-a3-01/.config/opencode/commands/big-05:run-05.md (would create)
  skill /tmp/ocm-e2e3/home-a3-01/.cache/ocm/links/ocm-e2e3-big/skills/big-05--notes-05 (would create)
--stderr--
```

Every component `would create`, **zero** `collision:` lines ✓.

```
$ env HOME=$H1 ocm install big-05@ocm-e2e3-big
exit=0
--stdout--
installed big-05@ocm-e2e3-big (1 command, 1 skill)
restart opencode to activate
--stderr--
```

Install without `--force` succeeds — proves the scan's "would create" was
truthful ✓.

```
$ env HOME=$H1 ocm scan big-05@ocm-e2e3-big
exit=0
--stdout--
installing big-05@ocm-e2e3-big would materialize:
  /tmp/ocm-e2e3/home-a3-01/.config/opencode/commands/big-05:run-05.md (already linked)
  skill /tmp/ocm-e2e3/home-a3-01/.cache/ocm/links/ocm-e2e3-big/skills/big-05--notes-05 (already linked)
--stderr--
```

Installed plugin → `already linked` for every component ✓.

Foreign file at the destination (big-06 uninstalled, hand-written regular file
put at its command dest):

```
$ env HOME=$H1 ocm scan big-06@ocm-e2e3-big
exit=0
--stdout--
installing big-06@ocm-e2e3-big would materialize:
  /tmp/ocm-e2e3/home-a3-01/.config/opencode/commands/big-06:run-06.md (collision: foreign file)
  skill /tmp/ocm-e2e3/home-a3-01/.cache/ocm/links/ocm-e2e3-big/skills/big-06--notes-06 (would create)
--stderr--
```

Collision named, the foreign file's path named ✓. Observation O3: the line
does not say what happens next — spec 18's edge table says "`--force`
displacement implied", but the user is never told that
`ocm install big-06@ocm-e2e3-big --force` would displace (and preserve) their
file. Low.

ocm-owned symlink to another plugin's tree (big-03's dest belongs to collide
after the A3.2 takeover; scanning the *incumbent's* disabled big-03):

```
$ env HOME=$H1 ocm scan big-03@ocm-e2e3-big
exit=0
--stdout--
installing big-03@ocm-e2e3-big would materialize:
  /tmp/ocm-e2e3/home-a3-01/.config/opencode/commands/big-03:run-03.md (collision: symlink → /tmp/ocm-e2e3/home-a3-01/.cache/ocm/marketplaces/ocm-e2e3-collide/plugins/big-03/commands/run-03.md)
  skill /tmp/ocm-e2e3/home-a3-01/.cache/ocm/links/ocm-e2e3-big/skills/big-03--big-topic (would create)
  skill /tmp/ocm-e2e3/home-a3-01/.cache/ocm/links/ocm-e2e3-big/skills/big-03--notes-03 (would create)
--stderr--
```

All four spec §4.1 destination states are distinct and truthful ✓.

Trust-gated annotation (F39) with MP-B, added non-interactively (prompt
defaults to safe; executable components blocked):

```
$ env HOME=$H1 ocm add file:///tmp/ocm-e2e3/repos/exec-src </dev/null
exit=0
--stdout--
cloning file:///tmp/ocm-e2e3/repos/exec-src...
marketplace "ocm-e2e3-exec" ships code that opencode will execute:
  mcp     notify/everything (local server: )
  plugin  notify/notify (plugins/notify/plugin/notify.js)
this code runs with your shell's permissions on every opencode start.
review it at /tmp/ocm-e2e3/home-a3-01/.cache/ocm/marketplaces/ocm-e2e3-exec
added marketplace "ocm-e2e3-exec"
  notify (1 plugins, 1 mcp servers)
commands and agents are available as /<plugin>:<name> in every project
--stderr--
trust this marketplace to run code? [y/N/skip]
  warning: blocked (untrusted): notify:notify.js not linked — run `ocm trust ocm-e2e3-exec` to approve
  warning: blocked (untrusted): notify:mcp/everything not installed — run `ocm trust ocm-e2e3-exec` to approve
```

```
$ env HOME=$H1 ocm scan notify@ocm-e2e3-exec
exit=0
--stdout--
installing notify@ocm-e2e3-exec would materialize:
  /tmp/ocm-e2e3/home-a3-01/.config/opencode/plugins/ocm--notify--notify.js (would create, trust-gated)
  mcp ocm--notify--everything in /tmp/ocm-e2e3/home-a3-01/.config/opencode/opencode.json (would create, trust-gated)
--stderr--
```

Identical output after `ocm uninstall notify@ocm-e2e3-exec`. Executable
components (plugin JS, MCP) annotated `trust-gated` — "would create" does not
imply "would run" ✓. (Exact annotation text is `(would create, trust-gated)`,
i.e. the plan's "(trust-gated)" rides inside the state parenthesis.)

**Verdict: pass.**

## A3.7 — F38: scan on an empty dir

```
$ env HOME=$H1 ocm scan /tmp/ocm-e2e3/a3-fixtures/empty-mp
exit=0
--stdout--
no plugins found in /tmp/ocm-e2e3/a3-fixtures/empty-mp
  expected plugins/<name>/{commands,agents,skills}/ at the repository root
  see ocm validate and the README's marketplace format
--stderr--
```

Layout hint + validate/README pointer, exactly spec 18 §5. Exit 0 (a scan that
found nothing to do is not an error — defensible). **Verdict: pass.**

## A3.8 — F25: validate's two new rows

```
$ env HOME=$H1 ocm validate /tmp/ocm-e2e3/fixtures/broken/dup-plugins
exit=1
--stdout--
validate /tmp/ocm-e2e3/fixtures/broken/dup-plugins
  error   marketplace.json: plugin "p-one" listed twice — the second entry is ignored today
  warning plugins/p-one/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
  warning plugins/p-two/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
1 error, 2 warnings
--stderr--
```

Duplicate `plugins[]` name → error naming the file and the ignored second
entry ✓.

```
$ env HOME=$H1 ocm validate /tmp/ocm-e2e3/fixtures/broken/basename-clash
exit=1
--stdout--
validate /tmp/ocm-e2e3/fixtures/broken/basename-clash
  warning plugins/p-one/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
  warning plugins/p-two/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
  error   plugins/p-two: command "shared.md" is also shipped by plugin "p-one" — rename one
1 error, 2 warnings
--stderr--
```

Cross-plugin basename clash → error naming both plugins and the shared
target ✓.

Both defects in ONE marketplace (`a3-fixtures/both-defects`, non-fail-fast):

```
$ env HOME=$H1 ocm validate /tmp/ocm-e2e3/a3-fixtures/both-defects
exit=1
--stdout--
validate /tmp/ocm-e2e3/a3-fixtures/both-defects
  error   marketplace.json: plugin "p-one" listed twice — the second entry is ignored today
  warning plugins/p-one/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
  warning plugins/p-two/plugin.json: no "$schema" — not installable by Codex; pin to https://agent-plugins.org/schemas/1.0.0/plugin.schema.json
  error   plugins/p-two: command "shared.md" is also shipped by plugin "p-one" — rename one
2 errors, 2 warnings
--stderr--
```

**Verdict: pass.**

## Extra edge — takeover, then `ocm remove` of the incumbent (H4)

Spec 18 edge table: "no change to the taken-over plugin; no leftover collision
record".

```
$ env HOME=$H4 ocm remove ocm-e2e3-big
exit=0
--stdout--
removed marketplace "ocm-e2e3-big"
  big-01: 1 commands, 1 skills removed
  big-02: 1 agents, 1 commands, 1 skills removed
  big-04: 1 commands, 1 skills removed
  big-05: 1 commands, 1 skills removed
  big-06: 1 commands, 1 skills removed
  big-07: 1 commands, 1 skills removed
  big-08: 1 commands, 1 skills removed
  big-09: 1 commands, 1 skills removed
  big-10: 1 commands, 1 skills removed
--stderr--
```

- collide's big-03 untouched: `enabled: true`, link still → collide's clone ✓
  (big-03 correctly absent from the removed list — the name belongs to collide)
- big-03's own record carries no leftover collision ✓
- **But**: collide's big-07 — never taken over, registered disabled with
  `collision: "ocm-e2e3-big"` — keeps a now-dangling record. Doctor
  immediately after:

```
$ env HOME=$H4 ocm doctor
exit=1
--stdout--
doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
  error   plugin "big-07" from marketplace "ocm-e2e3-collide" is disabled — the name is owned by marketplace "ocm-e2e3-big"
    install with ocm install big-07@ocm-e2e3-collide --force, or remove one marketplace
1 error, 0 warnings
--stderr--
```

  `ocm-e2e3-big` no longer exists; the name big-07 is free. The record is
  stale and the finding is false. Recovery: `ocm install
  big-07@ocm-e2e3-collide` (plain, no --force) succeeds and clears the record
  (verified; doctor then exits 0). → new defect **a3-D2** (med).

## Cross-cutting invariants

H1 (main home; state: takeovers, collisions, untrusted exec marketplace,
one deliberate hand-written foreign file at `commands/big-06:run-06.md`):

```
# 1. never writes outside its own namespaces
clean (no .claude/.agents)
# 2. only ocm-loader.js in plugins/ (plus ocm-- links)
ocm-loader.js            (notify's JS never linked — untrusted, correct)
# 3. no broken symlinks anywhere ocm owns
(none)
# 4. configs parse
configs parse
# 5. doctor agrees
exit=1 — the single error is the deliberate big-07 collision record
```

H4 (after remove-incumbent + recovery install): all five clean, doctor exit 0.
User-key survival: no user keys were seeded in these homes (fresh homes, ocm
wrote the configs); nothing outside ocm's namespaces was touched, and
`opencode.json`/`tui.json` parse throughout.

## Findings summary

- **a3-D1** (new, low) — incumbent-side update writes a `collision` record
  with no report line (spec 18 §1.3 "reported, not acted on").
- **a3-D2** (new, med) — stale collision record after `ocm remove` of the
  incumbent: doctor names a removed marketplace and suggests an unnecessary
  `--force`.
- O1 (observation, low) — CLI refusals print without the `error:` prefix
  shown in spec 18 §1's example (house style for thrown errors is a bare
  stderr message; consistent across verbs).
- O2 (observation, low) — when colliding plugins ship different component
  basenames, the refusal carries no path lines and does not say the conflict
  is the name/namespace itself.
- O3 (observation, low) — scan's `(collision: foreign file)` line names the
  file but not the remedy (`--force` displacement, original preserved).

## Needs user (not testable without a live terminal)

- The TUI's `/ocm` install flow against a colliding plugin (spec 18 says the
  TUI consumes the same core refusal "for free" — unverified here).
- The interactive trust prompt's y/N/skip rendering (only the non-interactive
  default path was exercised).
