# a2 — round 5, Part A: steps 11–19 (A3 renames, A4 local changed-set, A5 small correctness)

ocm: 0.7.0 published binaries (`/tmp/ocm-e2e5/v070/node_modules/.bin/ocm`),
plus 0.6.1 (`/tmp/ocm-e2e5/v061/node_modules/.bin/ocm`) for upgrade-path
seeding. opencode: 1.18.32 (real binary, TUI via `expect` with a sized pty).
XDG: unset. Every ocm invocation ran under a scratch `HOME` (see the runner
incident, F220, for the two exceptions at session start).

Homes: a2-1 (steps 11–12), a2-2 (step 13), a2-3 (steps 14–16), a2-4
(step 17), a2-5 (step 18), a2-6 (step 19), a2-7 (loader-side upgrade
hazard, extra beyond the plan), a2-probe (incident reproduction, kept as
evidence).

Method notes:

- The loader's startup sync is throttled per marketplace by `lastSync.at`
  (1 h default, `loader/sync.js:153-170`). TUI sessions that needed a sync
  inside the throttle window ran with `OCM_SYNC_INTERVAL_MS=0` — the
  documented env override — rather than tampering with the registry. The
  throttle itself was observed live: a2-1's first session 50 s after `add`
  performed no sync (`lastSync.at` unchanged).
- Pre-seeding (steps 14, 16 and the a2-7 extra) follows round 4's technique
  (`docs/e2e-round4/a3-a5.md` step 11): a hand-written 0.6.1-era registry
  (`ocmVersion: "0.6.1"`, `installedAt: "2026-01-01"`, no `hashes` — 0.6.1
  never wrote them), then `ocm init` + `ocm install` under the real 0.6.1
  binary. `0.6.1 add` refuses a manifest-less marketplace outright
  (verified in a2-3 setup), so no CLI path can produce the seeded state —
  pre-seeding is the only route.
- The fold test (step 17) ran on the case-sensitive APFS volume: fixture
  copied to `/Volumes/CSX5/work-a2-fold-mp` (same volume), the lowercase
  sibling created there.

## Verdicts

| Step | Finding(s) | Verdict |
|---|---|---|
| A3-11 | colliding rename refused at loader sync | **closed** |
| A3-12 | doctor / doctor --fix / trust keep the refusal | **closed** |
| A3-13 | ordinary rename migrates in explicit mode | **closed** |
| A4-14 | local manifest-less plugin changed → refused, reported (F190, F73) | **closed** |
| A4-15 | local no-change update prints `already up to date` (F106) | **closed** |
| A4-16 | first 0.7.0 pass: no digests → unknown, not changed | **closed** |
| A5-17 | fold at local update: link gone, no cross-wire, doctor 0 (F128) | **closed for F128's harm — new defect F221 on the report line** |
| A5-18 | init / loader uninstall refuse corrupt registry (F126, F127) | **closed** |
| A5-19 | validate reports broken-only plugin.json, add matches (F146) | **closed for the isolated tree — residual F223 on mixed trees** |

New findings: F220 (runner incident), F221, F222, F223. No Part D re-files.

---

## A3 — renames in the loader (brief 40)

### Step 11 — CLOSED (home a2-1)

Setup: `work-a2/rename-mp` (git copy of MP-RENAME) at R1=1a3cd26 shipping
`old-kit` (1 command + 1 mcp server); `work-a2/rival-mp` (local, MP-RIVAL)
added first with `taken-kit` installed; rename-mp added
(`file://…/rename-mp --name rename-mp --trust`) and `old-kit` installed.
Upstream commit 629851d renames the plugin dir to `taken-kit` with
`plugin.json` `renames: {"old-kit": "taken-kit"}` — colliding with rival's
installed `taken-kit`. Restarted opencode (TUI session,
`OCM_SYNC_INTERVAL_MS=0`) to trigger the loader's startup sync.

After the session, on disk:

- registry `rename-mp`: `revision` 629851d, `lastSync.ok: true`,
  `refusedRenames: {"old-kit": "taken-kit"}`, plugin still recorded as
  `old-kit`, `enabled: true`;
- `commands/old-kit:greet.md` still present and resolving — into the
  renamed directory
  `…/marketplaces/rename-mp/plugins/taken-kit/commands/greet.md`
  (the refusal keeps the record under its old name while `record.source`
  follows the disk, `loader/reconcile.js:46-48`);
- rival-mp untouched: `taken-kit` record intact,
  `commands/taken-kit:hold.md` resolves into rival's tree;
- `opencode debug config` sees `old-kit:greet` (body "Old-kit greet
  body.") and the MCP key `ocm--old-kit--old-server`.

```
$ jq '.marketplaces["rename-mp"] | {revision, refusedRenames,
     plugins: (.plugins | to_entries | map({name: .key, enabled: .value.enabled}))}' registry.json
{
  "revision": "629851dbf516690375462a47ea1050ad080a8793",
  "refusedRenames": {
    "old-kit": "taken-kit"
  },
  "plugins": [ { "name": "old-kit", "enabled": true } ]
}
```

Exactly brief 40 §"a rename onto an installed name is refused, recorded,
and the old record kept". No defect.

### Step 12 — CLOSED (home a2-1, same state)

```
$ ocm doctor --fix            exit=0
doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
  cache   default-0b1ced (…)
$ ocm trust rename-mp --yes   exit=0
marketplace "rename-mp" already trusted
```

After both: `refusedRenames` intact, `old-kit` still recorded and enabled,
link resolves, MCP key present. A later plain `ocm doctor` also exit 0.
Nothing disables the refused plugin. Note (not a defect, not in plan
scope): doctor never mentions the recorded refusal — it reports loader
health only.

### Step 13 — CLOSED (home a2-2)

Fresh home; `rename2-mp` added with `--explicit` (registry confirms
`mode: "explicit"`), `old-kit` installed. Upstream commit 311f1a9 renames
to `fresh-kit` (non-colliding, `renames: {"old-kit": "fresh-kit"}`).
Loader startup sync (TUI session) migrates the state:

```
$ jq '.marketplaces["rename2-mp"] | {refusedRenames, mode,
     plugins: (.plugins | to_entries | map({name: .key, enabled: .value.enabled,
     installedAt: .value.installedAt, version: .value.version, source: .value.source}))}' registry.json
{
  "refusedRenames": null,
  "mode": "explicit",
  "plugins": [
    { "name": "fresh-kit", "enabled": true,
      "installedAt": "2026-09-22T16:14:54.549Z",
      "version": "0.2.0", "source": "plugins/fresh-kit" }
  ]
}
```

- `installedAt` preserved (the install survived the rename, not
  re-created);
- `enabled: true` — explicit mode did not silently disable the migrated
  plugin;
- old link gone, `commands/fresh-kit:greet.md` resolves into the renamed
  dir; `refusedRenames` cleared;
- `opencode debug config` sees `fresh-kit:greet`, not `old-kit:greet`;
- `ocm list --all` shows `fresh-kit (0.2.0)` under `rename2-mp (explicit)`;
  `ocm doctor` exit 0.

---

## A4 — local changed-set (brief 30)

### Step 14 — CLOSED (F190, F73; home a2-3)

0.6.1-era pre-seed (see method notes) with `nomanifest-mp` (local,
`ancient-kit`, no plugin.json) installed and linked. First 0.7.0 pass
(step 16) baselined digests. Then the plugin's `commands/old.md` was
edited and `ocm update` run:

```
$ ocm update                   exit=0
--stdout--
updating nomanifest-mp...
  ancient-kit   uninstalled — plugin.json required now that it changed
  restart opencode to activate
--stderr--
warning: plugin "ancient-kit": changed upstream and still has no plugin.json — uninstalled
  it predates the plugin.json requirement and kept working until it changed
  add plugins/ancient-kit/plugin.json ({ "description": "…" }) and run ocm update to reinstall it
```

On disk afterwards: `plugins` map empty, `commands/` empty,
`ocm doctor` exit 0. This is brief 29 §3's "grandfather until changed"
firing for a **local** source — the exact F190 defect (grandfather
forever, because change detection was git-revision-only) and F73's local
scope. Both closed by brief 30's digests.

### Step 15 — CLOSED (F106; home a2-3)

```
$ ocm update                   exit=0
--stdout--
updating nomanifest-mp...
  already up to date
--stderr-- (empty)
```

Registry diff before/after: only `lastSync.at` moved (the throttle's own
bookkeeping write, spec 08); every plugin record byte-identical.

### Step 16 — CLOSED (home a2-3)

The upgrade hazard, exercised exactly: 0.6.1-era record with **no
hashes**, content not yet changed, first 0.7.0 command after the upgrade
(`ocm --version` first — prints `refreshed the auto-sync loader: 0.6.1 →
0.7.0 (restart opencode to activate)` — then `ocm update`):

```
$ ocm update                   exit=0
--stdout--
updating nomanifest-mp...
  change tracking initialized — this pass reports no per-plugin changes
--stderr-- (empty)
```

`ancient-kit` still installed afterwards (record intact, link resolves),
and the record now carries `hashes: {"commands/old.md": "d83658…"}` —
baselined by this pass. Brief 30 §4's "unknown, not unchanged" holds:
nothing was uninstalled, the grandfather did not end.

### Extra beyond the plan — loader-side upgrade hazard (home a2-7)

The same sequence driven through real opencode sessions instead of the
CLI, since brief 30 §3's hazard is the startup path. 0.6.1-era seed,
loader refreshed to 0.7.0 via `ocm --version`, content changed **before**
the first 0.7.0 loader session:

- session 1: grandfather **held** — nothing dropped at startup despite
  the changed content (no recorded digests → unknown, not changed);
  `hashes` baselined afterwards against the changed content;
- content changed again, session 2: grandfather **ended** — registry
  `plugins` empty, `commands/` empty, `opencode debug config` no longer
  sees `ancient-kit:old`.

The drop itself is correct on both passes. But the warning that `ocm
update` prints (quoted in step 14) never surfaced anywhere in the TUI
session — see F222.

---

## A5 — small correctness (brief 39)

### Step 17 — CLOSED for F128's harm; new defect F221 (home a2-4)

`fold-mp` added from `/Volumes/CSX5/work-a2-fold-mp` (case-sensitive
APFS, own copy on the same volume): `case-kit` (from dir `case-Kit`) and
`fold-plain-kit` both install; `commands/case-kit:run.md` →
`…/plugins/case-Kit/commands/run.md`. Then the lowercase sibling
`plugins/case-kit/` (own plugin.json + different `run.md`) created on the
volume, and `ocm update`:

```
$ ocm update                   exit=0
--stdout--
updating fold-mp...
  already up to date
  restart opencode to activate
--stderr--
warning: plugins/case-Kit and plugins/case-kit differ only in case — plugin "case-kit" skipped; ask the author to rename one and update again
```

On disk: `case-kit` record gone, `commands/case-kit:run.md` **gone**, no
link under `~/.config/opencode` points into either case directory
(`find … -type l -exec readlink` matches none), `fold-plain-kit` record
and link untouched, `ocm doctor` exit 0.

F128's harm — the cross-wired link surviving the drop — is fixed. The
stdout report, however, claims nothing changed while a plugin was just
uninstalled: F221.

### Step 18 — CLOSED (F126, F127; home a2-5)

`printf '{' > registry.json`, then:

```
$ ocm init                     exit=1
--stderr--
error: registry at …/ocm/registry.json is not valid JSON — ocm will not overwrite it
  inspect or move the file, then run ocm doctor; to start over, remove it and re-add your marketplaces

$ ocm loader uninstall         exit=1
--stderr--
error: registry at …/ocm/registry.json is not valid JSON — ocm will not overwrite it
  inspect or move the file, then run ocm doctor; to start over, remove it and re-add your marketplaces
```

Both streams otherwise empty; `registry.json` byte-identical after
(sha 60ba4b2d…, still present). Both commands refuse with the named error
and the next action — brief 27 §1.6's mutating set now includes `init` and
`loader uninstall`. F126 and F127 closed.

### Step 19 — CLOSED for the plan's scope; residual F223 (home a2-6)

Isolated tree `broken-mp`: `plugins/broken-kit/` whose only content is a
broken `plugin.json` (`{ "description": ` — invalid JSON):

```
$ ocm validate …/broken-mp    exit=1
--stdout--
validate /tmp/ocm-e2e5/work-a2/broken-mp
  error   plugins/broken-kit/plugin.json: not valid JSON — fix it or remove it; ocm requires this file to be readable
  plugins/broken-kit has no components — nothing installs, so users are unaffected
1 error, 0 warnings

$ ocm add …/broken-mp --name broken-mp    exit=1
--stderr--
no plugins found in /private/tmp/ocm-e2e5/work-a2/broken-mp
  plugins/broken-kit/plugin.json: not valid JSON — fix it or remove it; ocm requires this file to be readable
  plugins/broken-kit has no components — nothing installs, so users are unaffected
  expected plugins/<name>/{commands,agents,skills}/ at the repository root
  run `ocm scan …` to see what was found
```

`add` refuses, writes nothing (no registry), and carries the two
diagnosis lines **verbatim** from validate — the F146 disagreement (false
clean vs bare "no plugins found") is gone. Closed for the tree the plan
names. The mixed-tree residual is F223.

---

## Findings

## F220 — runner incident: two bare `ocm --version` calls ran against the real home; a third writer repaired it concurrently   (area: docs, severity: low)

Test:        session start, before any scratch home existed
ocm version: 0.7.0 (~21:00:25) and 0.6.1 (~21:02:13), both bare
Command:     `ocm --version` (real HOME, twice, ~2 min apart)
Expected:    SHARED.md: every ocm invocation under a scratch HOME
Actual:      my two commands ran against the real home. The deterministic
             effect of that exact pair was probe-verified in
             `/tmp/ocm-e2e5/homes/a2-probe`: full revert to the
             0.6.1-stamped loader plus four stray 0.7.0-only module files
             (`renames.js`, `digest.js`, `defect.js`, `gate.js`).
             The real home's **current** state is instead a coherent,
             complete published-0.7.0 loader (all 36 module stamps + entry
             `// ocm-version: 0.7.0 7a0130cd`); `registry.json` /
             `opencode.json` untouched (mtime 20:34, `ocmVersion` 0.6.1),
             `tui.json` Sep 15, no leftover `registry.lock`, cache already
             in 0.7.0 layout since Sep 20. The loader rewrite timestamp
             (~21:02:13) postdates my 0.6.1 call, so a **third** 0.7.0
             binary — not mine — rewrote it concurrently, most likely
             another round-5 participant making the same bare-command
             mistake.
Impact:      no user impact and no restoration needed (the current state
             is a valid 0.7.0 loader over an untouched registry), but the
             primary agent should know more than one participant is
             leaking commands into the real home.
Regression?: process breach, first occurrence for this agent. All
             subsequent invocations (every command in this report) ran
             under scratch homes; every home's invariants (no `.claude` /
             `.agents`, no dangling links, valid JSON) were checked at the
             end of each scenario.

## F221 — a local fold's dropped plugin is invisible to the update report: `already up to date` while a plugin is uninstalled   (area: CLI, severity: low)

Test:        A5 / step 17
ocm version: 0.7.0      opencode: 1.18.32      XDG: unset
Command:     local marketplace on case-sensitive APFS with `case-Kit`
             installed; lowercase `plugins/case-kit` appears; `ocm update`
Expected:    brief 28 §3.3: the folded pair's plugins are skipped with the
             warning and the rest of the update proceeds — F128's fix was
             supposed to make the report and the disk agree
Actual:      exit 0; the skip warning fires on stderr and the drop is real
             (record gone, link gone), but stdout prints
             `already up to date` + `restart opencode to activate`. Root
             cause: the fold filter removes the candidate from
             `registrable` (`loader/reconcile.js:90-96`) without pushing
             it to `dropped`/`pruned`/`removed`; the record then vanishes
             via `registerPlugins`' wholesale replacement of the plugins
             map, so `report.changed` stays false
             (`src/commands/update.ts:176-183`) and the per-item report
             never runs.
Impact:      a user reading stdout believes nothing changed while a
             plugin was just uninstalled; the stderr warning tells the
             truth but the summary contradicts it. Disk state is correct.
Regression?: round 4's F128 reported the same reporting half ("report
             says 0 removed") alongside the cross-wire; the cross-wire is
             fixed, the uncounted drop remains.

## F222 — the loader's startup sync discards its warnings: a grandfather-ended uninstall inside opencode is completely silent   (area: loader, severity: med)

Test:        A4 / step 16 extra (loader-side sequence, home a2-7)
ocm version: 0.7.0      opencode: 1.18.32      XDG: unset
Command:     0.6.1-era seeded local marketplace (manifest-less
             `ancient-kit` installed, digests baselined by one 0.7.0
             session), plugin content edited, opencode restarted (TUI,
             sized pty)
Expected:    the same event via `ocm update` prints a careful explanation
             (warning: "changed upstream and still has no plugin.json —
             uninstalled / it predates the plugin.json requirement…" );
             brief 30 §3's startup path should surface the same signal
Actual:      the drop happens (registry record gone, link gone, opencode
             no longer sees the command) but nothing anywhere reports it:
             no stderr in the pty log, no TUI surface, and
             `lastSync: { ok: true, error: null }` — the registry records
             the sync as clean. Root cause: `loader/ocm-loader.js:11` runs
             `void core.syncAll({ reason: "startup" })`, discarding the
             result; `syncAll` collects reconcile warnings (fold skips,
             rename refusals, grandfather-ended drops) in
             `result.warnings` (`loader/sync.js:204-205`) and no consumer
             exists on the startup path.
Impact:      for a user who only runs opencode (the auto-sync loader's
             whole audience), a plugin silently disappears from their
             session after an upstream change, with no pointer to the
             remedy the CLI path prints. Disk state is correct; this is a
             missing diagnostic on the highest-traffic path.
Regression?: new defect — the warnings themselves (fold, refusal,
             grandfather-end) predate 0.7.0, but the grandfather can now
             end at startup for local marketplaces (brief 30), which makes
             the silence consequential for the first time.

## F223 — `add` on a mixed tree silently ignores a component-less broken plugin.json while installing the sibling   (area: CLI, severity: low)

Test:        A5 / step 19 (beyond the plan's isolated-tree scope)
ocm version: 0.7.0      opencode: 1.18.32      XDG: unset
Command:     `ocm add` on a local tree with `broken-kit` (only content a
             broken plugin.json) + `good-kit` (valid, 1 command)
Expected:    F146's stated expectation — "report an unreadable
             plugin.json wherever the directory exists"
Actual:      `ocm validate` reports it (1 error, exit 1, same wording as
             the isolated tree), but `ocm add` exits 0, installs
             `good-kit`, and never mentions `broken-kit` in either stream.
             Registry holds only `good-kit`.
Impact:      an author who tests with `add` instead of `validate` inside a
             mixed marketplace gets no signal about the broken manifest;
             users are unaffected (nothing installs either way). Same
             shape round 4 recorded as F146's mixed-marketplace half.
Regression?: residual of F146 — the isolated-tree half (what round 5's
             step 19 names) is closed; the mixed-tree half at `add` is
             unchanged from round 4.

---

## Delegated to human

None. Every step in my scope (11–19) ran to completion against real
binaries; the only TUI interaction needed (restart sessions) was driven
via `expect` with a sized pty.

## Not run

Nothing in my assigned scope. Items worth flagging to the primary agent:

- The loader-side warning silence (F222) was tested for the
  grandfather-end event only; the fold and rename-refusal warnings share
  the same discarded `result.warnings` path (code-verified,
  `loader/sync.js:204-205`) but were not separately driven through a TUI
  session.
- The third writer identified in F220 operated concurrently with my
  session; if no other round-5 participant reports it, the primary agent
  should treat the real-home loader rewrite as unexplained rather than
  closed by my account.
