# Round-5 E2E — Part B1 (agent b1): 0.6.1 → 0.7.0 in-place upgrade, cache migration

Date: 2026-09-22. ocm under test: 0.7.0 (`/tmp/ocm-e2e5/v070/node_modules/.bin/ocm`),
baseline 0.6.1 (`/tmp/ocm-e2e5/v061/node_modules/.bin/ocm`), opencode 1.18.32.
XDG_CONFIG_HOME unset unless stated. Every ocm invocation ran under a scratch
`HOME` under `/tmp/ocm-e2e5/homes/b1-*` (enforced by the `ocmt`/`run` helpers in
`/tmp/ocm-e2e5/work-b1/env.sh`) — with one exception, disclosed below.

Plan: `.work/briefs/T4-round5-plan.md` Part B1, steps 1–6.

## 0. Disclosure — one real-HOME invocation

My session's very first command, before any scratch home existed, was
`/tmp/ocm-e2e5/v070/node_modules/.bin/ocm --version` **without `HOME=`** — it ran
against the real home. It printed
`refreshed the auto-sync loader: 0.6.1 → 0.7.0 (restart opencode to activate)`
and rewrote the loader module stamps (core.js / ocm-loader.js / ui.js) in the
real `~/.config/opencode/ocm/` at 21:02. The registry (20:34) was untouched and
the real home was already on the `roots/` layout from earlier rounds, so no
old→new cache migration ran there. No other real-HOME invocation happened; every
subsequent command went through the `$H` prefix check. This incident is also
live proof of the loader-refresh half of F246.

## 1. Verdicts at a glance

| Step | What | Verdict |
|---|---|---|
| 1 | build 0.6.1 three-marketplace home, old layout, displaced record | **PASS** |
| 2 | first 0.7.0 command migrates the cache | **PASS** (but see F246) |
| 3 | post-migration verification (bytes, TUI, debug, sync) | **PASS** |
| 4 | interrupt matrix during migration + recovery | **FAIL** — F244 (high), F245 (med), F246 (low) |
| 5 | local/exec-only variants (no git clone) | **PASS** (but see F249) |
| 6 | dual-root home (default + XDG) sharing the old cache | **FAIL** — F247 (high), F248 (med), F249 (low) |

## 2. Step-by-step evidence

### Step 1 — 0.6.1 home (PASS)

`zsh /tmp/ocm-e2e5/work-b1/build061.sh /tmp/ocm-e2e5/homes/b1-1` — adds
root-mp (git), demo-marketplace (local), exec-mp (local, exec plugins), grants
trust, then displaces a hand-written `plain-kit:quiet.md` via
`ocm install plain-kit --force`. Output: `built … OK (3 marketplaces, old
layout, displaced record)`.

Pre-upgrade inventory: `$W/b1-1-pre.txt` — 40 cache files in the 0.6.1 layout,
including (verbatim lines):

```
65ec9051e31f772b22d0f09db816da1fe1efaf00  /.cache/ocm/displaced-records.json
6fc9cd5536fc08f3904a1b84911e32dee8276d44  /.cache/ocm/links/demo-marketplace/...
de7c1050ae7e53f2fb7dece9bb33b411fc808769  /.cache/ocm/roots.json
```

`opencode.json` `skills.paths` pointed at the old links path; the displaced
original lived under `.cache/ocm/displaced/<ts>/tmp/…`. A TUI session under
0.6.1 ran clean (sized pty, `$W/b1-1-tui-before.log`).

### Step 2 — first 0.7.0 command migrates (PASS)

The first 0.7.0 invocation under `b1-1` was `ocm --version`; it printed the
four `moved …` lines and `refreshed the auto-sync loader to 0.7.0 …` (output
not captured to a file at the time; the identical four lines are captured
verbatim in run 6's killed stdout below, and the mechanism re-captured cleanly
on a fresh home for F246). Every claim was verified true on disk against
`b1-1-pre.txt`/`b1-1-post.txt`: `marketplaces`, `links`, `displaced`,
`displaced-records.json` all renamed into `roots/default-5ca4a1/`; `roots.json`
**not** moved (same sha `de7c1050…` at `/.cache/ocm/roots.json` before and
after). `ocm list --all` exit 0; `ocm doctor` exit 0 printing the slug.

### Step 3 — post-migration verification (PASS)

Byte-level compare of the whole cache, pre vs post (`$W/b1-1-pre.txt` vs
`$W/b1-1-post.txt`, paths/shas normalized for the declared noise list):
**39 of 40 files identical**. The two differences, verbatim from the raw
inventories:

```
pre:  65ec9051…  /.cache/ocm/displaced-records.json
post: 3b74c916…  /.cache/ocm/roots/default-5ca4a1/displaced-records.json
pre+post: de7c1050…  /.cache/ocm/roots.json      (unmoved, same sha)
```

`displaced-records.json` was legitimately rewritten (its `dir` field now names
the new namespace — the tree itself moved by rename, content byte-identical
apart from that field). The git clone survived intact (HEAD `de29ae7`, clean
working tree). Registry `dir` fields, `skills.paths` and all command-symlink
targets rewritten to the new namespace. Two full TUI sessions ran with **no
trust re-prompts** (`$W/b1-1-tui-after.log`). A forced sync
(`OCM_SYNC_INTERVAL_MS=0`, `$W/b1-1-tui-forced.log`) re-materialized
`root-kit:hello.md` as a rendered 283 B regular file with the
`${OCM_PLUGIN_ROOT}` substitution resolving to the **new** cache path.
`opencode debug config` / `debug skill` (run from a neutral cwd) resolve
commands, agents, skills, mcp keys and the exec plugins (`$W/b1-1-debug-before.json`,
`$W/b1-1-debug-after.json`). Invariants clean, doctor exit 0.

### Step 4 — interrupt matrix (FAIL → F244, F245, F246)

Matrix: `kill -9` after 10/12/13/14/15/20/25/30/50 ms against `list --all`,
`doctor`, `update` (`$W/interrupt.sh`, 11 runs, logs `$W/log-i-{1..11}.txt`),
each followed by recovery (`list --all`, `doctor`) and a normalized whole-tree
diff against a clean-migration reference home (`b1-ref`, `$W/ref-clean.txt`).

- **10–12 ms** (runs 2, 5): killed before the migration started → recovery
  finishes the whole job → final state identical to reference.
- **13 ms** (run 6): killed inside the migration's rewrite window → **F244**.
- **14–50 ms** (runs 1, 3, 4, 7, 8): killed after the migration (or the command
  had already finished) → recovery clean → final state identical to reference.
- **150/400/900 ms against `update`** (runs 9–11): the command completed before
  the kill landed — **not true interrupts**; the diffs are only update's own
  legitimate digest/FETCH_HEAD writes.

Honesty note on my own harness: the normalized diff reported "DIFFERS" for
runs 1, 3 and 4, but direct on-disk verification shows those homes are in the
clean state — the diffs consisted solely of sha lines for `roots.json`,
`displaced-records.json` and `.git/index`/reflogs, i.e. exactly the declared
per-home noise (those files embed the home path or clone timestamps); my
exclusion regex missed them. Runs 2/5/7/8 compared clean. Only run 6 diverged
in substance (symlink targets, `skills.paths`, displaced record — see below).

Run 6, verbatim (from `$W/log-i-6.txt`, home `b1-i-6`, `kill -9` after 13 ms
during `list --all`):

```
kill exit=0, process exit=137
--killed stdout--
moved …/.cache/ocm/marketplaces -> …/.cache/ocm/roots/default-51c090/marketplaces
moved …/.cache/ocm/links -> …/.cache/ocm/roots/default-51c090/links
moved …/.cache/ocm/displaced -> …/.cache/ocm/roots/default-51c090/displaced
moved …/.cache/ocm/displaced-records.json -> …/.cache/ocm/roots/default-51c090/displaced-records.json
-- state right after kill --
old-items=0 moved-items=4
root-mp.dir = …/.cache/ocm/roots/default-51c090/marketplaces/root-mp     ← registry already rewritten
skills.paths = …/.cache/ocm/links/demo-marketplace/skills                 ← still old
displaced rec dir (old/new): …/.cache/ocm/displaced/202-…-631Z |          ← still old
cmd-link target: …/.cache/ocm/marketplaces/root-mp/…/hello.md             ← still old
--- recovery: 0.7.0 list --all ---
recovery exit=0                      ← exits 0, migrates nothing, repairs nothing
```

The recovery chain (deterministic reproduction on `b1-sim3`, built fresh via
`build061.sh` + the same four renames + registry-dir rewrite only — state
matching run 6's on-disk state exactly) is captured under F244.

The restart-opencode-first variant (`b1-sim2`, F245) and the `--version`
capture (`b1-f246`, F246) are below.

### Step 5 — local/exec-only variants (PASS)

- `b1-local1` (demo-marketplace + exec-mp: links + displaced record, no git
  clone): 0.7.0 moved `links`, `displaced`, `displaced-records.json`, rewrote
  `skills.paths`. Doctor, verbatim:

```
doctor
  loader  core.js (current)
  loader  ocm-loader.js (current)
  loader  ui.js (current)
  cache   default-a915a5 (…)
  displaced  1 original in …/roots/default-a915a5/displaced
exit=0
```

- `b1-local2` (exec-mp only, zero cache footprint): nothing to migrate, no
  spurious warnings, doctor exit 0 — but note the `cache` line names a
  namespace that does not exist on disk (F249).

### Step 6 — dual-root home (FAIL → F247, F248, F249)

Home `b1-dual`: default root (root-mp git + demo-marketplace local) and XDG
root `$H/xdg` (root-mp2 — a second copy of the root-mp tree, avoiding F187's
ENOTEMPTY — plus exec-mp), both installed under 0.6.1, sharing the old
`.cache/ocm/{marketplaces,links}` (verified: `marketplaces/{root-mp,root-mp2}`,
`links/demo-marketplace`). `roots.json` records both roots.

Default-root-first order: 0.7.0 under the default root moved the **whole**
shared cache — including root-mp2's clone, owned by the XDG root — into
`roots/default-d4d059/`. Then 0.7.0 under the XDG root printed no `moved`
lines at all (nothing left to move), silently re-pointed `root-mp2.dir` at
`roots/xdg-31c2a4/marketplaces/root-mp2` (never created), and `list --all`
**exited 0** claiming `root-mp2 … revision: de29ae7, synced just now` while
the clone was missing. Doctor under the XDG root: 5 errors, exit 1 (verbatim
under F247). Recovery via `ocm update` re-clones and repairs (2 skills
re-materialized). Mirror-image breakage confirmed in the reverse order
(`b1-dual2`: XDG first → default root's root-mp clone and demo-marketplace
skill mirrors swallowed; 3 broken symlinks + 2 missing-materialization errors;
`ocm update` repaired everything except the stranded error, see F248).

## 3. Findings

### F244 — a 0.7.0 command killed inside the migration's rewrite window is never finished by any later command; the displaced original is permanently stranded and doctor mislabels ocm's own broken links   (area: CLI, severity: high)

```
Test:        Part B1 / step 4
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset
Command:     zsh /tmp/ocm-e2e5/work-b1/interrupt.sh 6 13 list --all
             (kill -9 after 13 ms; deterministic repro: build061.sh + 4 renames
              + registry-dir rewrite only — home b1-sim3, recovery chain below)
Expected:    migration runs "before dispatch" on every command (0.7.0 release
             notes / migrate-cache.ts), so a later command should detect and
             finish a partial migration; nothing user-owned may be stranded.
Actual:      kill lands after rewriteRegistryDirs but before
             rewriteSkillsPaths / rewriteDisplacedRecords / repointSymlinks.
             From that moment cacheMigrationNeeded() is permanently false
             (registry dirs name the new path; links/displaced-records no
             longer sit at the old path), so no later 0.7.0 command re-runs
             any of it. Recovery chain, verbatim (b1-sim3):

  $ ocm list --all            exit=0 — no migration, no repair
  $ ocm doctor                exit=1 — 7 errors, 1 warning:
    error   …/commands/root-kit:plain.md: broken symlink → …/.cache/ocm/marketplaces/root-mp/… (not ocm's, left in place)
    error   …/commands/plain-kit:quiet.md: broken symlink → … (not ocm's, left in place)
    error   …/commands/root-kit:hello.md: broken symlink → … (not ocm's, left in place)
    warning …/roots/default-337177/displaced/2026-…: displaced copy with no record (removed by an older ocm?) — restore or remove it by hand
    error   marketplace "root-mp": 3 materialized component(s) missing (ocm update)
    error   marketplace "root-mp": plugin "plain-kit" records command "quiet.md" with no materialization — stale record (ocm update root-mp)
    error   marketplace "root-mp": plugin "root-kit" records command "hello.md" with no materialization — stale record (ocm update root-mp)
    error   marketplace "root-mp": plugin "root-kit" records command "plain.md" with no materialization — stale record (ocm update root-mp)
    7 errors, 1 warning
  $ ocm doctor --fix          exit=1 — "fixed  marketplace "root-mp":
    re-materialized 3 component(s)" (links repaired) but skills.paths and the
    displaced record untouched
  $ ocm update                exit=0 — appends skills.paths, leaving
    ["/tmp/…/b1-sim3/.cache/ocm/links/demo-marketplace/skills",     ← stale, dir does not exist
     "/tmp/…/b1-sim3/.cache/ocm/roots/default-337177/links/demo-marketplace/skills"]
    displaced record still points at …/.cache/ocm/displaced/2026-… (old path)
  $ ocm doctor                exit=0 — but the warning repeats forever:
    warning …/roots/default-337177/displaced/2026-…: displaced copy with no
    record (removed by an older ocm?) — restore or remove it by hand
    0 errors, 1 warning
Impact:      three defects in one window. (1) The user's displaced original —
             a hand-written file ocm promised to keep — is permanently
             stranded: no command repairs the record's dir, and the standing
             advice ("restore or remove it by hand") sends the user hunting
             for a file "removed by an older ocm" that ocm itself moved.
             (2) doctor labels ocm's OWN broken command links "(not ocm's,
             left in place)" — the opposite of the truth, and `doctor --fix`
             then fixes them anyway. (3) skills.paths keeps a dead entry
             pointing at a nonexistent directory that doctor never flags
             (possible Part D / brief-33 overlap — skills-paths semantics).
Regression?: new — the cache migration is new in 0.7.0; round 4 had no
             interrupted-migration test. Natural repro (run 6) and the
             deterministic repro agree.
```

### F245 — restarting opencode before running any 0.7.0 CLI command makes the migration run forever: every command re-prints a false "moved" claim and three warnings, and the old tree is never cleaned up   (area: loader/CLI, severity: med)

```
Test:        Part B1 / step 4 (restart-opencode-first variant)
ocm version: 0.7.0 (CLI) with the un-refreshed 0.6.1 loader still installed
             opencode: 1.18.32
XDG:         unset
Command:     build 0.6.1 home → run opencode TUI (the 0.6.1 loader re-creates
             the skill mirrors at the OLD .cache/ocm/links path) → run any
             0.7.0 command. Reproduced on b1-sim2; re-run verbatim:
  $ HOME=$H ocm list --all          (third+ iteration, same output every time)
  exit=0
  --stdout--
  moved …/.cache/ocm/links -> …/.cache/ocm/roots/default-587738/links
  …full healthy listing…
  --stderr--
  warning: left …/.cache/ocm/links/demo-marketplace/skills/demo-kit--code-review/SKILL.md in place; …/roots/default-587738/links/…/SKILL.md already exists — move it by hand
  warning: left …/.cache/ocm/links/demo-marketplace/skills/release-kit--release-notes/SKILL.md in place; … already exists — move it by hand
  warning: …/.cache/ocm/links left in place — it may belong to another config root
Expected:    after one migration the predicate is false and nothing re-runs;
             printed lines state true facts.
Actual:      the 0.6.1 loader's startup sync re-creates `links/<name>` under
             the old cache path, which keeps cacheMigrationNeeded() true
             forever. Every subsequent 0.7.0 command re-runs the migration:
             the "moved … links" line is false both times after the first
             (nothing moves — the destination already exists), the two
             "already exists — move it by hand" warnings are self-inflicted
             (ocm's own loader created the "conflict"), and the old tree is
             never removed. Confirmed repeating across list and doctor runs
             ($W/…/b1-sim2/.out, .d2out, .o3 — identical output each time).
Impact:      a user who upgrades ocm and simply restarts opencode (the natural
             order — the loader tells them to) gets a cache that never
             converges: permanent warning noise on every command, a stale
             duplicate tree, and migration output that lies. Irony: this path
             DOES repair skills.paths and the displaced record (the re-run
             rewrites them), unlike the CLI-first path of F244.
Regression?: new — no round-4 finding covers the old loader re-creating
             mirrors at the old path post-upgrade.
```

### F246 — `ocm --version` performs the cache migration and refreshes the loader: a version query mutates user data   (area: CLI, severity: low)

```
Test:        Part B1 / steps 2/4
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset
Command:     HOME=$H /tmp/ocm-e2e5/v070/node_modules/.bin/ocm --version
             (fresh 0.6.1 home, one git marketplace, old layout)
Expected:    a --version query is read-only.
Actual:      exit=0
  --stdout--
  moved /tmp/ocm-e2e5/homes/b1-f246/.cache/ocm/marketplaces -> /tmp/ocm-e2e5/homes/b1-f246/.cache/ocm/roots/default-1df3d8/marketplaces
  refreshed the auto-sync loader to 0.7.0 (restart opencode to activate)
  0.7.0
  --stderr-- (empty)
             On disk afterwards: old `marketplaces/` gone, `roots/default-1df3d8/`
             present, loader modules rewritten.
Impact:      scripts that probe `ocm --version` (release tooling, CI matrices)
             silently migrate the invoking user's cache and rewrite their
             loader. Harmless in the happy path, surprising at best; it also
             widens F244's crash window to any version probe.
Regression?: new behaviour in 0.7.0 (migration-before-dispatch runs on every
             command including --version). Round 4's F166 fix (loader
             auto-refresh) is what made version queries mutating.
```

### F247 — in a dual-root home, whichever root runs 0.7.0 first swallows the whole shared cache — the other root's clone and mirrors included — and that root is left broken while `list` exits 0 claiming health   (area: CLI, severity: high)

```
Test:        Part B1 / step 6
ocm version: 0.7.0      opencode: 1.18.32
XDG:         set to $H/xdg for the second root
Command:     HOME=$H ocm list --all        (default root, first 0.7.0 run)
             HOME=$H XDG_CONFIG_HOME=$H/xdg ocm list --all   (XDG root, after)
Expected:    each root's migration moves only what that root owns, or refuses
             to touch a cache it cannot attribute; brief 38 makes dual-root a
             supported configuration.
Actual:      default-root run moved the ENTIRE old cache into its own
             namespace — including root-mp2's clone, which belongs to the XDG
             root:
  moved …/.cache/ocm/marketplaces -> …/.cache/ocm/roots/default-d4d059/marketplaces
  moved …/.cache/ocm/links -> …/.cache/ocm/roots/default-d4d059/links
             (afterwards: roots/default-d4d059/marketplaces/{root-mp,root-mp2}
              both present; roots/xdg-31c2a4 never created)
             The XDG-root run then printed NO migration lines (nothing left to
             move), silently re-pointed the registry:
  root-mp2.dir = …/.cache/ocm/roots/xdg-31c2a4/marketplaces/root-mp2   ← does not exist
             and lied about health:
  $ HOME=$H XDG_CONFIG_HOME=$H/xdg ocm list --all
  exit=0
  root-mp2 (auto)
    source: file:///tmp/ocm-e2e5/work-b1/root-mp2
    revision: de29ae7
    synced just now                       ← clone is MISSING on disk
             All three command symlinks broken. Doctor: 5 errors, exit 1 —
             stranded error (F248) + "marketplace "root-mp2": directory
             missing (…roots/xdg-31c2a4/…) — ocm update re-clones" + three
             broken-symlink errors. Mirror image confirmed in reverse order
             (b1-dual2, XDG first): default root left with root-mp clone and
             demo-marketplace skill mirrors swallowed — 3 broken symlinks,
             "marketplace "demo-marketplace": 2 materialized component(s)
             missing". Recovery works: `ocm update` re-clones / re-materializes
             (exit 0, links OK afterwards).
Impact:      a dual-root user who upgrades one root first silently steals the
             other root's data. The broken root's `list` exits 0 and reports
             "synced just now", so the damage is invisible until a command or
             opencode actually fails. Data is recoverable only by re-fetching
             (re-clone/re-materialize); any local-only state in the swallowed
             clone is gone.
Regression?: new trigger of the round-4 shared-cache family: F168 (doctor
             --fix deletes the other root's mirrors) and F189 (ocm remove
             breaks the other root) share the root cause — one physical cache,
             two registries — but were fixed/documented for those commands;
             the 0.7.0 migration is a new, unsupervised writer of the same
             shared resource, and it runs automatically on every command.
```

### F248 — doctor reports a legitimate dual-root setup as "an ocm install is stranded in another config root", forever, contradicting the mutation notice's own guard   (area: CLI, severity: med)

```
Test:        Part B1 / step 6
ocm version: 0.7.0      opencode: 1.18.32
XDG:         both directions tested
Command:     HOME=$H ocm doctor           (b1-dual, default root — a fully
             working install: registry, clone, links all healthy)
Expected:    brief 38 legitimizes dual-root; src/stranded.ts's own mutation
             notice guards with "two live installs are a user decision, not a
             stranding" (reportStrandedNotice returns early when the active
             root holds a registry).
Actual:      exit=1, on every run, in both directions:
  error   an ocm install is stranded in another config root
    installed at: …/b1-dual/xdg/opencode (2 marketplaces)
    this shell:   …/b1-dual/.config/opencode (XDG_CONFIG_HOME is not set)
    opencode reads the second; nothing in the first is visible to it
    set XDG_CONFIG_HOME to …/b1-dual/xdg to use the existing install, or re-add those marketplaces here
             (doctor.ts pushes strandedMessage for every recorded root with
              marketplaces, with no existsSync(registry) guard — stranded.ts:89
              has the guard, doctor.ts:27-50 does not.)
Impact:      a user who deliberately runs two roots (the configuration brief
             38 supports, and roots.json explicitly records) can never get a
             green doctor: exit 1 forever, with advice ("nothing in the first
             is visible to it") that is false for their setup. It also drowns
             the one real signal in F247's doctor output.
Regression?: the code is identical in 0.6.1 (not a code regression), but
             brief 38 legitimizing dual-root makes the contradiction newly
             reachable in supported use; round 4 filed only the opposite
             direction (F167, read-only commands DENYING a stranded install
             exists — fixed in 0.7.0).
```

### F249 — doctor prints a `cache <slug> (<path>)` line for a namespace directory that does not exist   (area: CLI, severity: low)

```
Test:        Part B1 / steps 5/6
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset (b1-local2) / set (b1-dual xdg root pre-recovery)
Command:     HOME=$H ocm doctor   (b1-local2: exec-mp only — no git clone, no
             skill mirrors, zero cache footprint)
Expected:    every printed line states a true fact.
Actual:      exit=0
  cache   default-8d46e9 (…/b1-local2/.cache/ocm/roots/default-8d46e9)
             followed by `ls …/b1-local2/.cache/ocm/roots/`:
  ls: …: No such file or directory
             Same shape on b1-dual's XDG root before recovery: the cache line
             named roots/xdg-31c2a4 while that directory did not exist.
Impact:     a user following the line to "find their own" cache (its stated
             purpose, brief 38) finds nothing. Minor, but the line asserts
             existence it never checks.
Regression?: new line in 0.7.0 (brief 38 added it).
```

## 4. Cross-cutting invariants

Sweep across all b1 homes (`env.sh inv` + doctor exit):

- No writes under `$H/.claude`, `$H/.agents` anywhere. **PASS**
- Registry and `opencode.json` valid JSON everywhere. **PASS**
- No broken ocm symlinks and doctor exit 0 on: b1-1, b1-ref, b1-i-{1,2,3,4,5,7,8}
  (post-recovery), b1-local1, b1-local2. **PASS**
- Broken ocm symlinks with doctor exit 1 on: b1-i-6 / b1-sim3 (F244 — until
  `update`/`doctor --fix` repaired the links; the displaced record and stale
  skills.paths remain), b1-dual + b1-dual2 second roots (F247, until
  `ocm update`), b1-sim2 (F245 — old tree left in place, by design of the
  defect). These ARE the findings; no invariants violated beyond them.
- b1-dual/b1-dual2 post-`update`: all links OK; doctor still exit 1 solely
  from F248's stranded error.

## 5. Not run / honesty notes

- The 150/400/900 ms `update` interrupts (runs 9–11) were **not true
  interrupts** — update finished in under 150 ms every time. The migration's
  own crash window was covered by the 13 ms kill (natural) and the
  deterministic simulation (twice, agreeing).
- b1-1's original first-`--version` output was not captured to a file at the
  time; the mechanism was re-captured verbatim on b1-f246, and the identical
  four `moved` lines appear in run 6's killed stdout. On-disk effects for b1-1
  are fully covered by the pre/post inventories.
- My interrupt harness's normalized diff produced three false "DIFFERS"
  verdicts (runs 1/3/4) from per-home sha noise its exclusion regex missed;
  direct on-disk verification (symlink targets, skills.paths, displaced record,
  doctor exit) shows those homes clean. Verdicts above rely on the direct
  checks, not the harness diffs.
- `opencode debug config` / `debug skill` were run from a neutral cwd to avoid
  this repo's `.opencode/skills` leaking into the result.

## 6. Delegated to human

None. All TUI interaction was driven via `expect` with a sized 30×100 pty
(`$W/tui.exp`); no scenario required a human in the loop.

## 7. Evidence manifest (all under /tmp/ocm-e2e5/)

- `work-b1/env.sh`, `build061.sh`, `interrupt.sh`, `tui.exp` — harness
- `work-b1/b1-1-pre.txt`, `b1-1-post.txt` — step 1/3 inventories (40 files)
- `work-b1/b1-1-tui-{before,after,forced}.log`, `b1-1-debug-{before,after}.json` — step 3
- `work-b1/ref-clean.txt`, `ref-n.txt`, `i-{1..11}.txt`, `i-*-n.txt`, `i-*-diff.txt`, `log-i-{1..11}.txt` — step 4 matrix (run 6 = F244 natural repro)
- `homes/b1-sim3/.r1o…r5e` — F244 deterministic recovery chain (list / doctor / doctor --fix / update / final doctor)
- `homes/b1-sim2/.out`, `.d2out`, `.d2err`, `.o3`, `.e3`, `work-b1/sim2-skills.json`, `b1-sim2-tui.log` — F245
- `homes/b1-f246/.o`, `.e` — F246
- `homes/b1-dual/.o1/.e1/.o2/.e2/.d1/.d2/.u2/.ue2/.d` , `homes/b1-dual2/.o1/.e1/.o2/.e2/.d/.u/.ue/.d2` — F247/F248/F249 (both orders + recoveries)
- `homes/b1-local1`, `b1-local2` — step 5
