# ocm v0.7.0 E2E — round 5, agent B2 (Parts B2, B3, B4)

- ocm: 0.7.0 (`/tmp/ocm-e2e5/v070/node_modules/.bin/ocm`), checked before every scenario
- opencode: 1.18.32, `/usr/bin/expect` for TUI sessions
- All invocations under scratch homes `/tmp/ocm-e2e5/homes/{b2-1,b2-2,b3-1,b3-2,b4-0,b4-1}` with stdout/stderr captured separately
- Fixtures: copies under `/tmp/ocm-e2e5/work-b2/` (root-mp at 694e071, rename-mp at 1aad6c8, rival-mp, many-mp, rename-mp2 at 6af7d3b)

**Verdict: 11/11 steps PASS.** No high findings. One med (F258: silent revert of user-edited rendered file). Known-fixed items F84 and F106 verified good.

---

## Part B2 — rendered-command lifecycle (steps 7–11)

### Step 7 — upstream edit refreshes the rendered command — PASS

Setup: b2-1, root-mp-a installed (plugin root-kit, rendered command `commands/root-kit:hello.md` with `!`-block and vars). Upstream `hello.md` edited ("Say hello" → "Say hello v3"), committed 694e071, then:

```
$ ocm update root-mp-a            exit=0
updating root-mp-a...
  de29ae7 → 694e071
  root-kit   0.1.0 → 0.2.0
    ~ commands/hello.md
  restart opencode to activate
```

`--json` (re-run): `{"component":"hello.md","state":"refreshed"}` — refreshed, not created/removed. On disk the rendered file body was updated and its trailing marker named the new rev:

```
<!-- ocm: rendered from plugins/root-kit/commands/hello.md @ 694e071a5c46a1ad19f75c9fda2c685757ce693f -->
```

The rendered file was updated in place (mtime changed, inode kept — it is a real file, not a symlink).

### Step 8 — user edit of a rendered file is reverted on update — PASS (behaviour as designed, wording defect → F258)

User appended `MY LOCAL TWEAK` after the marker in the rendered file. Next update:

```
$ ocm update root-mp-a            exit=0
updating root-mp-a...
  already up to date
```

Yet the file on disk was silently reverted to the rendered body — the local tweak is gone with no warning on either stream. Content control is the point of rendered files (marker = "managed"), but "already up to date" + silent revert of user bytes is the defect (F258, med).

### Step 9 — uninstall removes rendered file; remove cleans everything — PASS

```
$ ocm uninstall root-kit@root-mp-a    exit=0
uninstalled root-kit from root-mp-a
  root-kit: 2 commands removed
restart opencode to activate
```

Rendered file and symlink both gone from `commands/`. Registry record kept: `enabled:false, installedAt:null`. Re-install (`ocm install root-kit@root-mp-a`) restored both files and reset `enabled:true, installedAt:<now>`.

```
$ ocm remove root-mp-a                 exit=0
removed marketplace "root-mp-a"
  root-kit: 2 commands removed
restart opencode to activate
```

Registry empty (`marketplaces: {}`), cache clone removed. Cosmetic only: the slug dir (`roots/default-*/`) remains with `roots.json` (`{"roots":{}}`) and an empty `marketplaces/`. No broken links; `ocm doctor` exit 0 after each operation.

### Step 10 — hand-written file at the rendered path — PASS (two findings: F259-adjacent headline, F260 skip-vs-success, F261 duplicate fact)

With a hand-written `commands/root-kit:hello.md` present:

```
$ ocm install root-kit@root-mp-a       exit=0
installed root-kit@root-mp-a ()
```
stderr:
```
warning: commands/root-kit:hello.md exists and is not managed by ocm
  skipped it; re-run with --force to displace it
```

Hand-written file untouched on disk (bytes compared). Exit code 0 with an "installed" headline while a component was skipped — misleading success (F260, low). Empty `()` after the plugin name — pre-derive snapshot, cosmetic (F259, low).

With `--force`:

```
$ ocm install root-kit@root-mp-a --force    exit=0
installed root-kit@root-mp-a (1 commands)
displaced your commands/root-kit:hello.md → /tmp/ocm-e2e5/homes/b2-1/.cache/ocm/roots/default-*/displaced/2026-09-22T…-root-kit:hello.md
restart opencode to activate
```

The displacement line was printed on stdout as part of the report **and** as a stderr warning (`warning: displaced your commands/root-kit:hello.md …`) — same fact, both streams (F261, low; F107-class). Rendered file took the destination; hand-written bytes preserved under `displaced/<ts>/`; `displaced-records.json` records it.

```
$ ocm remove root-mp-a                  exit=0
removed marketplace "root-mp-a"
  root-kit: 2 commands removed
restored your commands/root-kit:hello.md (was displaced by root-kit)
restart opencode to activate
```

Hand-written bytes returned verbatim to `commands/root-kit:hello.md`; `displaced-records.json` → `[]`. This is the round-4 displacement restore behaviour working as fixed.

### Step 11 — rendered command served and executed by real opencode — PASS

Home b2-2 (root-mp-b, forced-sync TUI session first). `opencode debug config` serves `root-kit:hello` with the `!`-block template and vars substituted (F84 fix verified good — round 3 had raw `{{plugin-root}}` here).

Executed for real:

```
$ OCM_SYNC_INTERVAL_MS=0 opencode run --command root-kit:hello --format json    exit=0
```

The stored user message (model input), verbatim from `opencode.db`:

```
Say hello v3: hello-from-root-kit-script

<!-- ocm: rendered from plugins/root-kit/commands/hello.md @ 694e071a5c46a1ad19f75c9fda2c685757ce693f -->
```

So the `!` script ran (its stdout `hello-from-root-kit-script` was spliced in) — but the ocm marker comment is also sent to the model on every invocation (F262, low: noise + leaks source path and git rev into every prompt).

---

## Part B3 — renames across a restart (steps 12–14)

### Step 12 — refused rename survives an opencode restart — PASS

Home b3-1: rival-mp (local) ships `taken-kit`; rename-mp (git) renamed `old-kit` → `taken-kit` with a `renames` map. Previous update refused:

```
refused rename old-kit → taken-kit: "taken-kit" is already provided by marketplace "rival-mp"
```

Restarted opencode under the home (`OCM_SYNC_INTERVAL_MS=0`, expect session). Palette filtered `old-kit` shows:

```
/old-kit:greet    the rename fixture command    old-kit
```

Post-restart disk state: `commands/old-kit:greet.md` → symlink re-pointed at `…/rename-mp/plugins/taken-kit/commands/greet.md` (the new upstream dir, via the refusal alias) — no broken links. Registry unchanged: plugins `["old-kit"]`, `refusedRenames: {"old-kit":"taken-kit"}`, `lastSync.at` advanced. `opencode debug config` (neutral cwd) serves exactly `old-kit:greet` and `taken-kit:hold`. Invoked for real:

```
$ OCM_SYNC_INTERVAL_MS=0 opencode run --command old-kit:greet --format json    exit=0
```

Stored user message body: `Old-kit greet body.` — the command works end-to-end under its old name after the restart.

### Step 13 — collision cleared → rename completes — PASS

```
$ ocm remove rival-mp                  exit=0
removed marketplace "rival-mp"
  taken-kit: 1 commands removed
restart opencode to activate
```

```
$ ocm update rename-mp                 exit=0
updating rename-mp...
  already up to date
  renamed old-kit → taken-kit
  taken-kit   0.1.0 → 0.2.0
  restart opencode to activate
```

Disk: single link `taken-kit:greet.md` → `…/rename-mp/plugins/taken-kit/commands/greet.md`; old `old-kit:greet.md` gone. Registry: plugins `{"taken-kit": {enabled, version 0.2.0, original installedAt kept}}`, `refusedRenames: null` (cleared). `opencode debug config` serves `taken-kit:greet` only.

### Step 14 — rename chain converges — PASS

Fresh home b3-2, fresh upstream copy rename-mp2 as `chain-mp`. Sync 1 (`git mv old-kit mid-kit` + renames map, commit e172ba5):

```
$ ocm update chain-mp                  exit=0
updating chain-mp...
  040d7ed → e172ba5
  renamed old-kit → mid-kit
  mid-kit   0.1.0 → 0.2.0
    ~ commands/greet.md
  restart opencode to activate
```

Commands dir: `mid-kit:greet.md` only. Sync 2 (`git mv mid-kit final-kit` + new renames map, commit 6af7d3b):

```
$ ocm update chain-mp                  exit=0
updating chain-mp...
  e172ba5 → 6af7d3b
  renamed mid-kit → final-kit
  final-kit   0.2.0 → 0.3.0
    ~ commands/greet.md
  restart opencode to activate
```

Converged: `final-kit:greet.md` only — no `old-kit`/`mid-kit` stragglers on disk or in registry (`plugins: ["final-kit"]`, `refusedRenames: null`). `opencode debug config` serves `final-kit:greet`. `ocm doctor` exit 0.

---

## Part B4 — local marketplaces, digests, cost (steps 15–17)

Home b4-1: many-mp (local, plugin many-kit: 25 skills + 1 command) + rival-mp (local). Home b4-0 (`ocm init` only) as baseline.

### Step 15 — timings — PASS

| Operation | Time |
|---|---|
| `ocm add` many-mp (clone/copy + install 25 skills + 1 command) | 0.088s |
| `ocm update many-mp`, unchanged, ×3 (incl. bare `ocm update`) | 0.048–0.067s each |
| opencode spawn→prompt, **sync forced** (`OCM_SYNC_INTERVAL_MS=0`), cold | 8s wall |
| opencode spawn→prompt, sync forced, warm | 2s |
| opencode spawn→prompt, sync throttled (fresh lastSync, no env) | 2s |
| opencode spawn→prompt, baseline b4-0 (no marketplaces) | 6s / 2s (two runs) |

Log-level evidence (bootstrap phase, `opencode.log`): cold forced run shows `loading …/opencode.jsonc` at 16:26:34.081 and `all LSPs are disabled` at 16:26:40.086 (~6.0s gap, includes the forced sync writing `lastSync.at` 16:26:40.082). Warm forced run: the same gap is 54ms (16:30:00.188 → 16:30:00.242), and the throttled run's gap is 25ms. Baseline b4-0's cold run also showed a ~4s gap (16:27:42.135 → 16:27:46.115) with no ocm marketplaces at all — so the 6s cold figure is opencode/first-run noise, not sync cost. **The loader sync itself costs ~25–55ms warm for 26 components; the hourly throttle makes it a no-op on ordinary restarts.**

`ocm add` output reported `many-kit (1 commands, 25 skills)`; `opencode.json` `skills.paths` has 1 entry; `opencode debug skill` (neutral cwd) sees 25 `skill-*` entries. `ocm update` on a local marketplace with no changes prints `already up to date` — F106 (round 3: silent) verified fixed in 0.7.0.

### Step 16 — one component edited → only that plugin reports — PASS

Baseline `ocm update`: both marketplaces `already up to date`. Edited one body line of `skills/skill-07/SKILL.md` upstream, then:

```
$ ocm update                           exit=0
updating many-mp...
  many-kit
    ~ skills/skill-07
  restart opencode to activate
updating rival-mp...
  already up to date
```

Only many-mp/many-kit/skill-07 reported. Rendered copy at `roots/default-*/links/many-mp/skills/many-kit--skill-07/SKILL.md` contains `Body of skill 7, EDITED.` with correct frontmatter `name: "many-kit:skill-07"`. Digest is per-component and per-plugin — no cross-talk. (Marker there reads `@ unknown` for the local marketplace — see F263.)

### Step 17 — no-change update writes only lastSync — PASS

Two consecutive `ocm update` runs, both `already up to date`. Registry diff after each run — the **only** changes:

```
19c19
<         "at": "2026-09-22T16:31:03.963Z",
---
>         "at": "2026-09-22T16:31:07.715Z",
110c110
<         "at": "2026-09-22T16:31:03.979Z",
---
>         "at": "2026-09-22T16:31:07.733Z",
```

i.e. exactly the two `lastSync.at` timestamps, one per marketplace. No plugin records, digests or links touched. Exit 0, empty stderr, both runs.

---

## Findings

## F256 — `ocm --version` mutates a stale auto-sync loader in the real home   (area: CLI, severity: low)

Test:        disclosure (pre-B2), Part B2 preamble
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset
Command:     bare `ocm --version` (real HOME, twice)
Expected:    `--version` is a read-only query; SHARED.md discipline assumes it cannot touch `~/.config/opencode`
Actual:      first call printed `0.7.0` silently; second printed `0.7.0` and `refreshed the auto-sync loader: 0.6.1 → 0.7.0`, rewriting loader modules under `~/.config/opencode/ocm/` and `plugins/ocm-loader.js` (stamped `// ocm-version: 0.7.0`). registry.json / opencode.json / tui.json untouched (older mtimes). The two calls behaved differently; the trigger for the second's refresh was not identified. `refreshStaleLoader` in `src/index.ts` runs for every command including `--version` (deliberate per code comment). Also ran bare `opencode --version` once (read-only).
Impact:      a version check silently upgrades files in the user's real opencode config; agents/tools that probe `--version` as a harmless query get a write. (Reported as my own discipline breach per SHARED.md; the write was ocm's, not mine.)
Regression?: not covered in rounds 3–4 findings; new observation.

## F257 — update omits the restart notice when only a refresh happened   (area: CLI, severity: low)

Test:        Part B2 / step 7
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset (scratch home)
Command:     `ocm update root-mp-a` after an upstream command edit
Expected:    consistent "restart opencode to activate" whenever the materialized output changed (the loader only reads at startup; nothing reloads in-session)
Actual:      the step-7 run printed it (`~ commands/hello.md … restart opencode to activate`), but the `--json` re-run of the same change class reports `"state":"refreshed"`, and `renderMarketplace` (`src/commands/update-report.ts`) prints the restart line only for created/removed components — a refresh-only change (e.g. body edit with no new/removed component) gets no notice, while `src/report.ts` `reportRestart` (install path) does include refreshed.
Impact:      a user whose command body changed on disk sees no notice; the change silently does not apply until restart, contradicting the notice shown for other change kinds.
Regression?: new; adjacent to F128/F190 (round 4) only in area.

## F258 — "already up to date" silently reverts a user-edited rendered file   (area: CLI, severity: med)

Test:        Part B2 / step 8
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset (scratch home b2-1)
Command:     append `MY LOCAL TWEAK` after the marker in the rendered `commands/root-kit:hello.md`, then `ocm update root-mp-a`
Expected:    plan step 8 verifies the revert happens; a revert of user bytes warrants a warning (content control is the marker's contract, but the output promises "nothing happened")
Actual:      `already up to date` on stdout, empty stderr, exit 0 — and the file on disk was reverted to the rendered body; the user's text is gone with no trace.
Impact:      user data loss with an explicit "nothing changed" message. The revert itself is by design; the missing warning is the defect.
Regression?: new (round 3/4 tested displacement, not post-marker edits).

## F259 — `installed root-kit@root-mp-a ()` — empty component summary   (area: CLI, severity: low)

Test:        Part B2 / step 10
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset (scratch home b2-1)
Command:     `ocm install root-kit@root-mp-a` with a pre-existing hand-written file at the rendered path
Expected:    a component summary (e.g. `1 commands`) or nothing
Actual:      `installed root-kit@root-mp-a ()` — the record snapshot is taken before components are derived, so the summary is empty on re-install.
Impact:      cosmetic; looks broken.
Regression?: new.

## F260 — install exits 0 with an "installed" headline while a component was skipped   (area: CLI, severity: low)

Test:        Part B2 / step 10
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset (scratch home b2-1)
Command:     `ocm install root-kit@root-mp-a` with hand-written `commands/root-kit:hello.md` present, no `--force`
Expected:    the skip is only a stderr warning; success headline + exit 0 implies the plugin is fully installed
Actual:      exit 0, stdout `installed root-kit@root-mp-a ()`, stderr `warning: commands/root-kit:hello.md exists and is not managed by ocm … skipped it; re-run with --force to displace it`; the hand-written file stayed. The plugin is NOT fully installed (its command is missing) yet the run reports success.
Impact:      a script checking the exit code believes the install succeeded; the user only finds out by reading stderr.
Regression?: new; F107-class family (success-shaped output for a partial operation).

## F261 — displacement fact printed on both stdout and stderr   (area: CLI, severity: low)

Test:        Part B2 / step 10
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset (scratch home b2-1)
Command:     `ocm install root-kit@root-mp-a --force` with the hand-written file present
Expected:    one report of the displacement (stderr per house convention: warnings to stderr, data to stdout)
Actual:      `displaced your commands/root-kit:hello.md → …/displaced/<ts>/…` appears both in the stdout report and as a stderr warning (`queueWarning` emits the same fact).
Impact:      cosmetic/duplicated; stdout consumers and terminal readers see it twice.
Regression?: F107-class; specific duplication not previously filed.

## F262 — ocm marker comment is sent to the model in every rendered-command invocation   (area: loader, severity: low)

Test:        Part B2 / step 11
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset (scratch home b2-2)
Command:     `opencode run --command root-kit:hello --format json`; read the stored user message from `opencode.db` (`part` table)
Expected:    the marker (`<!-- ocm: rendered from … @ <rev> -->`) is bookkeeping for update/revert logic
Actual:      the user message (model input) ends with the marker verbatim: `Say hello v3: hello-from-root-kit-script\n\n<!-- ocm: rendered from plugins/root-kit/commands/hello.md @ 694e071… -->`
Impact:      every invocation leaks the marketplace's source path layout and git rev into the prompt, and adds noise. Benign but inherent to the current design (marker lives in the file body).
Regression?: inherent since rendered commands shipped; first direct evidence (round 4 verified the marker exists, not that it reaches the model).

## F263 — rendered skill marker reads `@ unknown` for local marketplaces   (area: loader, severity: low)

Test:        Part B4 / step 16
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset (scratch home b4-1)
Command:     `cat` the rendered `roots/default-*/links/many-mp/skills/many-kit--skill-07/SKILL.md` after an update
Expected:    marker names a revision, as the command markers do (`@ 694e071…`)
Actual:      `<!-- ocm: rendered from plugins/many-kit/skills/skill-07/SKILL.md @ unknown -->` — local (non-git) marketplaces have no rev, so the marker says `unknown`
Impact:      cosmetic; slightly weakens the marker's provenance story and (per F262) puts the word `unknown` in prompts for local marketplaces.
Regression?: new.

---

## Invariants (all scenario homes)

| Home | .claude/.agents | broken links | registry JSON | doctor | opencode.json |
|---|---|---|---|---|---|
| b2-1 (post step 10) | none | none | valid | exit 0 | valid |
| b2-2 (step 11) | none | none | valid | exit 0 | valid |
| b3-1 (post step 13) | none | none | valid | exit 0 | — |
| b3-2 (post step 14) | none | none | valid | exit 0 | — |
| b4-1 (post step 17) | none | none | valid | exit 0 | valid |

Hand-written files survived every operation incl. `ocm remove` (step 10: bytes returned verbatim after displacement restore). Displaced copies preserved under `roots/*/displaced/`. `opencode debug config` / `debug skill` (run from a neutral cwd, `/tmp`) always matched disk exactly.

## Not run

- Nothing from my assigned parts. TUI palette was driven via filtered typing (not Enter-invoke; round 4 known limitation), command execution verified via `opencode run --command` instead — equivalent user-side proof.
- One methodology note: b3-1's first step-12 expect run hit the 120s tool timeout mid-session; the opencode process was killed, and the step was re-run cleanly (log + verification above are from the successful run).

## Disclosure

- I ran bare `ocm --version` (real home) twice and bare `opencode --version` once early in the round. The second `ocm --version` refreshed the real home's stale auto-sync loader (0.6.1 → 0.7.0) — filed as F256 above. No other invocation touched the real home; `~/.config/opencode/registry.json`, `opencode.json`, `tui.json` were not modified (mtimes predate the round).
