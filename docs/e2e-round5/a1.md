# Round 5 — agent a1 report: Part A1 (steps 1–6) and A2 (steps 7–10)

ocm: **0.7.0** (`/tmp/ocm-e2e5/v070/node_modules/.bin/ocm`, confirmed before every
scenario), opencode: **1.18.32** (real binary), git 2.50.1, macOS APFS.
Scratch homes: `/tmp/ocm-e2e5/homes/a1-*`. Fixture copies mutated under
`/tmp/ocm-e2e5/work-a1/`. All ocm invocations ran with `HOME=$H` (helper
`run`/`runx` capture stdout/stderr separately + exit code). One exception,
disclosed below.

## Verdict summary

| Step | Finding(s) | Verdict |
|---|---|---|
| 1 — every verb's report matches disk | F208, F211 (new, low) | pass with two wording defects |
| 2 — skill without frontmatter `name` | F76, F104 | **closed** (residue: F211) |
| 3 — plugin name > 64 chars | F69 | **closed** |
| 4 — restart notice created/removed/no-op | F96, F116, F102 | **closed** (residue: F209) |
| 5 — install twice / repair hand-deleted links | F117 | **closed** |
| 6 — `update <plugin>@<mp>` gone; bare name | F110 | **closed** |
| 7 — two config roots, same marketplace | F187 | **closed** (residue: F210) |
| 8 — `doctor --fix` cross-root + true `fixed` lines | F168 | **closed** (residue: F210) |
| 9 — `remove` under root A vs root B | F189 | **closed** |
| 10 — stranded reporting by list/search/info; relative XDG at add | F167, F186 | **closed** |

New findings: **F208–F211** (1 med, 3 low). No re-files of Part D.

---

## Runner incident (disclosed — real home touched, restored)

At session start I ran two `--version` checks **without** `HOME=$H`:

```
$ /tmp/ocm-e2e5/v070/node_modules/.bin/ocm --version
0.7.0
$ /tmp/ocm-e2e5/v061/node_modules/.bin/ocm --version
refreshed the auto-sync loader to 0.6.1 (restart opencode to activate)
0.6.1
```

The 0.6.1 binary's `--version` **wrote the loader** in the real
`~/.config/opencode`: it downgraded 32 loader modules from 0.7.0 stamps to
0.6.1 (mtimes 21:02), leaving the four 0.7.0-only modules
(`defect.js`, `digest.js`, `gate.js`, `renames.js`) stale at 0.7.0. This is
exactly the known, triaged-as-unfixable F118 shape (an older published
binary reverts the loader; round-4 status note) — not re-filed.

Restoration: ran the user's own global ocm (npm 0.7.0) `ocm init` →
`auto-sync loader already current`, exit 0. Verified afterwards: all 36
modules under `~/.config/opencode/ocm/` stamped `// ocm-version: 0.7.0`,
zero 0.6.1 stamps; `registry.json` (mtime 20:34, pre-session),
`opencode.json`, `tui.json`, `commands/`, `agents/` untouched. No other
bare-`ocm` invocations occurred; every later invocation used `HOME=$H`.

Side observation (not a finding — 0.6.1 behaviour, F118 family): `ocm
--version` on 0.6.1 mutates the loader. On 0.7.0, `--version` printed only
`0.7.0` with no loader message in every scratch home.

---

# Part A1 — outcome-derived reports (brief 31)

## Step 1 — every verb's report matches disk

Home `a1-1`. Full lifecycle: git add (gh-alpha), local add (demo-marketplace),
uninstall, install, trust, untrust, update (changed + unchanged), remove
(local + git). Every claim checked against registry JSON, `readlink`, cache
layout, `opencode.json`, and `opencode debug config` / `debug skill` after a
forced TUI sync (sized pty, expect).

### add (git)

```
$ ocm add https://github.com/wntic/ocm-e2e-alpha
exit=0
--stdout--
cloning https://github.com/wntic/ocm-e2e-alpha...
added marketplace "gh-alpha"
  fresh-gh (1 commands)
  hello-kit (1 commands, 1 skills)
  tool-kit (1 commands)
commands and agents are available as /<plugin>:<name> in every project
installed auto-sync loader (/tmp/ocm-e2e5/homes/a1-1/.config/opencode/plugins/ocm-loader.js)
installed TUI plugin (/ocm in the opencode TUI)
restart opencode to activate
--stderr--
```

Disk: registry holds `gh-alpha` with `dir` =
`$H/.cache/ocm/roots/default-d17faa/marketplaces/gh-alpha`, 3 plugins,
`enabled: true`, revision recorded; commands
`fresh-gh:fresh.md`, `hello-kit:greet.md`, `tool-kit:greet.md` are symlinks
into the clone (readlink verified); skill mirror
`links/gh-alpha/skills/hello-kit--hello-guide/SKILL.md` is a rendered file
(`name: "hello-kit:hello-guide"` + `ocm: rendered from … @ <sha>` marker);
`opencode.json` `skills.paths` points at the links dir; `tui.json` holds
`./ocm/ui.js`. After a TUI sync, `opencode debug config` serves
`fresh-gh:fresh`, `hello-kit:greet`, `tool-kit:greet`; `debug skill` serves
`hello-kit:hello-guide`. **Report matched disk.**

### add (local, untrusted default) — trust surface

```
$ ocm add /tmp/ocm-e2e5/work-a1/local-mp
exit=0
--stdout--
marketplace "demo-marketplace" ships code that opencode will execute:
  mcp     demo-kit/everything (local server: npx -y @modelcontextprotocol/server-everything)
  plugin  demo-kit/notify (plugins/demo-kit/plugin/notify.js)
this code runs with your shell's permissions on every opencode start.
review it at /private/tmp/ocm-e2e5/work-a1/local-mp
added marketplace "demo-marketplace"
  demo-kit (1 agents, 1 commands, 1 skills, 1 plugins, 1 mcp servers)
  release-kit (1 commands, 1 skills)
commands and agents are available as /<plugin>:<name> in every project
restart opencode to activate
--stderr--
trust this marketplace to run code? [y/N/skip]
  warning: blocked (untrusted): demo-kit:notify.js not linked — run `ocm trust demo-marketplace` to approve
  warning: blocked (untrusted): demo-kit:mcp/everything not installed — run `ocm trust demo-marketplace` to approve
  warning: 2 components blocked pending trust — run `ocm trust demo-marketplace`
```

Disk: no `ocm--*` mcp key, no plugin link — exactly what the warnings say.
Links point directly at the local source dir (no clone). **Matched.**

### trust

```
$ ocm trust demo-marketplace --yes
exit=0
--stdout--
marketplace "demo-marketplace" ships code that opencode will execute:
  ...
review it at /private/tmp/ocm-e2e5/work-a1/local-mp
marketplace "demo-marketplace" trusted to run code
restart opencode to activate
--stderr--
```

Disk: `plugins/ocm--demo-kit--notify.js` → source `notify.js` (readlink
verified); `opencode.json` gains `mcp.ocm--demo-kit--everything` with the
exact command array; registry `trust.code: "granted"`. **Matched.**

### untrust

```
$ ocm untrust demo-marketplace
exit=0
--stdout--
marketplace "demo-marketplace" no longer trusted; executable components removed
restart opencode to activate
--stderr--
  warning: 2 components blocked pending trust — run `ocm trust demo-marketplace`
```

Disk: plugin link gone, mcp key gone, `trust.code: "denied"`,
`skills.paths` intact. **Matched** (but see F209 for the no-op variant).

### uninstall / install

```
$ ocm uninstall demo-kit
exit=0
--stdout--
uninstalled demo-kit@demo-marketplace
restart opencode to activate
--stderr--
$ ocm install demo-kit
exit=0
--stdout--
installed demo-kit@demo-marketplace ()
restart opencode to activate
--stderr--
  warning: 2 components blocked pending trust — run `ocm trust demo-marketplace`
```

Disk after uninstall: `demo-kit:tdd.md`, `demo-kit:reviewer.md`,
`demo-kit--code-review` all gone, `enabled: false` — matched. After install:
all three back, `enabled: true` — matched, **except the headline**
`installed demo-kit@demo-marketplace ()` — empty parens (F208). A fresh
install (never auto-installed, explicit mode, home a1-12) prints
`installed solo-kit@demo-marketplace (1 skill)` — the empty summary is
specific to reinstall-after-uninstall.

### update (local, changed and unchanged)

```
$ ocm update demo-marketplace            # after editing commands/ship.md upstream
exit=0
--stdout--
updating demo-marketplace...
  release-kit
    ~ commands/ship.md
--stderr--
  warning: 2 components blocked pending trust — run `ocm trust demo-marketplace"

$ ocm update demo-marketplace            # unchanged
exit=0
--stdout--
updating demo-marketplace...
  already up to date
--stderr-- (same blocked-trust warning)

$ ocm update demo-marketplace            # after editing the skill upstream
exit=0
--stdout--
updating demo-marketplace...
  release-kit
    ~ skills/release-notes
  restart opencode to activate
--stderr-- (same blocked-trust warning)
```

Disk: skill mirror regenerated with the new body (verified by reading the
rendered file); notice present exactly when a file was rewritten, absent on
the no-op. Git update (`update root-mp` on an unchanged clone, a1-7 root B):
`already up to date`, exit 0. **Matched.** (Observations, not findings: the
rendered marker reads `@ unknown` for local sources — true but ugly; the
blocked-trust warning also fires on *refused* update commands, e.g.
`update demo-kit@demo-marketplace` — true fact, misplaced noise.)

### remove (local + git)

```
$ ocm remove demo-marketplace
exit=0
--stdout--
removed marketplace "demo-marketplace"
  demo-kit: 1 agents, 1 commands, 1 skills removed
  release-kit: 1 commands, 1 skills removed
restart opencode to activate
--stderr--
$ ocm remove gh-alpha
exit=0
--stdout--
removed marketplace "gh-alpha"
  fresh-gh: 1 commands removed
  hello-kit: 1 commands, 1 skills removed
  tool-kit: 1 commands removed
restart opencode to activate
--stderr--
```

Disk: registry records gone; all listed links/mirrors gone; `skills.paths`
entries removed (key dropped entirely when empty); gh-alpha's clone
directory gone (`roots/default-d17faa/marketplaces/` empty, `links/`
empty); commands/agents dirs empty; no broken symlinks anywhere under `$H`.
**Matched.**

Invariants on a1-1 after the lifecycle: no `.claude`/`.agents` writes, no
broken links, registry + config valid JSON, `ocm doctor` exit 0
(`cache default-d17faa`), tui.json intact.

**Verdict: pass, with F208 and F211.**

## Step 2 — skill whose SKILL.md lacks `name` (F76, F104)

Fixture: copy of local-mp with `name: code-review` stripped from
`plugins/demo-kit/skills/code-review/SKILL.md`. Home `a1-2`.

```
$ ocm add /tmp/ocm-e2e5/work-a1/noname-mp
exit=0
--stdout--
added marketplace "demo-marketplace"
  demo-kit (1 agents, 1 commands, 1 skills, 1 plugins, 1 mcp servers)
  release-kit (1 commands, 1 skills)
...
--stderr--
trust this marketplace to run code? [y/N/skip]
  warning: skipped /private/tmp/ocm-e2e5/work-a1/noname-mp/plugins/demo-kit/skills/code-review/SKILL.md: no name in frontmatter
  ...
```

Disk: registry `demo-kit.components` = `{command: [tdd.md], agent:
[reviewer.md]}` — **no skill**; no `demo-kit--code-review` mirror in the
links tree; `ocm list` shows demo-kit with `agents: reviewer.md,
commands: tdd.md` and no skills line; `opencode debug skill` (neutral cwd)
serves only `release-kit:release-notes`. The stderr warning names the file
and the reason. **F76/F104 closed.** Residue: the stdout headline still
counts `1 skills` for demo-kit — filed as F211.

## Step 3 — plugin name > 64 chars (F69)

Fixture: copy of local-mp with `demo-kit` renamed to a 70-char name
(35×`a` + `-` + 34×`b`) in dir, plugin.json and marketplace.json.

At `add` (fresh marketplace), the whole add is refused up front:

```
$ ocm add /tmp/ocm-e2e5/work-a1/longname-mp
exit=1
--stdout-- (empty)
--stderr--
plugin "aaaa…bbbb" exceeds 64 chars (70)
  rename it in the marketplace, then re-run ocm add
```

The F69 scenario proper — the limit hit at *update* (home `a1-3b`, valid
short name added first, then renamed upstream):

```
$ ocm update demo-marketplace
exit=0
--stdout--
updating demo-marketplace...
  demo-kit removed (no longer in the marketplace)
  restart opencode to activate
--stderr--
  warning: plugin "aaaa…bbbb" exceeds 64 chars (70)
  warning: 2 components blocked pending trust — run `ocm trust demo-marketplace`
```

Disk: demo-kit's links gone; **no link, mirror or registry record anywhere
for the 70-char name** (`find $H -name '*aaaa*'` empty); registry plugins =
`release-kit` only; no broken symlinks; `opencode debug config` serves only
`release-kit:ship`. **F69 closed.**

## Step 4 — restart notice: created/removed yes, true no-op no (F96, F116, F102)

Notice **present** (all verified against disk in step 1 / step 2 homes):

- `add` (git, local, skill-only): `restart opencode to activate` ✓
- `install` (fresh and reinstall): ✓
- `uninstall`: ✓
- `update` with a created/removed/refreshed component: ✓
- `remove` (both marketplaces): ✓ — F116's case
- `trust` (plugin link + mcp key created): ✓
- `untrust` with an mcp key + plugin link removed: ✓ — F96's MCP-key case
- skill-only install (`solo-kit`, home a1-4): ✓ — F102's case

Notice **absent** on true no-ops (home a1-4/a1-5):

```
$ ocm install solo-kit          → already installed solo-kit@demo-marketplace        (no notice)
$ ocm add <same path>           → exit 1, already-added refusal                      (no notice)
$ ocm update demo-marketplace   → already up to date                                 (no notice)
$ ocm uninstall solo-kit (2nd)  → solo-kit@demo-marketplace not installed            (no notice)
$ ocm untrust demo-marketplace (never trusted, no executable components)
exit=0
--stdout--
marketplace "demo-marketplace" no longer trusted; executable components removed
--stderr-- (empty)
```

The notice is correctly absent everywhere it should be — **F96/F116/F102
closed** — but the never-trusted `untrust` prints "executable components
removed" when nothing existed to remove: F209.

## Step 5 — install twice; hand-deleted links repaired (F117)

```
$ ocm install demo-kit                 # second run, home a1-4/a1-5
already installed solo-kit@demo-marketplace     (no notice, exit 0)

# links deleted by hand (command, agent, skill mirror), then:
$ ocm install demo-kit
exit=0
--stdout--
repaired 3 links for demo-kit@demo-marketplace
restart opencode to activate
--stderr--
  warning: 2 components blocked pending trust — run `ocm trust demo-marketplace`
```

Disk: all three deleted artifacts re-created (readlink + rendered SKILL.md
verified). **F117 closed.**

## Step 6 — `update <plugin>@<mp>` gone; bare plugin name resolves (F110)

```
$ ocm update demo-kit@demo-marketplace
exit=1
--stderr--
ocm update takes a marketplace, not a plugin — run `ocm update demo-marketplace"

$ ocm update demo-kit
exit=1
--stderr--
"demo-kit" is a plugin in marketplace "demo-marketplace" — run `ocm update demo-marketplace"

$ ocm update nosuch-kit
exit=1
--stderr--
marketplace "nosuch-kit" not found (ocm list)
```

**F110 closed** — the plugin@mp form is refused with the next action, and a
bare plugin name names its marketplace.

---

# Part A2 — per-root cache (brief 38)

## Step 7 — two config roots, same marketplace (F187)

Home `a1-7`. Root A = plain `$H`; root B = `HOME=$H XDG_CONFIG_HOME=$H/xdg`.
Same git URL and same `--name` added to both:

```
$ HOME=$H ocm add file:///tmp/ocm-e2e5/work-a1/root-mp --name root-mp
exit=0  (cloning…, added marketplace "root-mp", plain-kit (1 commands),
         root-kit (2 commands), loader + TUI + restart notice)

$ HOME=$H XDG_CONFIG_HOME=$H/xdg ocm add file:///…/root-mp --name root-mp
exit=0
--stdout-- (same shape, loader path …/a1-7/xdg/opencode/plugins/ocm-loader.js)
--stderr--
warning: an ocm install is stranded in another config root
  installed at: /tmp/ocm-e2e5/homes/a1-7/.config/opencode (1 marketplace)
  this shell:   /tmp/ocm-e2e5/homes/a1-7/xdg/opencode (XDG_CONFIG_HOME is set)
```

No `ENOTEMPTY`, no crash, no stray clone. Cache layout:

```
$ ls $H/.cache/ocm/roots/
default-ca9643/    xdg-686b38/
…/default-ca9643/marketplaces/root-mp/     (root A's own clone)
…/xdg-686b38/marketplaces/root-mp/         (root B's own clone)
$ cat $H/.cache/ocm/roots.json
{"roots": ["…/.config/opencode", "…/xdg/opencode"]}
```

Registry `dir` fields point into each root's own slug. Isolation: after
adding `demo-marketplace` to root A only, root A's `ocm list` shows
`root-mp` + `demo-marketplace`, root B's shows only `root-mp`. Links: root
A's point into `default-ca9643`, root B's into `xdg-686b38`; each root's
rendered `root-kit:hello.md` substitutes **its own** root path. The
stranded warning on root B's `add` is correct at that moment (root B had no
registry yet) and does **not** repeat on later root-B commands once root B
is populated (`list` clean). **F187 closed.**

## Step 8 — `doctor --fix` under root A vs root B (F168)

Root B snapshotted (every file md5 + every symlink target, 84→86 lines).
Broke root A by hand (deleted `plain-kit:quiet.md` link and the
`demo-kit--code-review` skill mirror):

```
$ HOME=$H ocm doctor
exit=1
  error   marketplace "root-mp": 1 materialized component(s) missing (ocm update)
  error   marketplace "demo-marketplace": 1 materialized component(s) missing (ocm update)
  error   marketplace "root-mp": plugin "plain-kit" records command "quiet.md" with no materialization — stale record (ocm update root-mp)
  error   marketplace "demo-marketplace": plugin "demo-kit" records skill "code-review" with no materialization — stale record (ocm update demo-marketplace)
  (+ the stranded error — see F210)

$ HOME=$H ocm doctor --fix
exit=1   (only the F210 stranded error remains)
  fixed   marketplace "root-mp": re-materialized 1 component(s) (restart opencode to activate)
  fixed   marketplace "demo-marketplace": re-materialized 1 component(s) (restart opencode to activate)
```

Both `fixed` lines are true: the link and the skill mirror are back on disk
(verified). Root B's snapshot is byte-identical before/after — links,
clones, skills, `opencode.json` untouched, including after adding a
skill-bearing marketplace to root B and re-running `doctor --fix` under
root A (idempotent: no false `fixed` lines on the healthy run). **F168
closed** — both halves (no cross-root harm; every `fixed` line a real
write). Residue: doctor's stranded *error* on a healthy dual-root install —
F210.

## Step 9 — `remove` under root A; root B still resolves (F189)

```
$ HOME=$H ocm remove root-mp
exit=0
removed marketplace "root-mp"
  plain-kit: 1 commands removed
  root-kit: 2 commands removed
restart opencode to activate
```

Disk: root A's registry, links and clone gone. Root B: registry still holds
`root-mp` + `demo-marketplace`, clone and links intact. Forced a loader
sync with one sized-pty TUI session under root B (`HOME=$H
XDG_CONFIG_HOME=$H/xdg`), then:

```
$ HOME=$H XDG_CONFIG_HOME=$H/xdg opencode debug config   (neutral cwd)
.command keys: [ "plain-kit:quiet", "root-kit:hello", "root-kit:plain" ]
$ HOME=$H XDG_CONFIG_HOME=$H/xdg opencode debug skill
solo-kit:only-skill
```

**F189 closed** — root B resolves every component in opencode after root
A's remove.

## Step 10 — stranded install reported by list/search/info (F167); relative XDG warns at add (F186)

Home `a1-10`: marketplace added under the default root, then read-only
commands with `XDG_CONFIG_HOME` pointing at an empty root:

```
$ HOME=$H XDG_CONFIG_HOME=$H/xdg ocm list
exit=0
--stdout-- (empty)
--stderr--
warning: an ocm install is stranded in another config root
  installed at: /tmp/ocm-e2e5/homes/a1-10/.config/opencode (1 marketplace)
  this shell:   /tmp/ocm-e2e5/homes/a1-10/xdg/opencode (XDG_CONFIG_HOME is set)

$ … ocm search tdd
exit=1
--stderr-- (same warning) + no matches for "tdd"

$ … ocm info demo-kit
exit=1
--stderr-- (same warning) + plugin "demo-kit" not found in any marketplace (ocm search <query>, or ocm update)
```

No "no marketplaces added yet" anywhere. (`list`'s empty stdout is by
design — list.ts: "the stranded notice already explained why the home looks
empty".) **F167 closed.**

Relative XDG at the write boundary (home `a1-11`, cwd = work dir):

```
$ HOME=$H XDG_CONFIG_HOME=relconf ocm add …/skillonly-mp
exit=0
--stdout--
added marketplace "demo-marketplace"
  solo-kit (1 skills)
…
--stderr--
warning: XDG_CONFIG_HOME is relative — every ocm command run from a different directory sees a different install; set it to an absolute path
```

The registry lands cwd-relative (`$W/relconf/opencode/ocm/registry.json`,
matching opencode), and `doctor` from a different cwd shows both the
relative warning and the resulting stranding. **F186 closed.**

---

# Findings

## F208 — `ocm install` prints an empty component summary `()` on reinstall-after-uninstall   (area: CLI, severity: low)

Test:        A1 / step 1
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset
Command:     `ocm uninstall demo-kit` then `ocm install demo-kit`
Expected:    brief 31 — reports derived from outcomes; the install headline
             states the plugin's components
Actual:      `installed demo-kit@demo-marketplace ()` — exit 0. The plugin
             has 3 components and all 3 were materialized. Cause:
             `setEnabled` (loader/mutations.js) captures
             `const components = record.components` as the *pre-derive*
             snapshot; the preceding uninstall's `deriveComponents` emptied
             the record, so the reinstall headline prints the stale empty
             set. A fresh install prints correctly
             (`installed solo-kit@demo-marketplace (1 skill)`, home a1-12).
Impact:      a user re-enabling a plugin reads `()` as "no components";
             the line under-reports what was just created.
Regression?: new defect — brief 31's outcome-derived rewrite; the
             reinstall-after-uninstall path was not covered.

## F209 — `ocm untrust` on a marketplace with nothing to remove still says "executable components removed"   (area: CLI, severity: low)

Test:        A1 / step 4
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset
Command:     `ocm untrust demo-marketplace` on a never-trusted, skill-only
             marketplace (home a1-4)
Expected:    a true no-op states it is a no-op (the restart notice
             correctly stays absent — F96's fix); the success line should
             be outcome-derived like the rest of brief 31
Actual:      exit 0, stdout `marketplace "demo-marketplace" no longer
             trusted; executable components removed` — nothing was
             removed (trust was `none`, the plugin ships no executable
             components). The line is unconditional
             (src/commands/trust.ts:167).
Impact:      a false claim in exactly the place F96/F116 fixed the notice;
             a user cannot tell a no-op from a real revocation.
Regression?: new defect — adjacent to the F96 fix, same verb, uncovered
             path.

## F210 — `ocm doctor` reports a healthy dual-root install as an error and exits 1, with a false remedy   (area: CLI, severity: med)

Test:        A2 / steps 7–8
ocm version: 0.7.0      opencode: 1.18.32
XDG:         two fully-populated roots (root A default, root B XDG), same
             marketplace installed in both, per-root caches
Command:     `HOME=$H XDG_CONFIG_HOME=$H/xdg ocm doctor` (and the mirror
             direction under root A)
Expected:    brief 38 §1 made dual-root a supported, isolated
             configuration (per-root slugs); brief 38 §2.3's own comment in
             src/stranded.ts says "two live installs are a user decision,
             not a stranding" — doctor should apply the same predicate it
             gave every other command
Actual:      exit 1, `error an ocm install is stranded in another config
             root … unset XDG_CONFIG_HOME to use the existing install, or
             re-add those marketplaces here` — under a root that holds a
             complete, working install of its own. The remedy is false
             (those marketplaces *are* added here), and the error fires in
             both directions. Doctor's stranded check
             (src/commands/doctor.ts:27–41) pushes every
             `strandedRoots()` result unconditionally;
             `reportStrandedNotice()` (stranded.ts:89–95) correctly checks
             `existsSync(OCM_REGISTRY_FILE)` first.
Impact:      the one user population brief 38 exists for — two config
             roots on one HOME — has a permanently red doctor telling them
             to redo something they already did. Doctor exit 1 stops
             meaning "unhealthy".
Regression?: new defect — could not arise before 0.7.0 made dual-root
             viable (F187's fix). F168's cross-root harm is gone; this is
             the residue on the reporting side.

## F211 — the `add` headline counts skipped and trust-blocked components as shipped, always plural   (area: CLI, severity: low)

Test:        A1 / steps 1–2
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset
Command:     `ocm add …/noname-mp` (skill missing frontmatter `name`)
Expected:    brief 31 — the report states outcomes; F76/F104's fix says the
             report reports the skill as skipped, not installed
Actual:      stdout `demo-kit (1 agents, 1 commands, 1 skills, 1 plugins,
             1 mcp servers)` while stderr says the skill was skipped and
             disk holds no skill (registry, links, `debug skill` all
             correct). The same headline also prints "1 agents, 1
             commands, 1 skills" — the add-side summary (src/commands/
             plugins.ts:116 → `scan`'s renderer shares it) never
             singularizes, unlike install's "1 skill". Two residues of the
             pre-brief-31 discovery summary surviving inside the
             outcome-derived report.
Impact:      the stdout summary and the stderr warnings contradict each
             other in the same run; a user reading only stdout believes a
             skill installed.
Regression?: new defect — F76/F104 closed the registry/list/opencode
             surfaces; the add headline was not rewritten.

---

# Invariants

Held in every home of every scenario (a1-1 … a1-12, both roots):

- no writes under `.claude`, `~/.claude`, `.agents`, `~/.agents` (checked
  per home; zero findings)
- no broken ocm symlinks (`find $H -type l ! -exec test -e {} \;` empty,
  including after the F69 rename-at-update and both removes)
- registry / opencode.json / tui.json valid JSON wherever present
- success lines on stdout, warnings/errors on stderr; exit codes 0/1 as
  specified (130 not exercised — no SIGINT scenario in my steps)
- `ocm doctor` exit 0 on every healthy single-root home; the only red
  doctors are the deliberately broken one (step 8, repaired by `--fix`)
  and the F210 dual-root error
- cross-checks against opencode itself: `debug config` and `debug skill`
  agreed with disk in all four homes where they were run (a1-1, a1-2,
  a1-7 root B, and the negative case a1-3b)

# Not run / delegated

- **Interactive trust prompt (`y`/`N`/`skip`) in a real TTY** — every trust
  decision in my steps was either the piped-stdin default (N), `--no-trust`
  or `--yes`. Same delegation as round 4's C-item; the non-TTY default
  path and the `--yes` path are verified.
- **TUI quit keystroke**: my expect sessions quit cleanly via ctrl+c on
  a1-1; the a1-7 session ignored the first ctrl+c and was killed by
  expect's exit (no stray process remained — verified). The loader sync
  completed in both cases (8 s dwell).
- No fold/case-sensitivity scenarios in my steps (A5's territory).

# Observations (not filed)

- `update` prints the blocked-trust warning even on refused commands
  (`update demo-kit@demo-marketplace`, `update nosuch-kit`) — true fact,
  misplaced noise.
- Rendered-skill marker reads `@ unknown` for local marketplaces (git
  sources get the SHA) — true but ugly.
- Root B's `add`-time stranded warning (before its registry exists) is
  correct per the brief-38 §2.3 predicate and does not repeat afterwards.
