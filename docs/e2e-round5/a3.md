# Round 5 — agent a3 report: Part A6 (steps 20–23) and A7 (steps 24–29)

ocm: **0.7.0** (`/tmp/ocm-e2e5/v070/node_modules/.bin/ocm`, confirmed before every
scenario), reference 0.6.1 (`/tmp/ocm-e2e5/v061/node_modules/.bin/ocm`),
opencode: **1.18.32** (real binary). Scratch homes: `/tmp/ocm-e2e5/homes/a3-*`.
Fixture copies mutated under `/tmp/ocm-e2e5/work-a3/` (root-a, root-b, lock-mp,
adw-mp, ocm-defect — a private copy of the published package used only in
step 29; the shared `/tmp/ocm-e2e5/v070` package was never modified). All ocm
invocations ran with `HOME=$H` except one pair of `--version` calls, disclosed
below.

## Verdict summary

| Step | Finding(s) | Verdict |
|---|---|---|
| 20 — plugin-root substitution on disk | F84 (round 4) | **closed** |
| 21 — substituted command runs in a real session | F84 | **closed** (note for B2-11) |
| 22 — two marketplaces, per-root substitution | — | **pass** (adapted; spec-18 collision verified) |
| 23 — author drops the variable upstream | — | **pass** |
| 24 — lock release ownership | F233 (new) | **pass** (aftermath defect filed) |
| 25 — TUI mutation takes the lock | — | **closed** |
| 26 — stranded install without breadcrumb | — | **closed** |
| 27 — `$schema` is not a server | — | **closed** |
| 28 — `doctor --fix` key precision | — | **closed** |
| 29 — a defect is visible | — | **closed** (with placement findings) |

New findings: **F232, F233** (1 med, 1 low). No re-files of Part D.

---

## Runner incident (disclosed — real home touched, no lasting effect)

At ~21:04:28, while checking binaries, I ran both `ocm --version` commands
**without** `HOME` overrides, against the real `~/.config/opencode`:
`/tmp/ocm-e2e5/v070/.../ocm --version` (printed `0.7.0` only) and
`/tmp/ocm-e2e5/v061/.../ocm --version`, which printed

```
refreshed the auto-sync loader to 0.6.1 (restart opencode to activate)
```

Forensics at the time: the 33 shared loader files carried mtime 21:04:28.28
but **published 0.7.0 content**; the 4 v070-only files (defect.js, digest.js,
gate.js, renames.js) were untouched since 20:58:45; registry (20:34:34) and
tui.json untouched; no lock left. The restoration could not be explained then.
It is explained now (see F232): each binary's `--version` rewrites the shared
loader files to its own version — the two calls ping-ponged the files and the
**last one run (0.7.0) won**, leaving no divergence. Verified byte-identical to
published 0.7.0 by diff. The mechanism, and a clean scratch reproduction, are
F232 below. Registry, tui.json, commands/ and agents/ were never touched by
either call.

---

## A6 — plugin-root substitution (brief 41)

### Step 20 — CLOSED (substitution on disk; home a3-1, work copy `root-a`)

`ocm add file://$W/root-a --name root-one` (exit 0). On disk afterwards:

- `commands/root-kit:hello.md` is a **regular rendered file** whose `!` block
  names the substituted path, with the provenance marker:

```
Say hello: !`"/tmp/ocm-e2e5/homes/a3-1/.cache/ocm/roots/default-5cb7bd/marketplaces/root-one/plugins/root-kit/scripts/hello.sh"`
<!-- ocm: rendered from plugins/root-kit/commands/hello.md @ de29ae739675b4c403f7d3841c30f4fcfe119e0d -->
```

- `root-kit:plain.md` and `plain-kit:quiet.md` are **symlinks** into the clone
  (no variable → linked, not rendered).
- `opencode debug config` resolves all three commands; hello's template shows
  the substituted absolute path. Registry records both plugins enabled.

### Step 21 — CLOSED (the command runs in a real session; home a3-1)

Python pty driver (30×100, `run-hello.py`, evidence
`work-a3/hello-session3.log`): `/root-kit:hello` selected from the palette and
submitted; the session renders

```
┃  Say hello: hello-from-root-kit-script
┃  <!-- ocm: rendered from plugins/root-kit/commands/hello.md @
┃  de29ae739675b4c403f7d3841c30f4fcfe119e0d -->
```

The `!` block executed the script at the substituted path (its stdout appears
in the reply). **Note for B2-11**: the rendered provenance marker comment is
part of the template opencode sends to the model — visible verbatim in the
session transcript.

### Step 22 — PASS, adapted (two marketplaces; home a3-2, copies root-a/root-b)

The plan's literal setup — a second marketplace providing the same plugin
names — is **refused at add time**, correctly per spec 18 (`incumbentMarketplace`,
`loader/marketplace.js:123`):

```
plugin "plain-kit" is already provided by marketplace "root-one"
  plain-kit:quiet.md from marketplace "root-one" conflicts with plain-kit:quiet.md from marketplace "root-two"
plugin "root-kit" is already provided by marketplace "root-one"
  ...
not adding "root-two"; remove one, or ask its author to rename
```

(exit 1; nothing cloned into the registry, no partial state.) The only way to
reach "each rendered body names its own root" is distinct plugin names, so
copy 2's plugins were renamed (root-kit→alt-kit, plain-kit→alt-plain, committed
in root-b) and re-added. Both rendered bodies then name **their own**
marketplace root:

```
root-one: root-kit:hello.md  -> !`".../roots/default-5cbf8c/marketplaces/root-one/plugins/root-kit/scripts/hello.sh"`
root-two: alt-kit:hello.md   -> !`".../roots/default-5cbf8c/marketplaces/root-two/plugins/alt-kit/scripts/hello.sh"`
```

Cross-check: neither body mentions the other marketplace (grep 0/0). Registry
holds both marketplaces with distinct dirs; no broken symlinks; one slug root.

### Step 23 — PASS (author drops the variable upstream; home a3-1)

`hello.md` edited in `root-a` to remove `${OCM_PLUGIN_ROOT}` (commit a1c96cd),
then `ocm update` (exit 0):

```
updating root-one...
  de29ae7 → a1c96cd
  root-kit
    ~ commands/hello.md
  restart opencode to activate
```

Afterwards `root-kit:hello.md` is a **symlink** into the clone (content = the
new plain body); no "not managed by ocm" or "skipped" anywhere in either
stream; the displaced cache is empty; the registry still records hello.md as a
root-kit component; `ocm doctor` exits 0. Render-to-link transition is clean
in both directions.

---

## A7 — the six review-only fixes

### Step 24 — PASS (lock release ownership; homes a3-3, a3-3b)

Setup: fresh home; a PATH git-shim (`sleep 40` on clone) holds a real
`ocm add` mid-mutation; a python driver rewrote the live lock's `at` to 11
minutes old (holder pid alive), then started a second slowed `ocm add` as the
breaker. Lock timeline (monitor sampling every 20 ms, `work-a3/step24b.py`):

```
t+0.05s  {"pid": 39285, ... "command": "ocm add"}     <- original
t+0.08s  {"pid":39288, ... "command": "ocm add"}      <- breaker (after rewrite)
```

- Breaker stderr: `warning: removed a stale ocm lock (pid 39285, 11m old) — a previous ocm was killed`
- Original stderr (exit 0, its add completed):

```
warning: this ocm's lock was broken by another process (now pid 39288, ocm add)
  its writes and ours may have interleaved — run ocm doctor
```

- The original **did not delete** the breaker's lock: the file stayed
  continuously present under the breaker's pid until the breaker itself
  released it (final state: gone). Exactly `loader/lock.js:118-129`.
- A first run without the monitor showed the original exiting *after* the
  breaker had already released and deleted the file — `readHolder()` then
  returns "pending" and release is silently a no-op. Also spec-compliant
  (nothing of the breaker's was deleted); the monitored run above demonstrates
  the required behaviour directly.

**Aftermath — the named remedy is a no-op (F233).** The interleaved writes
lost the original's marketplace: `ocm list` shows only `slow-two`, `ocm update`
says "already up to date" for it alone, yet slow-one's five command links and
cache clone remain on disk. `ocm doctor` **exits 0** and reports nothing — the
orphan sweep covers `ocm--` links and skill mirrors, not `plugin:name`
command/agent links. See F233.

### Step 25 — CLOSED (TUI mutation takes the lock; home a3-4)

`lock-mp` added `--explicit` (lock-kit "available, not installed"); a real
`ocm add` (shim, 40 s clone) held the lock as
`{"pid":40481,"at":…,"command":"ocm add"}` while a sized pty (30×100,
`run-install.py`, evidence `work-a3/s25-session.log`) drove
`/ocm → Browse plugins → lock-kit → Install → confirm`.

What the user sees, in order (ANSI-stripped from the pty stream; toasts split
across cells in the raw buffer):

1. `waiting for another ocm to finish (pid 40481, ocm add)...`
2. ~10 s later, on time (ceiling): 
   `error: another ocm is writing this installation (pid 40481, ocm add, started 25s ago)`
   with the remedy `wait for it to finish, or if it is gone, run ocm doctor`
3. No success toast; the dialog closed back to Browse; the session stayed
   usable and quit cleanly. Registry afterwards: lock-kit still not installed
   (no write underneath); the holder's add finished exit 0.

### Step 26 — CLOSED (stranded install, 0.6.1 → 0.7.0; home a3-5)

Install with 0.6.1 (exit 0), upgrade by running 0.7.0 `list` (migrated the
cache to `roots/default-633248`, refreshed the loader), then 0.7.0 `doctor`
with `XDG_CONFIG_HOME=$H/xdg` (exit 1):

```
error: an ocm install is stranded in another config root
  installed at: /tmp/ocm-e2e5/homes/a3-5/.config/opencode (1 marketplace)
  this shell:   /tmp/ocm-e2e5/homes/a3-5/xdg/opencode (XDG_CONFIG_HOME is set)
  opencode reads the second; nothing in the first is visible to it
  unset XDG_CONFIG_HOME to use the existing install, or re-add those marketplaces here
```

The XDG root stayed empty (verified by find — no mutation while reporting).

### Step 27 — CLOSED (`$schema` is not a server; home a3-6)

`ocm add /tmp/ocm-e2e5/fixtures/schema-mp --trust` (exit 0; note: the fixture
is a plain directory, not a git repo — the round-4 form with a bare path, not
`file://`). The trust listing shows only
`mcp schema-kit/real-server (local server: node -e console.log('real'))`;
`ocm list` shows `mcp: real-server`; `ocm info schema-kit` lists only
`ocm--schema-kit--real-server`; `opencode.json` contains exactly
`["ocm--schema-kit--real-server"]`. The top-level `$schema` key never appears
as a server on any surface (the word "schema" occurs only in the plugin's own
description text, which is fixture content).

### Step 28 — CLOSED (`doctor --fix` key precision; home a3-7)

`adw-mp` (plugin adw, one command) added and trusted; `opencode.json`
hand-seeded with a bad-shape key and a valid prefix-sharing sibling. Doctor,
report-only (exit 1, mutates nothing):

```
  error   ocm--adw--legacy: invalid MCP entry — plugin "adw", server "legacy" is missing "type" (expected "local" or "remote"); opencode would refuse to start (ocm doctor --fix)
1 error, 0 warnings
```

`doctor --fix` (exit 0):

```
  fixed   ocm--adw--legacy: removed from opencode.json
```

Keys after: `["ocm--adw--legacy--sub"]` — exactly the offending key removed
(`removeMcpKeysExact`), the valid sibling under the bad key's prefix kept.

### Step 29 — CLOSED (a defect is visible; home a3-9, private package copy `work-a3/ocm-defect`)

Simulating a *shipped* defect required the defect to live in the published
package itself (an installed-file edit is detected by `loaderStatus`'s
content comparison and silently healed by `refreshStaleLoader` before doctor
runs — observed, exit 0 with "refreshed the auto-sync loader to 0.7.0").
Placement findings along the way, all consistent with
`scripts/oc-probe.sh`'s own notes:

- top-level defect in `ui.js`: invisible — `debug skill` does not load TUI
  plugins; doctor silent.
- top-level defect in `ocm-loader.js`: invisible — opencode **swallows
  import-stage failures**; no log line at all.
- defect inside the `server()` hook of `ocm-loader.js`
  (`ocmDefectProbeNoSuchFunction()` as the first statement): **reported**.

`ocm doctor` (exit 1):

```
  error   timestamp=2026-09-22T16:27:24.160Z level=ERROR run=6c1c17a0 message="failed to load plugin" path=file:///var/folders/…/ocm-doctor-CR9Yci/config/plugins/ocm-loader.js error="ocmDefectProbeNoSuchFunction is not defined" (ocm update)
1 error, 0 warnings
``` The probe copied the config to a
scratch dir (real home untouched by opencode), ran
`opencode debug skill --print-logs --log-level ERROR`, and attributed the line
to an ocm file. **opencode does not crash**: the same command run directly
exits 0 with the error on stderr —

```
timestamp=… level=ERROR run=e2dce6da message="failed to load plugin" path=file:///…/plugins/ocm-loader.js error="ocmDefectProbeNoSuchFunction is not defined"
```

— and a real TUI session starts (input-ready OK, "Ask anything" reached; the
/ocm dialog even opens — the TUI plugin chain is independent of the broken
server plugin). The user keeps a working opencode and a diagnosis.

---

## Findings

## F232 — any command from an older ocm, even `--version`, downgrades a newer home's loader files (area: CLI, severity: med)

Test:        A7 runner incident + dedicated probe (homes a3-probe2)
ocm version: 0.6.1 running against a 0.7.0 install      opencode: 1.18.32
XDG:         unset
Command:     `HOME=$H ocm061 --version` on a home installed by 0.7.0
Expected:    a read-only `--version` writes nothing; the newer-home guard
             (`refuseNewerHome`) exists precisely to stop an older binary
             writing a newer installation
Actual:      v0.6.1 `--version` prints
             `refreshed the auto-sync loader: 0.7.0 → 0.6.1 (restart opencode to activate)`
             and rewrites all 33 shared loader files to 0.6.1 stamps
             (`// ocm-version: 0.6.1 …` in lock.js, core.js, ui.js, …) while
             the 4 v070-only files (defect.js, digest.js, gate.js, renames.js)
             keep 0.7.0 stamps — a **mixed-version install** the registry
             still records as `ocmVersion: 0.7.0`. A v061 *mutating* command
             is correctly refused (`error: this installation was last written
             by ocm 0.7.0; you are running 0.6.1`), but
             `refreshStaleLoader` (`src/index.ts:138`) runs for every command
             except `help`/`init` — including `--version` — and does not
             consult `refuseNewerHome`, which guards only `mutating()`
             (`src/index.ts:122`).
Impact:      anyone with two ocm versions on PATH (npx vs global, project vs
             user install) silently downgrades their loader files — and with
             the version set mixed, a subsequent opencode start runs 0.6.1
             modules next to 0.7.0-only ones. This is also what made the
             runner incident above a ping-pong: whichever `--version` ran
             last won.
Evidence:    `/tmp/ocm-e2e5/homes/a3-probe2` (grep `ocm-version` there for
             the mixed stamps); the refusal contrast is one `ocm update` away.

## F233 — doctor is silent about `plugin:name` links orphaned by registry loss, though the lock-break warning names it as the remedy (area: CLI, severity: low)

Test:        A7 / step 24 aftermath (home a3-3b)
ocm version: 0.7.0      opencode: 1.18.32
XDG:         unset
Command:     `ocm doctor`, `ocm list`, `ocm update` after a lock broken as
             stale mid-run let two `ocm add`s interleave
Expected:    the release warning says "its writes and ours may have
             interleaved — run ocm doctor"; doctor should surface the damage
Actual:      the loser's marketplace vanished from the registry (`list`
             shows only `slow-two`; `update` reports "already up to date"
             for it alone) while its five `plugin:name` command links and
             cache clone remain on disk and keep loading in opencode.
             `ocm doctor` exits 0 and prints nothing about them: the orphan
             sweep (`src/commands/doctor-orphans.ts`) covers `ocm--` links
             and skill mirrors, not command/agent mirrors. The user who
             follows the warning gets "all clean" and keeps zombie commands
             from a marketplace ocm no longer manages.
Scope note:  the registry clobbering itself is the known brief-37 lock-steal
             race (Part D, not re-filed); what is new is that the prescribed
             remedy does not detect the result. Trigger is rare (a hold
             >10 min broken as stale), hence low — but any registry-loss
             path leaves the same orphaned links.
Evidence:    `work-a3/step24b.py`, homes a3-3b (`ocm list` vs
             `ls commands/` diverge; doctor exit 0).

---

## Cross-cutting invariants (all homes a3-1 … a3-9, a3-probe, a3-probe2)

- no `.claude`/`.agents` under any home (VIOLATION check clean)
- no broken symlinks (`find -type l ! -exec test -e`)
- every registry and opencode.json parses (`jq .`)
- no `registry.lock` left behind anywhere
- doctor exits 0 on every home except where a defect was the point
  (a3-9 step 29, a3-7 pre-fix, a3-5 XDG report)
- no stray opencode/ocm processes of mine remain (the only match is another
  agent's b4 run)

## Notes for other agents

- **B2-11**: the provenance marker `<!-- ocm: rendered from … -->` is part of
  the command template opencode sends to the model (step 21 evidence).
- The schema-mp fixture is a plain directory (no `.git`); add it by bare path,
  not `file://` (the file:// form fails with the F81-family "private,
  unreachable" diagnosis — known family, not re-filed).
- opencode swallows import-stage plugin failures entirely (no log line at any
  level) — only load-stage failures (`server()` rejecting or bad module shape)
  produce `level=ERROR failed to load plugin`. Recorded in oc-probe.sh;
  confirmed empirically in step 29.
